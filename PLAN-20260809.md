# Agentty 临时工作计划（2026-08-09）

> **性质**：临时协调文档，不是规范正本。产品/架构行为以 `docs/specs/`、`docs/quality/development-tracker.yaml`、`docs/quality/traceability.yaml` 为准。本文档可在任务完成后删除或归档。

---

## 1. 当前位置

| 项 | 状态 |
|---|---|
| 上游 | [l0ng-ai/tty7](https://github.com/l0ng-ai/tty7) `upstream/main @ 1fd99ca`（2026-08-09 已 fetch；原审计基线 `8981cc7` / v26.8.2） |
| 本仓 `main`（已 push） | `f1ae37a` — 主要是 CI / Nightly 打包修复 |
| 分叉点 | `6374a08`（fix: restarting server must not strand window on home page） |
| 相对上游 | commit 图为本仓独有 **26** / 上游独有 **344**；另有大量未提交 Agentty WIP，不能用 commit 数代替 feature gap |
| 本地 WIP | 大量未提交改动（Environment 窗、Session Navigator、Surfaces、i18n、Codex 标题解析等） |

**产品路线（与 tty7 分叉处）**

- 一 Environment 一窗口；Sidebar = Agent Session Navigator（Ashide 对齐）
- 规范驱动：`SPEC → tracker → matrix → check → test → implement → verify`
- **禁止** `git merge upstream/main` 整包合并（workspace 模型、命名、UI 正本已不同）

---

## 2. 今日已落地 / 已操作（2026-08-09）

### 2.1 远程连接（dnyx226 / `172.16.70.226`）

- **根因**：本地 WIP 客户端控制通道带 `grok`；远端旧 `agentty-server-c6p5` 未协商完整 typed request vocabulary，最终在枚举反序列化后断线
- **已闭环**：`CONTROL_VERSION=7` 在握手阶段 fail closed；所有 mismatch modal / notice / group error 只提供 **替换服务**，统一走 canonical `ReplaceServer` + bundled-musl-first installer；失败保留可重试 notice
- **实机验证**：`live_replace_remote_helper_full_chain` 已将 dnyx226 从 c6p5 替换为包内 c7p5（sha256 `7e930e…a8c1`），随后 full-chain control link 通过

### 2.2 会话标题（`019fdcef-…`）

- **根因**：Codex 新 transcript 无 `event_msg.user_message`，首条用户消息在 `response_item role=user`；旧解析器读不到 → 永远「Agent 会话」
- **已做**：`codex_user_message` 支持 `response_item`，跳过 AGENTS.md / `environment_context` 注入；SPEC `SESSION-TITLE-PRECEDENCE-11` 已收紧
- **验证**：用户需用 **本地 WIP 构建** 重启 app 并刷新 Navigator

### 2.3 i18n 漏译防呆

- **决策**：保留 `assets/i18n/*.yaml`，**不**迁到 tty7 #372 的 per-language Rust `match` 模块
- **已做**：
  - `I18N-EXHAUSTIVE-TRANSLATION-06`（齐套 key、非空值、禁止重复 key）
  - `script/check_i18n` + `script/tests/i18n_exhaustive_contract.sh`
  - `build.rs` → `validate_i18n_catalogs`（**编译期** fail closed）
  - `src/core/i18n.rs` 单元测试（各 locale 自洽、不靠 cross-locale fallback）
- **以后加 ja-JP**：新增 `ja-JP.yaml` + `Locale` 接线即可；门禁自动要求三语齐套

---

## 3. 上游 tty7 吸收状态（2026-08-09 实证审计）

> 原则：**摘失败语义 + 安装/远程可靠性**，不复制 workspace-switcher 外壳；每项走 Agentty 强制开发顺序。结论：原计划中的可吸收 feature **没有全部开发完**。

### P0 — 远程 helper 错配闭环（已完成）

| 上游参考 | 吸收内容 | 状态 |
|---|---|---|
| `#387` dialect behind | 检测 control/protocol 或 agent 枚举落后 | verified |
| `#389` | 禁止「带着旧 server 凑合连」 | verified |
| `#352` / `#384` | mismatch 对话框走 **replace**；文案「旧」而非「不是 tty7」 | verified |
| `#344` | SSH 安装 **优先 bundled musl**，再联网下载 | verified |

**Agentty 落点**：`REMOTE-HELPER-DIALECT-SKEW-14` / `HELPER-VERSION-MISMATCH-NOTICE-13`；focused tests、presubmit full、dnyx226 c6p5 → c7p5 实机替换均通过。

### P1 — 远程 / WSL 环境 parity（已完成）

| 主题 | 上游 | 状态 / 证据 |
|---|---|---|
| WSL 当 machine：restart server | `#417` | **已完成**：typed `RestartServer` route 通过 `RouteSetup::blocking` 调用既有 `restart_wsl_daemon`；WSL ReplaceServer 仍显式不支持；focused test、static gate、presubmit quick 通过 |
| WSL 路径补全 | `#408` `#418` | **已覆盖且更强**：completion 始终在 WSL distro Host authority 内执行，失败不读 GUI 本地目录；不吸收 `\\wsl$` 本地绕路 |
| 远程 / SSH / WSL 图片粘贴 | `#338` `#399` | **已完成**：SSH 走 owner-only SFTP staging + 完成验证后粘贴远端路径；WSL 转 `/mnt/<drive>`；macOS 远端也 staging，local 保留 SYN；失败不插入不可达本地路径 |
| 远程错误挂在 machine 组下 | `#354` | **已完成**：`ConnectFlow::Failed` 写入 `group.error`，switcher 在对应 Environment 组内渲染 retry / replace |

### P2 — 分发与网络（未完成）

| 主题 | 上游 | 状态 / 证据 |
|---|---|---|
| 应用内更新 + Stable/Nightly 频道分离 | `#309` `#330` `#386` | **部分**：已有 rolling Nightly CI 和 stable notify-only 检查；没有 channel 设置、下载/校验/安装 updater；现测试仍允许 Nightly 被同版本 Stable 提示，未满足“禁止降级” |
| 系统代理（macOS / Windows SOCKS） | `#367` `#364` | **未吸收**：只有 SSH profile 的手工 SOCKS/HTTP proxy；更新检查与 helper 下载没有系统代理解析 / 全局 override |

### P3 — 体验与 CLI（大多未完成）

| 主题 | 状态 / 证据 |
|---|---|
| `agentty wait`（等 agent 态，`#248`） | **已完成**：复用 `AgentStates` + 节流 machine liveness；支持 `--changed` activity edge、结构化 exit、timeout 124 与 deadline-bounded polling；focused tests、CLI package、static gate 通过 |
| 平滑滚动（`#382` / 后续 `#414`） | **已完成**：离散滚轮使用相对累积距离和 GPUI `on_next_frame` 衰减；触控板/应用 mouse reporting 保持 direct；stale epoch、clamp、窗口唤醒和持久化设置均有回归证据 |
| Windows ConPTY / UI 线程（`#415` `#407` 等） | **未完成专项吸收/验证**：有 portable-pty/ConPTY 基础，但没有对应 spec、matrix 与 Windows 回归证据 |
| 可点 toast（`#373`） | **未实现** |
| IDEA 风格 Ctrl+Tab switcher（`#380`） | **未实现其交互语义**：当前 Ctrl+Tab 直接 NextTab；Environment switcher 使用独立快捷键 |

### 原审计基线之后的新增上游 feature（尚未系统吸收）

`upstream/main` 已从 `8981cc7` 前进到 `1fd99ca`。新增候选至少包括 pane 拖拽重排 `#445`、主题选择器实时预览、UI 独立字体大小、modifier+wheel 字号缩放、Windows Mica/Acrylic `#412`。fish history `#421` 已按 `INPUT-FISH-HISTORY-AUTHORITY-09` 完成。`#446` 的尾部省略 + hover details 已由 Agent Session Navigator 的 trailing fade + sidecar HoverCard 独立覆盖；其余尚未进入 Agentty spec/matrix，不能算完成。

### 明确少碰 / 不整包吸收

- **ja-JP + i18n 拆 Rust 模块**（`#372`）：已用 yaml + 漏译防呆替代；不迁 match 表
- **去掉 Outline 右栏 / About 重做**（`#375` `#358`）：产品取舍不同
- **整包 merge upstream/main**：与 Environment 窗 + Navigator 正本冲突
- **CI fork review 流程**：可参考，不进产品路径

---

## 4. 近期里程碑（建议顺序）

### M1 — 远程 helper 产品化（已完成）

1. SPEC：`REMOTE-HELPER-DIALECT-SKEW-14` + `HELPER-VERSION-MISMATCH-NOTICE-13`
2. Matrix + `script/check_remote_helper` 已扩展并 fail closed
3. Failing test 已证明旧 modal / notice 误走 restart-only
4. 实现：握手拒绝、replace-only UI、canonical `ReplaceServer`、bundled-first、失败保留 affordance
5. VERIFY：focused tests、presubmit quick/full、dnyx226 c6p5 → c7p5、重连、DMG / codesign / 安装运行均通过

### M2 — 本地 WIP 收敛与可安装包

1. 提交/整理当前 WIP（UI Surfaces、Session、i18n、Codex 标题）
2. `./script/presubmit quick` → 适用场景跑 full
3. 本地 release 装 `/Applications/Agentty.app`（避免 GitHub Nightly = 旧 main 无 WIP 的视觉回退）
4. POLISH-008 等 UI 项：用户截图 sign-off

### M3 — 上游 P1 远程/WSL parity（按需）

按 Environment 窗口实际使用面逐项 cherry-pick 语义。

### M4 — 上游 P2 更新频道 + 代理

与 fork 的 Nightly 发布流程对齐设计。

---

## 5. 工程纪律（不变）

```
SPEC → tracker → matrix → static CHECK → failing TEST → IMPLEMENTATION → VERIFY → DELETE OLD PATH
```

- 架构/用户可观察行为变更必须更新 spec + tracker + matrix
- Rust 改动：定向测试 → `presubmit quick`；交付前适用 full gate
- 参考 Ashide：提取失败语义、兜底不变量、race 规则；不复制重型框架
- 用户反馈：`FEEDBACK-YYYYMMDD-NNN` 写入 tracker，禁止只留会话记忆

---

## 6. 打开项 / 风险

| 项 | 说明 |
|---|---|
| 本地 vs 远程 `main` 分裂 | GitHub Nightly 仍不含全部本地 WIP；当前已安装并验证本地 release |
| 远程 helper 方言偏斜 | M1 已闭环；旧 helper 会在握手拒绝，用户点「替换服务」后使用包内匹配 musl helper |
| 大量未提交 WIP | 合并/变基前需 stash/分支策略；`refs/wip/packaging-session` 可作备份参考 |
| POLISH-008 | 缺交互截图 sign-off，tracker 仍 blocked on user |

---

## 7. 文档与 ID 索引

| 主题 | 正本 |
|---|---|
| i18n 漏译防呆 | `I18N-EXHAUSTIVE-TRANSLATION-06` / `I18N-EXHAUSTIVE-079` / `FEEDBACK-20260809-003` |
| 会话标题 | `SESSION-TITLE-PRECEDENCE-11` / `FEEDBACK-20260809-002` |
| 远程方言偏斜 | `REMOTE-HELPER-DIALECT-SKEW-14` / `HELPER-VERSION-MISMATCH-NOTICE-13` / `FEEDBACK-20260809-001` / `FEEDBACK-20260809-005` |
| 开发阶段策略 | `DEVELOPMENT.md` |
| 工程合同 | `AGENTS.md` |

---

*最后更新：2026-08-09。下次大方向变更时重写或删除本文件。*
