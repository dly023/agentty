# Agentty project skills

Project skills encode stable workflows only. Feature state and architecture live in `docs/specs/` and `docs/quality/traceability.yaml`.

Required skills:

- `agentty-architecture-change`: spec-driven architecture workflow.
- `agentty-rust-check`: minimal effective Rust verification workflow.

不得创建保存某一功能“当前实现细节”的 feature-memory skill。
