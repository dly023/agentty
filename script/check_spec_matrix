#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

[[ -x script/check_structured_docs ]] || {
  echo "[spec-matrix] missing executable script/check_structured_docs" >&2
  exit 1
}
./script/check_structured_docs spec-matrix
echo "spec matrix checks passed"
