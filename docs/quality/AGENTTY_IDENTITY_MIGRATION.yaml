version: 1
status: complete
policy:
  canonical_product: Agentty
  canonical_config_env: AGENTTY_CONFIG_DIR
  canonical_config_dir: .config/agentty
  legacy_runtime_fallback: forbidden
phases:
  - id: identity-runtime
    status: verified
    evidence:
      - commit 3d7dded Agentty runtime identity, config dir ~/.config/agentty
      - Agentty.app deployed with com.dly023.agentty bundle id
    scope:
      - config root and environment override
      - desktop display name and package metadata
      - user documentation and developer harness
  - id: identity-executables
    status: verified
    evidence:
      - commit 86a19d5 atomic rename of crates, binaries, helper paths, release assets
      - full workspace tests green (894 gui / 840 core / 79 cli / e2e+server+stdio)
  - id: identity-protocol
    status: verified
    evidence:
      - commit 86a19d5 TTY7_* -> AGENTTY_* across wire markers, sockets, hooks
      - rg scan: no tty7/TTY7/Tty7 residue outside CHANGELOG and upstream fork branch urls
legacy_allowlist:
  rationale: Rename is complete; only externally-required legacy references remain.
  categories:
    - historical CHANGELOG entries
    - upstream fork dependency urls (l0ng-ai/zed, l0ng-ai/gpui-component) pinned to branch=tty7
exit_criteria:
  - no user-visible agentty product branding
  - no legacy config authority
  - Agentty.app installs and launches with version 0.0.1
  - remaining legacy identifiers are only those explicitly listed above
