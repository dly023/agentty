version: 1
updated: '2026-09-11'
verification_notes:
  - area: remaining_requirement_audit
    status: reviewed_not_complete
    owner: Codex
    finding: Feedback entrypoints still described unapproved implementation, proposed workspace-root selection and absent history title subscriptions after later user decisions and verified implementation. These are stale execution directions, not new requirements. Retain historical evidence but retire superseded directory-model proposals and route current work to canonical specs.
    current_evidence: Current source has render_machine_rail, shared machine pin/fold/activation, default-terminal plus alternate submenu, whole-tab Sleep handler/tests and history title projection. Full96752 plus final52 history tests verify the title consumer; v18 package and ordinary-shell native lifecycle were verified separately. Source presence is not native interaction or provider/SSH acceptance.
    unresolved: Native click/screenshot permission, selected SSH test target/deployment scope, real provider test scope, live-shell original-slot Resume behavior and development updater policy. Do not choose shell termination/input loss, an arbitrary remote target or upstream-update changes without the missing decisions. Passing mock/GPUI/ordinary-shell tests do not close these requirements.
    next_action: Complete native interaction and real provider/SSH acceptance when permissions/scope are supplied; resolve SESSION-SLOT-17.live_shell_decision before implementation. Do not reimplement superseded workspace-root UI or the now-present title consumer.
  - area: history_v18_native_acceptance
    result: Exact DMGbb535858 launched isolated GUI13660/native window39467 at1440x900 and daemon13663/shell13667. Normal NSRunningApplication termination with exact executable check exited GUI0 da744b; daemon/shell retained PIDs/start times af707b. Reopen GUI14217/window39479 retained workspacefb5800c2-0d9b-4b78-b33d-31974be414a9, tabec0ae2be-6842-4786-85f6-2cc5da1e6667, live pane1 a9dbf9. Explicit CLI exit targeted only fixture pane1; shell exited0 and target kept stopped identity9a4df9. After another normal GUI quit/reopen14995, same stopped records/code survived and server had zero running panes51f268. No provider /exit, Resume/Rejoin, screenshot or click acceptance claimed.
    cleanup: All three GUI runs terminated normally after exact PID/executable checks; last GUI exited0 83a23d and only fixture config-scoped server stop returnedtrue0e3369. Provider marker SHA256e773938eeffbb4cebf7eab10e0cf972404754e098ab3f959a4bbdd4f473a4f6c stayed unchanged through launches. Remaining native UI permissions and actual provider/SSH scope still require user action.
    status: in_progress
    owner: Codex
    permission_preflight: Native arm64 Swift returned accessibility_trusted=false and screen_capture_allowed=false c5e362. No desktop automation tool is exposed. Do not inject input or claim screenshot/click acceptance; no permission prompts were issued.
    plan: Read-only mount exact DMGbb535858; launch with /tmp/agentty-v18-native-mIrd2W isolated config, zsh-f, isolated Claude fixture and inherited TTY7_CONFIG_DIR unset. Disable updates, CLI installation, agent auto-restore and tray. Observe native window plus target identities, normal quit/reopen, then retire only exact fixture processes. This is new-package ordinary-shell lifecycle evidence, not provider/SSH or rendered interaction acceptance.
  - area: history_v18_package
    artifact_result: aarch64 musl build70712 passedd592de; scratch helper gate passed686895 before exact canonical replacements2a141f. Canonical ad-hoc packaging87489 passedf6d1a6. DMG dist/agentty-26.9.1-macos-arm64.dmg SHA256bb5358581498dd1716ece1123f485a71d8e8208a04417c2eaa2b9f79d9d401a3. Old helpers retained under /tmp/agentty-v18-package-rH2WDM/prior-helpers, old DMG under prior-v17.dmg. No foreign binary executed or target deployed.
    payload_verification: DMG checksum/read-only mount passed6eb6ad; mounted deep strict signature, arm64 UUID matching fresh GUI, numeric26.9.1 plist, Agentty product/tty7 executable, CLI26.9.1, server control18/pane6/source731bc414 and both helper checks passed448787/32cb68. Mounted GUI SHA25609a3bf304933d8fec5cd7c366cb2fc1c6136553fb342df04995ee3e5f3d5f6e6; signing changes bytes from unsigned build. Signature adhoc/no TeamIdentifier. Mount ejected32cb68; final source hashes unchanged. Quick39369 passed470e8a. No native GUI launch, provider Resume, SSH, installation, notarization or publication claimed.
    helper_hashes:
      x86_64: f7db21d3e48f943dbdf80265ecbf8ea0be6f0379a4b99951c5672f7fe3379d36
      aarch64: 6378bd1c5cdd25e263813a599f21b1b6f2dd87e2432a766c410c556300445bea
    next_action: Accept the fresh bb535858 package in isolated native GUI/provider lifecycle tests and an explicitly selected SSH target. Existing old-package lifecycle evidence does not verify this package. Resolve remaining native permission and provider/remote scope choices without touching installed app or real sessions.
    native_build: Locked offline arm64 release15606 passed8b00da in2m52s. Server control18/pane6/source731bc41434ccde3625ff0e2556bbeac2094d3989aa4cec34b632fd1674835f75; old helpers rejected47b2fb. GUI source hashes unchanged61b9ee, unsigned GUI SHA25620862d5d9b8a8e60e40b2b495b31c76f410bb4410a366321b8fd6b21d57d6b4d and UUID7CBEA42C-BF0A-32FA-8972-35E8F31ADCA7a4f8ee. x86_64 musl locked offline build38075 passedc67697 in56s; aarch64 build next. Helper fixtures4/47 passed614402, package fixtures3/24 and cleanup guard passed156185. Initial package fixture filename was wrong and corrected, no assertion changed; dwarfdump Rosetta failure corrected with arch-arm64.
    cleanup_result: Package-scoped dev clean removed298 regenerable files/4.6GiB79ce4c after dry-run81b2b8; disk5.5GiB free. Prior DMG backup hash matches2609656f. Native arm64 locked offline release build15606 started; GUI source hashes captured7d6457. No source edits during build, no user configuration or installed application touched.
    status: in_progress
    owner: Codex
    plan: Rebuild matching native arm64 GUI/server/CLI and both Linux musl helpers sequentially with pinned locked offline Cargo. Retain existing full96752 production baseline and final52 history tests/quick45367. Validate helpers in scratch before replacing canonical assets; run existing package cleanup/identity gates, create ad-hoc DMG and inspect mounted payload. No install, remote deployment, notarization or provider mutation.
    preflight: No cargo/rustc active129d84; about1.6GiB free. Native release server remains control17. Prior DMG2609656f copied to /tmp/agentty-v18-package-rH2WDM/prior-v17.dmg before any cleanup. Clean only tty7 dev package artifacts after dry-run; preserve release files, helpers and user state.
  - area: routed_stdio_fixture_config_isolation
    final_gate: After fixture correction, quick96803 exited0 ea67c7. Bounded source auditae461f confirms one production title watcher/parser; changed source hashes recorded there. Full60830 is the IndexSource source baseline before the test-only fixture fix, not proof that its original routed fixture was isolated.
    status: fixture_corrected_focused_verified
    owner: Codex
    finding: Full60830 remote_router test logged machine tree at /Users/admin/.config/tty7/machine.json (9e0c27). Sourcef82372 confirms a_routed_connection_reaches_a_real_server passes --stdio --serve and a temporary --control-sock, but no --config-dir. A temporary socket does not isolate control_services MachineStore or its automatic pane-title watcher. Do not repeat this route against default user state.
    plan: Keep routed real-server coverage and all file/RPC assertions; pass the existing tempfile directory as explicit --config-dir and assert the child creates machine.json there. Audit startup side effects read-only; do not rewrite or restore real state without a proven baseline and user approval. Verify the focused test before any further full run.
    verification: Safe argv assertion failedd61ef5 before any listener/thread/child launch, then explicit private --config-dir and preseeded pane-free MachineStore readback were added. Initial fixture called workspace_create with name in ID position78a437; corrected only the argument order. All three remote_router tests passedcd9d93 and logged the temporary machine.json path. All original file/RPC assertions retained. Adjacent stdio_conformance already sets TTY7_DATA_DIR to its temporary sandbox67611e; no broad test rewrite performed.
    impact_limit: Read-only startup audit8ee21a/67611e confirms MachineStore loads real state and title publication can persist changed PaneFacts; source opening alone is not a no-write guarantee. No pre-run content/hash baseline exists, so prior real-state mutation cannot be ruled out from the mtime30017b while the installed daemon is active. No restoration or real config/provider edits were attempted. Installed GUI24715/daemon24719 remained running; no test build process remained30017b.
  - area: open_history_live_title_gap
    scan_failure_quick: Final quick45367 exited0 eddcab. Current native release server still reports control17/source33b4405e a7237d; bundled Linux helpers retain their v17 timestamps. Disk has about1.6GiB free and tty7 debug package build directory alone is2.4GiB. Next rebuild source-matched native and Linux release payloads after scoped regenerable dev-cache cleanup; no cleanup, package replacement or deployment performed in this test-only slice.
    scan_failure_result: Missing-test source guard REDb94997 preceded two real-control GPUI cases. Nine title tests passeda6df8b. Removing only the dismissed-scan check caused an exact snapshot-is-none failure665596 after the Host capture completion barrier; check restored immediately. Final history-filtered suite passed52 tests68db2c with no ignores, formatting/source checks pass. Test-only expansion; prior full96752 remains the production-code baseline, not a full rerun of these new tests. No native/provider/SSH or package acceptance claimed.
    scan_failure_plan: Extend the existing scripted Peer with a paused discovery response and one-shot scan reply. Verify normal CloseWindow before discovery completes using Host capture release as the completion barrier; no rows, title interest or pane may appear. Verify target error and newly missing provider refresh preserve the prior enriched snapshot without reopening interest. Test-only expansion first; no new production abstraction or provider I/O.
    manager_full_gate: Frozen-source full96752 exited0 6d64b6; GUI1939/core1370/CLI138, six source-identity and all server integration suites passed with unchanged ignored counts. Corrected remote_router fixture logged temporary machine.json e5ed55. This supersedes pending full in manager_result only. Additional paused-scan/failure tests, native/provider/SSH acceptance and fresh v18 artifacts remain open.
    manager_result: GUI consumes canonical Host title subscriptions off-thread and projects exact identity-bound title batches without Resume or pane creation. Window-close regression RED43867d preceded explicit dismissal through prepare_window_close and Drop; refresh, scan completion and title delivery reject dismissed managers. All seven GPUI title tests passed25eb32, including actual socket delayed setup, refresh retirement and retained-app CloseWindow. Formatting, source/harness checks and quick23212 passed88c2ab. Full and additional delayed-scan/failure acceptance remain pending; no native/provider/SSH acceptance or refreshed package claimed.
    window_close_plan: Retained-app GPUI fixture now initializes WindowRegistry. Behavioral RED43867d times out waiting for TitleWatchClose after CloseWindow. Add a manager dismissal flag and shared cancellation method to normal window-close preparation and Drop; reject refresh, scan completion and title delivery after dismissal. Keep raw subscription disposal off the UI thread. Focused lifetime tests and quick/full remain required.
    manager_plan: Consume the verified Host subscription after a successful current scan. Keep its lifetime entirely in the existing blocking background lane, pass only a receiver/cancel signal to UI, and retire late setup after close. Add exact Host/interest guards and transactional title-only projection, retaining selection, aliases and in-flight action intent. Reuse current manager layout/error style with a separate localized title warning. Prove delayed-name/no-pane rendered flow and close/refresh/foreign/stale failure behavior before implementation.
    wire_final_static_gate: Supplementary exact-cleanup guard initially misread parentheses as regex groups and quick96654 failedfe37cf. Changed only to fixed-string matching; four positive/negative guard cases passed9a7cb4 and quick29433 exited0 27bc17. No Rust changes after full28237. Old UUID-only retire_id helper is removed and rejected by the source guard. Final source hashes captured2fd4d1; existing packages remain stale.
    wire_full_gate: Frozen-source28237 exited0; 58 control tests, quick and full passed. Full GUI1932/core1370/CLI138 plus six source-identity and all server integration suites passed; existing ignored counts unchanged. Fourteen title-filtered tests passed before the freeze. New dialect18 and core identity invalidate existing v17 helpers/DMG; no deployment, real-provider store mutation or native GUI acceptance performed.
    wire_remaining: The target-to-RemoteHost path is implemented and verified with real control sockets plus injected target sources. HistoryManager still consumes only manual scan snapshots. Next connect its actual lifetime and filtered selection to these events, keep source handles and their blocking disposal off the UI thread, and guard each delivery with manager generation, exact Host instance and subscription identity. Verify open/close/refresh, delayed no-pane name, replaced connection, aliases and search before rebuilding artifacts.
    wire_result: Dialect18 Open/Close/HistoryTitles now routes through RemoteHost and Conn to the canonical Host subscription. Fourteen focused history_title tests passede652b3, including real control connections, events before acknowledgement, target source disposal, paused-source close/cancel/disconnect and late cancellation after reply. Cancelled-close RED94c855 and reused-token close RED338817 required exact opening-request capture and cleanup before cancellation return. Shared protocol fixture/deadline coverage is expanded; quick/full and GUI integration are next, not yet complete.
    wire_plan: Implement dialect18 typed Open/Close/HistoryTitles through RemoteHost and Conn. Reserve at most16 interests before queued validation; cancellation/close invalidate immediately and late install/failed reply cleanup require exact opening request identity. Client pre-registers a UUID and coalesces a one-slot receiver; disconnect closes registry. Test target reservation races and real control-wire delivery/close/error without using real provider state. GUI remains the next required consumer.
    target_subscription_full_gate: Frozen-source quick/full46326 exited0 (final17dec7); GUI1932/core1358/CLI138 plus source-identity and server suites passed with existing ignored counts unchanged. Corrected routed fixture logged temporary machine.json. This verifies target code only; Host/control and GUI still require integration.
    host_boundary_plan: Introduce the canonical Host subscription method and shared receiver/lifetime handle; LocalHost delegates off-UI, unsupported backends fail closed. Test receiver closure and exactly-once backend disposal, LocalHost bounded rejection before provider reads, and remote unsupported refusal without local fallback before wiring typed remote transport.
    host_boundary_result: Static RED8a526b and missing-API REDcd3907 preceded Host.watch_agent_session_titles, LocalHost delegation and shared lifetime guard. Five history_title-filtered tests passede2c98d, including cloned-receiver closure before exactly-once backend disposal and oversized local interest rejection. RemoteHost still inherits Unsupported deliberately; typed connection-owned wire and HistoryManager consumers remain mandatory, not optional follow-up.
    host_boundary_full_gate: Frozen-source74310 exited0; eight live_titles tests passed621234, quick passed9ce924 and full passed through final stopped_resume5 plus stdio55. Core1361 passed with four existing ignored; routed fixture again logged its temporary machine.json. No native/provider/SSH acceptance or matching package claimed.
    wire_next_slice: Source auditf66c46/cb369c confirms server submit queues jobs concurrently and ControlClient timeout sends Cancel after forgetting the request. Specify ordered connection-level reservation before worker dispatch, exact request/subscription ownership through late cancellation, and early-event registration before open RPC. Reuse this Host lifetime and canonical target factory; test close-before-open-completion, duplicate token, queue refusal, late Cancel after reply, disconnect and one-slot client coalescing. Existing file WatchOpen reply-before-event ordering alone does not guarantee a client has registered its receiver.
    target_subscription_result: Typed HistoryTitleSubscription now validates bounded distinct source rows, uses IndexSource without MachineStore, emits title-only Names or Unavailable, preserves complete names on missing/partial source, revalidates source identity for changed epochs, coalesces through a one-slot nonblocking channel and joins cancellation. Seven focused live-title tests passed09394b. Static REDc8c101 and missing-API RED453e8f preceded implementation; client-title echo REDb8040b then required provider-index-only publication. No real provider stores were used.
    target_subscription_integration_debt:
      owner: Codex
      status: in_progress_not_for_release
      removal_condition: Connect watch_history_current through connection-owned target Host/control subscription and current HistoryManager lifetime before packaging or claiming feature completion; do not leave a test-only backend as a permanent unused API.
      verification: Wire opening/cancellation/early event/disconnect/close, cross-Host stale deliveries, alias precedence and filtered selection tests plus real GUI/provider acceptance. Current filesystem tests prove target subscription only.
    subscription_plan: SESSION-HISTORY-TITLE-26 defines bounded validated target subscriptions and full Host/manager lifecycle. First implement the target subscription using IndexSource, typed title-only snapshots and one-slot coalescing; validate all requested source identities before subscription and before changed publication. Remote transport and UI integration remain mandatory next slices; do not expose a local-only UI fallback or ship an unused backend as completed functionality.
    source_full_gate: Frozen-source quick/full session60830 exited0 19a7d5. GUI1932 passed/one existing ignored, core1356/four existing ignored, CLI138, source identity6 and all server suites including stopped_resume5 passed. No new ignored tests or retired assertions. Integration fixture isolation finding above prevents treating green full as proof of test isolation.
    source_result: IndexSource now owns the sole directory watcher, dropped-event repair, bounded epoch-checked parser and complete-name cache; the existing pane worker consumes it. New real-filesystem test requires no MachineStore and covers initially absent index, delayed official name, rejected partial parse with prior cache retained, atomic file replacement and directory replacement. Five live-title tests passed160fa3; no pane or history membership is fabricated.
    source_red: New test initially failed compilation because IndexSource was absent (d16fb0). Static guard first ran before set -eu and did not fail as intended5e89e3; corrected its placement, then missing IndexSource guard failed8d766b before implementation. No gate bypass retained.
    artifact_impact: Core source identity changes with this extraction. Existing bundled helpers and DMG2609656f predate it; do not claim current-source packaging or deploy them as matching helpers until the final title integration is verified and rebuilt.
    source_slice: First extract the existing watcher/parser/cache as a pane-independent IndexSource used by the current pane worker, without adding another watcher implementation or unused public API. Add a real-filesystem no-pane delayed-name/partial/atomic-replacement test before extraction. Target Host subscription/remote delivery and manager reconciliation remain the next required integration, not completion of this source slice.
    status: confirmed_implementation_gap
    owner: Codex
    contracts: [SESSION-TITLE-06, SESSION-LIVE-TITLE-12, MACHINE-SCAN-04]
    finding: An open HistoryManager renders and filters HistorySession.title from its saved ScanState snapshot. Its sole subscription is to the search InputState; refresh is called on open or explicit refresh. Target live_titles only reads/publishes titles for MachineStore pane bindings and skips parsing with no bindings. A discovered history-only conversation therefore has no live update route; pane title updates also do not enrich the open manager snapshot.
    source_evidence: Read-only590ddc/7d2e40 inspected HistoryManager fields, open/refresh, visible/history_matches and row/detail rendering plus the target watcher binding collection/empty-binding branch. This is source-path evidence, not a native UI reproduction. Existing cold-reader and PaneFacts title tests do not cover this missing subscriber/target-history interest path.
    impact: Official naming is implemented for cold discovery and known pane rows, but not promptly projected into an already-open history manager. The earlier approximate95-percent implementation assessment must not be interpreted as all remaining gaps being acceptance-only.
    implementation_boundary: Cover valid discovered conversations even when no matching pane exists. Reuse the target bounded title parser and existing source invalidation, with a bounded ephemeral interest/projection for validated snapshot identities. Do not patch only resident pane titles, rescan history membership on each turn, poll local files for a remote manager, or create durable provider facts from client input.
    required_cases: Delayed title with no pane, same identity on another Host, renamed matching search result/selection, user alias priority, partial/missing/replaced index, source epoch during parse, source/connection replacement, close/reopen and stale subscription delivery. A failed title refresh keeps the last complete snapshot and cannot start Resume/Rejoin or change membership/identity/cwd.
    next_action: Specify the canonical target-to-open-manager metadata subscription and lifetime before implementation; then add static coverage and a failing no-pane delayed-title test. This is independent of native permissions, SSH target choice and update-policy approval.
  - area: installation_update_guide_audit
    status: guides_synchronized_update_policy_pending
    owner: Codex
    contracts: [PRODUCT-BRAND-01, PACKAGE-CLEANUP-02, QUALITY-VERSION-04]
    finding: Installation guide claims upstream releases distribute this fork, uses retired tty7 package names, promises notarization and stable Rust, and describes source builds as categorically unable to update. Current packagers emit agentty names, macOS signing is conditional, CI/presubmit pin nightly-2026-07-31, and updater still reads l0ng-ai/tty7 plus tty7 package names. No separate fork release feed is established.
    plan: Correct existing installation/update guides from canonical packaging/update source. Distinguish artifact patterns from actual platform acceptance, local ad-hoc package from signed releases, and upstream feeds from Agentty distribution. Preserve core executable/config names and avoid any install, update, provider or user-config side effect.
    update_policy: Asked whether development builds should disable update checks/download/install while retaining explicitly labeled upstream release links. No answer yet; do not silently change runtime update policy or claim documentation fixes the feed boundary.
    result: Both guides distinguish local Agentty artifacts from upstream distribution and conditional signing; package names match scripts, technical tty7 executable/config names remain, build command selects pinned nightly and three real packages with --locked. Removed nonexistent Windows installer context-menu task claim. Updates guide describes current upstream feed, manual-action versus automatic-check flags, Linux manual-package fallback and Windows elevation prerequisites without claiming actual platform acceptance.
    verification: Guide navigation/frontmatter/local links/component/fence checks and negative stale package/toolchain/signing assertions passedbddd4f. Quick session52156 exited0 7ea4c0. Source update endpoints/asset policy/forced checks and canonical packaging/CI selectors inspectedc1fcba/915ef1/0a2c24/de2faa. No Rust changes or full Rust rerun; no MDX rendering, update request, install or configuration write. Latest DMG checksum remains2609656f5f02bead0dd0c1147781a15213eb860925c21cf04231cb67d35b613c (5540cb).
    next_action: Complete guide validation; promote the confirmed update-policy choice into SPEC, tracker, matrix and failing tests before runtime implementation.
  - area: machine_guide_entrypoint_followup
    status: selected_pages_synchronized
    owner: Codex
    contracts: [MACHINE-ENTRYPOINT-25, PRODUCT-BRAND-01, SESSION-EXIT-RETENTION-13]
    finding: Current command-palette guide still advertises removed workspace create/rename/stop/delete; customization keybindings still advertises New workspace. Shortcut reference falsely says switcher cleanup remains underway. Homepage implies shells survive host reboot and presents upstream performance as current fork measurement; site metadata remains tty7.
    plan: Align these five existing documentation files with current machine-only source and lifecycle contracts; preserve technical tty7 identifiers and upstream attribution. No runtime behavior, SPEC semantics, package or public site deployment changes. Validate navigation, frontmatter/local links and removed claims; run quick.
    remaining: Installation/download/update and broader site pages require separate source-backed audit; do not imply upstream release URLs distribute this local fork.
    result: docs.json now names Agentty and labels the release link Upstream releases without changing its URL. Homepage distinguishes surviving processes from explicit recovery after target reboot, labels the inherited screenshot/performance as historical upstream material and keeps core tty7 attribution. Palette and both shortcut guides no longer advertise retired workspace actions or claim their removed forms still exist.
    verification: Navigation membership/existence, selected YAML frontmatter, local page/asset links and component balance passeddda0f9. Removed table actions/shortcut, reboot-overclaim and brand assertions passed24a5e2. Quick session24056 exited0 a9855d. Source palette/keymap/theme and current MACHINE-ENTRYPOINT-25/DEVELOPMENT.md were checked before edits. No Rust or package change; installed-product and provider/SSH acceptance unaffected.
    render_limit: mint and mintlify executables were not found15ac56; no tool installation, MDX compilation, site preview, screenshot or deployment performed. Structural checks are not rendered UI acceptance.
  - area: final_cleanup_native_package
    native_gate: Evidence/fixture-only turn, no Rust edits. Quick session40634 exited0 504fef. Final process check5a7601 found all four fixture PIDs gone and installed GUI24715/daemon24719 untouched; DMG hash unchanged. Frozen-source full18274 remains source baseline, not a substitute for remaining native/vendor/SSH acceptance.
    native_result: Current DMG2609656f mounted read-onlyc9488b; packaged GUI has expected e2de806f hash and passes strict signature0fbb51. Fresh isolated GUI64064/window39428 at1440x900, daemon64067/shell64079 and workspace9dd1956d-5892-4cda-b499-d24ccf73094c/tab6999e188-c560-4ad2-b74f-09cdb9e01c26/pane1 observed5c1bf6/ce2df0/ba84e5. Exact PID/executable normal termination returnedtrue ba84e5 and GUI exited0 dcddec. Daemon/shell and live identities survivedbc506a. Reopened GUI65016/window39441 at same geometry and same daemon/shell/pane identitiescc346a/1f1f02. No replacement shell observed. This proves isolated ordinary-shell live quit/reopen, not rendered tab appearance, stopped output, vendor Resume/Rejoin or remote lifecycle.
    native_cleanup: Second exact PID/path normal termination returnedtrue1f1f02 and GUI exited0e4f4d3. Config-scoped fixture server stop returned stopped=true468b00; read-only mount ejected. Provider fixture hashdd812b8c8ec3ea972c0bb2809d8f95219e8e277afd23b3a79693d9a2808ca7c7 stayed unchanged across both launches and cleanup. No real provider configuration, installed application or real session was used as a fixture.
    current_native_plan: Verify exact DMG2609656f in read-only mount with fresh /tmp/agentty-current-native-8K8OwV config, zsh -f, isolated ZDOTDIR/Claude fixture and inherited TTY7_CONFIG_DIR unset. Disable updates, CLI installation, tray and agent restore. Observe window and pane identities, normal quit/reopen, then stop only isolated fixture daemon. No native input/screenshot or real provider/SSH acceptance implied. Permission preflight543cbe remains false/false.
    status: artifact_and_isolated_live_reopen_verified
    owner: Codex
    contracts: [MACHINE-ENTRYPOINT-25, PRODUCT-BRAND-01, PACKAGE-CLEANUP-02, BUNDLED-HELPERS-06]
    source_baseline: Frozen-source full18274 passed after final unused GUI helper removal; no new Rust edits are planned for this artifact refresh.
    plan: Rebuild locked offline native arm64 GUI/server/CLI, validate existing Linux helpers through the canonical gate, then use canonical ad-hoc macOS packaging and verify the mounted payload and signature. Preserve installed application and real sessions; previous native lifecycle evidence stays specific to its old artifact.
    backup: /tmp/agentty-final-package-OeBnTW/prior-gui.dmg
    backup_sha256: e51f8d1513225f1df8bb823c5bd01254f5fd4d9977ac63250869b1230f13901c
    preflight: No Cargo/rustc process was active; disk had 925 MiB free (7cd97b). Package-specific tty7 dev cleanup started before release build; release/helpers/user state are outside its scope.
    cleanup_result: Package-scoped dev clean completed715cbc, removing442 regenerable files/6.1GiB and leaving6.5GiB free. Release outputs, helpers, backed-up DMG and user state were preserved.
    artifact: dist/agentty-26.9.1-macos-arm64.dmg
    sha256: 2609656f5f02bead0dd0c1147781a15213eb860925c21cf04231cb67d35b613c
    native_gui_sha256: e1ee5242bddc5eb230201707f602d040dcb9dad6ec3dda157d8df792e2a5e597
    packaged_gui_sha256: e2de806f0ac13b7cbb0b53682b56f5217fa0649fda7fed08fac174174883b369
    gui_uuid: 3F932F9C-28D9-3152-AF18-816DD898F7FD
    build_result: Single locked offline arm64 release session47834 exited0 in2m30s b84855; warnings remain and were not suppressed. Seven affected GUI source hashes stayed unchanged3e68ab/cbe740/c5de69/9fca45. Native helper gate4f4b3a passed; canonical ad-hoc package session57797 exited0 143a74.
    verification: Package cleanup tests3/24 assertions4dc4dc and helper tests4/47 assertions3fb2d5 passed. Quick session95910 exited0 77fdd1. DMG checksum VALID/read-only mountf30147; mounted deep strict signature/plist/arm64 UUIDfa259a match the fresh build UUID. Packaged server control17/pane6/source33b4405eaeb6b9dc46e4b68594734d55cbc1184761b5a952709caba59a036e37, CLI26.9.1 and both bundled helper checks passed0d59ce. Agentty display name, Applications link and ad-hoc/no-TeamIdentifier signing verified9fca45. Signing changes raw executable hash; no raw-byte equality claim.
    verification_limits: Initial package refresh had no GUI launch; native_result now supersedes that limit only for isolated ordinary-shell live quit/reopen. No native click/screenshot, provider request, remote deployment, installation, notarization or publication. Older DMG e51f8d15 retains its artifact-specific stopped-shell lifecycle evidence below, not proof of stopped/vendor recovery on this new package.
    final_cleanup: Read-only mount ejected79b246; new DMG and old backup hashes reconfirmed. Installed GUI24715 and daemon24719 still run from /Applications/tty7.app. Canonical packaging replaced only current arm64 package/stages; prior DMG can be recovered from the recorded backup.
    next_action: Complete isolated real-provider and native interaction acceptance; real SSH needs the requested Host alias and test deployment scope. Do not expand source hygiene instead of completing user-facing acceptance.
  - area: machine_only_gui_package
    status: artifact_and_isolated_shell_lifecycle_verified
    lifecycle_gate: Artifact/fixture/evidence-only turn, no Rust source edits. Quick session12641 exited0 (350684); dedicated tracker/matrix YAML validation passedf298a2. Source full37178 remains the frozen-code baseline; real shell lifecycle does not substitute for remaining vendor/SSH/native interaction acceptance.
    native_lifecycle_result: Exact DMG e51f8d1513225f1df8bb823c5bd01254f5fd4d9977ac63250869b1230f13901c mounted read-only59297f; packaged GUI hash f181b2d314b01eada7dea84f10977c7fc6ec92df81e0f0148732ceb869d63e2f and arm64 strict signature verified596110/cdfda8. First GUI24183/window39346 at1440x900, isolated daemon24288/shell24293, workspacea20135aa-1d8d-489b-88b3-ebb1c7769f01/tabe978bbc9-5ae6-4103-a0f4-45aa0bb27f61/pane1 observed0220e1/b81c30. Exact executable/PID normal quit0a6a6b returnedtrue and GUI exited0 ef3aeb; daemon/shell and target identities survived52c03c. Reopened GUI25902/window39364 with same1440x900 geometry, daemon/shell and identitiesf895b3. This supersedes the no-GUI-launch portion of earlier payload notes, not native click/screenshot or provider acceptance.
    native_shell_exit_result: Config-scoped CLI sent exit only to verified test pane1 deeaef. Shell24293 disappeared; pane1 became live=false while original tab/workspace stayed225b74, and private scrollback/1.bin remained227bytes. Second GUI normal quit returnedtrue/exited0 b2aedc. Third GUI27128/window39378 reopened same1440x900 geometry; target pane/tab identities remained, live=false and no child shell under daemon24288 (1090b9/de8a00). No automatic replacement was observed. This is ordinary-shell record retention, not vendor /exit semantics, actual rendered stopped-label proof or Agent Resume.
    native_hook_fixture: Isolated provider/settings.json SHA256308be6c69d8675bd1187b002fa01c92c76d4077df01737a4cb048ada2041715a remained unchanged across three launches and both live/stopped reopen cycles. GUI had --config-dir and inherited TTY7_CONFIG_DIR unset; only CLI queries/fixture commands set TTY7_CONFIG_DIR. No real provider configuration was edited.
    native_shutdown: Third exact-PID/path normal termination returnedtrue de8a00 and GUI exited0 d35da1; config-scoped server stop returned stopped=true e4c6cd. Final PID checkc60bb5 found no test GUI/daemon/shell; installed GUI24715/daemon24719 remained running. Fixture/provider and DMG hashes unchanged, mount ejected; fixture data retained.
    native_warning: Launch-command output contained an xcrun architecture mismatch. Controlled read-only104941 reproduced the identical warning with only /usr/bin/true in the same directory and no GUI; login=false a0cb5c then exited0 without output. This establishes a host login-shell initialization source, not an Agentty failure. Explicit arm64 signature verification passed. Future native commands should use login=false to separate target logs from host startup hooks; do not inspect/change unrelated shell configuration or repair global tools for this task.
    native_lifecycle_plan: Revalidate current DMG hash and mount read-only; launch its GUI with a fresh /tmp/agentty-native-machine-smoke-IXM3lc config, inherited TTY7_CONFIG_DIR unset, isolated ZDOTDIR/Claude fixture, zsh -f, updates/CLI installation/tray/agent auto-restore disabled. Observe GUI PID/window and isolated daemon/pane identities, normal-quit only exact verified test executable, reopen and compare identities. Stop only isolated test daemon afterwards. No native clicks/screenshots, provider execution or real user configuration mutation implied.
    native_exit_extension: After live quit/reopen, explicitly exit only the isolated ordinary-shell pane through the config-scoped CLI. Verify target tab/pane records survive the observed exit, then normal-quit/reopen again and check no replacement shell was automatically spawned. This covers ordinary-shell retention, not vendor CLI /exit semantics or Agent Resume.
    owner: Codex
    contracts: [MACHINE-ENTRYPOINT-25, PRODUCT-BRAND-01, PACKAGE-CLEANUP-02, BUNDLED-HELPERS-06]
    plan: After frozen-source full passes, rebuild native arm64 release GUI/server/CLI sequentially with locked offline Cargo. Reuse the unchanged core/server Linux helpers only if canonical static/architecture/source-marker gate passes. Run canonical macOS packaging with signing/notarization credential variables unset; validate DMG, mounted payload, strict ad-hoc signature and GUI executable freshness. No installation, publication or real session mutation.
    backup: /tmp/agentty-machine-ui-package-5mKyah/prior-gui.dmg
    backup_sha256: 4f6e88c13f3dc7d4a0b08a42f69ad1970e2620f117c50ef205d5ea63b72fb8fb
    artifact: dist/agentty-26.9.1-macos-arm64.dmg
    sha256: e51f8d1513225f1df8bb823c5bd01254f5fd4d9977ac63250869b1230f13901c
    build_result: Locked offline native arm64 release session90499 exited0 in2m31s (10489a). Canonical helper gate and macOS packaging session23099 exited0 (2852af). No Rust edits after full37178; five changed GUI source hashes match before/after release and packaging48dd31/3ffdbe/cf6332.
    native_gui_sha256: a3730d4e6c59f0b2a17c282de7e10d39a7203c631c0641b8a24feb02a1fae12f
    packaged_gui_sha256: f181b2d314b01eada7dea84f10977c7fc6ec92df81e0f0148732ceb869d63e2f
    gui_uuid: BC174051-C2B8-3FCA-9994-345D3C0D1AB6
    payload_verification: DMG checksum VALID and read-only mount5b153c. Deep strict signature and plist passedc56a43; mounted CLI26.9.1/server control17/pane6/source33b4405eaeb6b9dc46e4b68594734d55cbc1184761b5a952709caba59a036e37 and both helper checks passed5d192b. Signature is adhoc with no TeamIdentifier, Agentty display name and Applications symlink verified05965b. No GUI launch, native clicks, SSH deployment, installation, publication or notarization performed for this package.
    code_comparison: Raw unsigned-copy cmp failed12a8bf at two bytes. Investigationdd3f18/2cbab7/29b325 identified only LC_SEGMENT_64 __LINKEDIT.vmsize at offset3312, native9060352 versus packaged8568832; original signature sizes647968 versus162256 explain segment page rounding. Bounded read-only comparisona03503 asserts all82919576 other bytes equal and matching command/segment/field values. Do not claim raw stripped hashes match; UUID and all non-field bytes establish this package carries the freshly built GUI rather than relying on core-only source identity.
    cleanup: Read-only mount ejected a03503 and absence recheckedcf6332. Two disposable unsigned comparison copies removed; they can be regenerated from the release executable and DMG. Prior GUI DMG backup retained. Canonical packaging removed only current arm64 old stages/package, replacing its final DMG; no installed app or user session changed.
    evidence: Old DMG checksum and backup match48dd31. Five changed GUI source hashes captured before release build; server still reports control17/pane6/source33b4405eaeb6b9dc46e4b68594734d55cbc1184761b5a952709caba59a036e37 (2bbecd), whose scope deliberately excludes GUI files. Native package not yet rebuilt.
    native_permission: Read-only arm64 Swift preflight a91083 again returned accessibility_trusted=false and screen_capture_allowed=false. No permission changes; native click/screenshot acceptance remains open.
    remote_acceptance: Requested a specific SSH Host alias and permission to deploy this server and create/clean isolated test sessions. No target selected or remote deployment performed; existing sessions must not be used as destructive fixtures.
    provider_preflight: Read-only version queriesa8b807 found /opt/homebrew/bin/codex at codex-cli0.154.0 and /Users/admin/.local/bin/claude at Claude Code2.1.245. This is executable availability only; no model request, authentication inspection or real provider/session file mutation. Real vendor acceptance still needs an isolated approved target/state boundary.
  - area: legacy_workspace_entrypoints
    status: source_cleanup_verified_native_acceptance_pending
    current_gate: Frozen-source session18274 exited0 a363bd. Focused remote_connect20/switcher44/liveness7, quick and full passed; GUI1932/one existing ignored, core1355/four existing ignored, CLI138 plus source-identity and all server suites including five stopped_resume tests. Old GUI action/form/delete/name/filter/invalidation closure removed with active search/ownership/liveness regressions retained. Supersedes all pending cleanup source gates below. DMG e51f8d15 and its native lifecycle predate final helper deletion; refreshed packaging and native menu/vendor/SSH acceptance remain separate.
    name_cleanup_gate: Frozen-source quick/full session37178 exited0. GUI1936/one existing ignored, core1355/four existing ignored, CLI138, source-identity and all server suites including five stopped_resume tests passed. Name closure and both exposed unused wrappers/imports are removed. Historical name-cleanup baseline, superseded by current_gate for source verification.
    form_verification: Final machine-filtered54 passed6dadfe. Frozen-source quick/full session76468 exited0; GUI1941/core1355/CLI138, source-identity tests and all server suites including five stopped_resume tests passed with unchanged ignored counts. Six retired form-only tests were replaced by rendered-footer and connection-intent coverage; registered-window switching remains tested. Native interaction and rebuilt package acceptance remain separate. This supersedes pending form gates below, not residual windows.rs cleanup.
    form_retirement: Rendered footer RED3acb88 and static old-form REDd6beb2 preceded deletion. CreateForm/Page/PendingCreate, named create and rename methods, mutation row menus, and deferred create fields/callbacks removed. Six obsolete form-only tests retired; registered-window switching regression retained through switch_workspace. Two deferred-create tests migrated to connection-only semantics, with additional absent/foreign-intent coverage. Initial compile found two stale RowRef.live fixture fields (225d87), corrected without removing assertions; machine53 passed5ee435. Final tests/gates pending.
    residual_cleanup:
      owner: Codex
      final_helper_focused: Frozen-source session18274 passed remote_connect20, switcher44 and liveness7 focused tests plus quick. GUI full portion1932 passed with one existing ignored; core/server gates still running. Bounded old-entry/form/name/filter source audit4c1068 found no matches in the six affected GUI files. No broad unused-code sweep performed.
      final_helper_progress: Static REDa96e8e preceded removal of filter_hosts/host_score/host fixture and four old-dropdown-only tests. TTL test migrated first and passed9621ed; invalidate method removed afterwards. Active current search/WSL/liveness tests retained. Focused and quick/full running serially in session18274 with frozen Rust inputs. Existing DMG e51f8d15 predates this source hygiene slice; its recorded native lifecycle evidence remains artifact-specific, not proof of latest-source packaging.
      final_helper_plan: Remove remote_connect filter_hosts/host_score and their exclusive four dropdown-filter tests/host fixture. Keep WSL discovery and current switcher search tests. Replace the liveness probe test's manual invalidate call with direct time-expiry fixture plus retry assertion, then remove the unused invalidate method. No runtime cache policy changes, no target processes or current search path changes. Static RED, focused remote_connect/switcher/liveness and quick/full required.
      release_only_residuals: Release compiler and call auditc4eb81 expose remote_connect::filter_hosts/host_score and PaneLivenessCache::invalidate as now test-only after form/delete retirement. Audit their feature-only tests and current machine search/liveness coverage before deletion; no Rust edits during frozen build. This is source hygiene followup, not evidence of a user-facing entrypoint or a reason to infer native acceptance.
      name_cleanup_verification: Static RED3e4f12 preceded removal; two target-name table tests first passed381090 before deletion and cover24 local/remote prime/hydration cases without I/O. Chosen-name state/setter/accessor/settlement/GUI rename and pull override removed; target readback and generated internal names retained. Four typed-form-only tests retired; existing target-name/epoch tests retained. Refused-connection test now proves no tree-sync state. Unused ForwardRoute::teardown and BorrowAppContext removed. Final focused session39067 exited0 with tree_sync65, connection1 and forwarding23 passed; format/static passed3839f3. Quick/full pending.
      name_cleanup_plan: Remove chosen_name field/setter/accessor/settlement and GUI rename wrapper; prime/hydration accept target names without a retired form override. Keep internal codename creation and core/CLI rename. Add table-driven prime/hydration name tests for current named/unnamed success, stale success/failure and current failure, then retain existing target-name and epoch regression coverage. Retire four old typed-form-only tests; replace the remote connection test's old chosen-name accessor with a no-tree-sync-state assertion. Remove exclusive ForwardRoute::teardown and unused BorrowAppContext. Static RED precedes implementation; focused and quick/full gates follow.
      delete_final_gate: Frozen-source session47887 completed windows13/switcher44/tree_sync67 focused tests and quick/full with GUI1938/one existing ignored, core1355/four existing ignored, CLI138 and all integration suites including five stopped_resume tests. No Rust edits during gates. This supersedes pending delete-wrapper gates below, not remaining name-chain/native/package work.
      compiler_followup: This deletion exposed unused BorrowAppContext in windows.rs and ForwardRoute::teardown in app.rs (aefa5c/9f3b97). The latter has no remaining GUI caller; remove both with the next source cleanup. Keep core daemon TeardownForwards handling and its workspace forwarding tests, which still have protocol callers. No blanket dead-code cleanup or allowlist change.
      delete_closure_verification: Static REDac3c1a preceded removal of windows delete/kill-list/snapshot/window-replacement helpers, exclusive app forward teardown and unused switcher form constants/render helpers. Two retired GUI-delete-only tests removed. Forget-versus-target-remove keeps all assertions through canonical fire_workspace_op. Source guard passedc648f2; frozen-source windows13, switcher44 and tree_sync67 tests passed in session47887. Quick/full still running; no native or packaging claim.
      remaining_name_audit: Read-only525a9b found tree_sync::name_new_workspace now called only by its old form test; chosen_name still flows through prime/materialization/settle and additional tests. Audit this retired input's complete closure and retain authoritative target-name/epoch behavior before deleting it. Do not conflate provider session titles with internal layout chosen_name. No tree_sync source edits in this slice.
      remaining_name_boundary: Read-onlydf0e07 confirms rename_workspace is called only by name_new_workspace/settle_chosen_name. Remove this obsolete GUI arbitration only after covering finish_prime and settle_hydration target-name adoption plus stale/failed replies. Keep fresh_workspace_name, target protocol WorkspaceRename and actual CLI operations. Existing name tests include typed-name input/readback/existing-target override/hydration override and a no-chosen target-readback regression; migrate applicable target-readback coverage rather than preserving a production field writable only by tests.
      delete_closure_plan: Remove windows delete wrapper and six exclusive helpers plus app.teardown_workspace_forwards, and unused switcher form geometry/render helpers. Retire a_delete_reads_its_kill_list_before_the_removal_blanks_the_mirror and a_deleted_remote_workspace_leaves_the_switcher because their GUI action no longer exists. Preserve every assertion in forgetting_a_workspace_never_tells_its_machine by exercising canonical fire_workspace_op for its remove contrast. Keep tree_sync target-deletion/failed-rebuild/ownership tests intact. First strengthen source guard and reproduce RED; then focused windows/switcher/tree_sync tests and quick/full.
      current_slice: Remove unreachable confirmation/query/standalone stop entrypoints and their eight exclusive locale keys. Retire obsolete confirmation-copy test; keep Japanese plural coverage on an active key. Separate delete wrapper and its layout tests remain under audit, not silently removed.
      confirmation_final_gate: Frozen-source session44405 exited0 f366ef. Eight locale tests passed489ab4, quick passed6abefe and full passed with GUI1940/one existing ignored, core1355/four existing ignored, CLI138, six source-identity tests and all server integration suites including five stopped_resume tests. This supersedes pending confirmation gates below, not remaining delete-wrapper cleanup or native/package acceptance. No real configuration or session mutation performed.
      next_closure_audit: Read-only call audit100b1f/e55da8/a95c39 found windows::delete_workspace only called by switcher test a_deleted_remote_workspace_leaves_the_switcher. Exclusive closure includes doomed_pane_ids, stop_workspace_keeping, scrub_host_snapshots, delete_from_tree, close_window_for and app.teardown_workspace_forwards. Before deleting, separate retired GUI delete fixtures from active tree_sync::deleted/forgetting/failed-rebuild ownership tests. fire_workspace_op and canonical target/core/CLI operations remain outside this deletion scope. Also inspect obsolete switcher form constants/helpers reported by compiler; no source change during frozen full gate.
      confirmation_verification: Static RED033d1b preceded removal of PaneCountQuery, two query functions, confirmation entrypoints/detail/dispatcher and standalone stop_workspace. Eight exclusive keys removed from all locales and plural inventory; Japanese variant coverage now uses active HomeTimeMinutesAgo. Obsolete confirmation-copy test retired with feature, not ignored. Source guard and formatting passedbeba06; all14 windows tests passed470bb2, retaining ownership and forgetting regressions. Removed newly unused t import afterwards; frozen-source locale tests and quick/full running in session44405. Remaining delete wrapper closure is still unfinished.
      status: source_cleanup_verified
      removal_condition: Retire newly unreachable windows.rs confirm_and_stop/confirm_and_delete and their exclusive confirmation helpers after preserving target/session ownership regression coverage; audit remaining test-only delete_workspace wrapper separately. Do not remove canonical core/CLI layout operations or user data.
      verification: Current source guard, bounded call audit4c1068 and frozen-source full18274 passed. Identified old GUI helpers and exclusive tests are retired; reusable ownership, TTL, target-name and active search assertions retained. Earlier pending audit/plans above are historical evidence. Native user-entry acceptance remains open at the parent contract level.
    form_retirement_plan: Reproduce the rendered new-workspace footer before deleting its form, rename and row-mutation entrypoints. Remove form-only pending-create callbacks, retain ordinary machine connection/reconnection and target internal layout ownership. Replace feature-only form tests with footer and connection-state preservation coverage; retain registered-window switching regression through its canonical internal operation. No real SSH/session mutation.
    action_layer: Keymap behavioral REDcafdbb and native-menu source RED5c6055 preceded removal of four action declarations, palette variants/commands, keymap defaults/builders, native-menu entries and window handlers. Empty Window menu now offers existing NewTab. Removed dead inline workspace rename state and seven unused localization keys across all locales. Switcher form/row-menu and pending-create chain are still active and must be retired separately; no complete migration claim.
    action_verification: Initial machine-filtered suite passed50 tests8ce7ad; final51 passed043145 including canonical native menu construction. Frozen-source quick/full session80329 exited0, GUI1945/core1355/CLI138 plus source-identity and all server suites including five stopped_resume tests passed, existing ignored counts unchanged. Both earlier intentional RED tests are now green. Public action source guard passes; switcher form/row-menu guard remains pending. No real user config or process touched; existing DMG predates these GUI changes.
    owner: Codex
    contract: MACHINE-ENTRYPOINT-25
    evidence: Native menu actions in theme.rs, palette base_commands, default/custom keymap and app action handlers all expose named workspace mutations (412658/0cbb0d/cb379f). switcher footer reaches Page::Create; form Enter creates a workspace and applies a user name (6406c5). This contradicts full machine-only GUI completion, not just wording.
    plan: Add a real palette projection regression, then remove the four public action families and their dedicated create/rename form and row-menu callers together. Retain machine connection, canonical internal carrier creation, persisted data and CLI APIs. Add menu/keymap/rendered-entry coverage before implementation; never replace stop/delete with Disconnect under an old label.
    verification: Historical pre-implementation RED retained as evidence. Missing-test guard failed633c10; real GPUI base_commands failed73cf34 with exactly the four obsolete command IDs while NewTab remained. Keymap later failedcafdbb before action removal. Current action-layer verification above supersedes this failing baseline, but does not establish complete switcher retirement or native desktop acceptance.
    callback_audit: switcher_form_create is the producer of PendingCreate; remote_workspace finish_connect consumes it and calls name_fresh_workspace. A protocol refusal can retain parked_create for later reconnect (3765c6). Retirement must remove or migrate this entire dedicated callback chain and its obsolete feature-only tests while retaining ordinary machine connect/cancel and target internal carrier creation. Merely hiding form/menu controls is insufficient.
    removal_condition: No user-facing or dispatchable named-workspace management path remains; machine/session flows, ownership and data preservation pass focused/full and native acceptance.
  - area: startup_recovery_user_guides
    status: synchronized_selected_guides
    owner: Codex
    contracts: [MACHINE-RAIL-01, SESSION-EXIT-RETENTION-13, SESSION-OUTPUT-23, CLIENT-HOOK-03]
    plan: Correct first-launch and troubleshooting advice about GUI quit, target server termination, stopped output preview and provider history. Retain currently wired workspace action names in the shortcut reference with explicit internal-layout and destructive-action scope, rather than documenting nonexistent renamed commands.
    evidence: Config roots verified in core/config.rs; stopped output contract and terminal replay semantics distinguish nonconsuming View output from older restoration. app.rs still wires NewWorkspace, RenameWorkspace, StopWorkspace and DeleteWorkspace (a6b72a); this is a remaining GUI entry audit, not evidence that every old workspace action is retired.
    verification: First-launch, troubleshooting and keyboard-shortcuts passed frontmatter, navigation membership, Markdown/component page-link and component-balance checks; tracker YAML and harness passed (b23a2c). No MDX compilation/rendering, runtime mutation or real session test. No Rust source or artifact changed.
    next_action: Audit active legacy workspace commands and window handlers against machine-only product semantics before further completion claims. app.rs lines5357/5360/5361/5440 and window handlers still dispatch NewWorkspace, StopWorkspace, DeleteWorkspace and RenameWorkspace; declaration/translation presence alone is not proof of reachable obsolete UI. Preserve legitimate target layout ownership and destructive-action distinctions while identifying actual user-visible paths.
  - area: machine_session_user_guides
    status: synchronized_selected_guides
    quick_gate: Session35671 exited0 with presubmit quick passed (9af482); dedicated tracker YAML/evidence check passed74a18a. Documentation-only changes, no new Rust full gate or package rebuild required.
    owner: Codex
    contracts: [MACHINE-RAIL-01, MACHINE-SCAN-04, SESSION-RECOVERY-03, TERMINAL-BOTTOM-01]
    finding: User guides still describe repository/Scratch headers, directory-driven row ownership and automatic recovery as unconditional. Current machine rail, history controls and jump-to-bottom behavior are absent.
    plan: Synchronize existing concepts, sidebar, remote workspaces, agent sessions and scrolling pages against canonical specs and current controls. Keep internal WorkspaceId distinct from the user-facing machine, and process attachment distinct from provider Resume. Preserve CLI tty7 names and defer unverified provider/native claims. No runtime behavior changes.
    verification: Five selected guides passed YAML frontmatter, docs.json navigation membership, local page-link existence and MDX component balance checks (2a997d). Targeted old directory-grouping, flat-switcher and unconditional-resume wording is absent; harness passed. Source controls inspected in switcher.rs, tab_strip.rs, tab_sidebar.rs, history_manager.rs and terminal/view.rs, with corresponding machine/scroll specs. These checks do not compile or render MDX and do not constitute native UI acceptance.
    remaining: Other site pages, including first-launch, troubleshooting, shortcuts and site-wide branding still need separate source-backed synchronization. No docs site build or screenshot acceptance performed. Rust sources and packaged artifact identity unchanged.
  - area: guarded_native_arm64_package
    status: historical_isolated_lifecycle_verified
    superseded_by: machine_only_gui_package
    artifact_scope: Lifecycle evidence below belongs to the prior GUI package now retained at /tmp/agentty-machine-ui-package-5mKyah/prior-gui.dmg. It is not native acceptance of the newly packaged GUI at the canonical dist path.
    owner: Codex
    contracts: [CLIENT-HOOK-03, PRODUCT-BRAND-01, BUNDLED-HELPERS-06]
    artifact: dist/agentty-26.9.1-macos-arm64.dmg
    sha256: 4f6e88c13f3dc7d4a0b08a42f69ad1970e2620f117c50ef205d5ea63b72fb8fb
    source_identity: 26.9.1+source.33b4405eaeb6b9dc46e4b68594734d55cbc1184761b5a952709caba59a036e37
    build: Sequential locked offline Linux helper and native release builds completed 9a8c4a/569a16/9bd8e1. Canonical macOS packaging completed0512ea; checksum and read-only mount passed700f3c, strict signature/plist checks passed2aea4c. Current artifact hash independently rechecked f1eae1; mounted server identity and strict signature rechecked6e7947. Ad-hoc signature only, no publication, notarization, installation or replacement of the user's app.
    fixture: /tmp/agentty-hook-scope-smoke-M8PcFs
    lifecycle: Real GUI73420/window39244 launched at1440x900 (b5b1a2). Exact PID/path-guarded normal termination succeeded c88469 and GUI exited0 (2d4997). Daemon65253/shell65257 survived with workspace0992e87e-1c99-43c9-ae0a-fc4702c63d40, tab48245ea0-5de2-4147-bc36-9b0d49abefe3 and live pane1 unchanged (a60b78). Reopened GUI76586/window39260 retained the same geometry and all identities (e63e2b).
    hook_fixture: Redirected CLAUDE_CONFIG_DIR contains a stale agent-hook command plus an unrelated user field. GUI receives --config-dir with inherited TTY7_CONFIG_DIR unset. SHA256 dc356b2d40dc90e82bb48f96c91f6ec4048fd85c1cc3b682d43722d57637ac4c remained unchanged before launch, after normal quit and after reopen (36a794/a60b78/e63e2b). This proves stale fixture byte preservation under isolated release startup, not every provider's native behavior.
    cleanup: Second normal quit succeeded02ed89 with GUI exit0 (5b70b0); exact config-scoped server stop returned stopped=true (e3f889). No test GUI/daemon/shell remained; installed GUI24715/daemon24719 stayed intact (6e7947). Test mount ejected5558d3. Fixture files and prior-unguarded.dmg backup retained. Both GUI exits logged an IMK mach-port warning with unconfirmed cause.
    verification: Quick session28912 passed1cb1f9. Tracker plain-scalar colon syntax repaired without changing its text; dedicated YAML.safe_load passed844680. Rust sources unchanged since full18276.
    remaining: Accessibility and screen-capture preflight both false (b5b1a2); no screenshot, terminal-grid or native mouse acceptance claimed. Real provider Resume/Rejoin, chosen SSH target, Linux execution and broader lifecycle acceptance remain open. Ordinary-shell lifecycle evidence cannot establish those results.
  - area: isolated_launch_hook_refresh
    status: implemented
    full_gate: Frozen-source quick/full session 18276 exited 0. GUI 1942/core 1355/CLI 138 passed with existing ignored counts unchanged, plus six source-identity tests and all server suites including five stopped_resume tests. New native source identity is 26.9.1+source.33b4405eaeb6b9dc46e4b68594734d55cbc1184761b5a952709caba59a036e37 (d37d5a). This supersedes pending source gates, not native artifact acceptance.
    next_action: Guarded helpers and native DMG rebuilt; isolated stale-hook fixture and normal GUI quit/reopen verified under guarded_native_arm64_package. Continue separate real-provider/SSH and native interaction acceptance; never use the prior unguarded DMG as guard evidence.
    focused_verification: Static missing-guard RED e31b50 and missing-function RED 427499 preceded the canonical scope callback guard. All 29 hook tests passed 789259, including eight scope/build-mode combinations, no callback for different/unknown ownership, and unchanged explicit local/remote hook management. No real provider file writes performed. Quick/full and rebuilt artifacts remain pending.
    build_space: Free disk was 1.3 GiB with 8.1 GiB under debug tty7-core build variants (9cc46a). Package-scoped cargo clean -p tty7-core --profile dev removed 2906 regenerable files/13.0 GiB (5f97ed), leaving 12 GiB (1c87a8). Native release files, DMG, helpers, user config and installed app remained intact. Other observed Cargo/rustc processes belonged to /Users/admin/ashide, not this target directory; they were not interrupted or cleaned.
    owner: Codex
    contract: CLIENT-HOOK-03
    finding: Release startup unconditionally calls refresh_hooks_at_launch; its only guard is debug_assertions, while HookTarget::local uses real user home rather than --config-dir (90e752/d870f6). No invocation-scope gate exists before target provider I/O.
    impact_check: After stopping both test GUIs and their isolated daemon/shell, bounded exact-path checks of the 13 supported provider hook targets found no temporary application path. The five existing files have mtimes predating this test; the other eight do not exist (ed483c). No hook content was printed or modified. No evidence of real hook changes, but do not repeat native release launch until the missing guard is fixed.
    plan: Gate the existing startup refresh with exact known config-versus-machine scope and build mode before creating Host. Inject only the refresh callback for no-I/O tests. Preserve normal default release behavior and explicit/remote management. Rebuild source-stamped helpers and DMG after verification; do not silently claim the old artifact contains the fix.
  - area: historical_unguarded_native_arm64_package
    superseded_by: guarded_native_arm64_package
    normal_quit_reference: https://developer.apple.com/documentation/appkit/nsrunningapplication/terminate()
    native_lifecycle_verification: Exact DMG hash 4c2714; read-only mount d16a28; real package GUI PID90350 and 1440x900 window39139 launched (4ee12a), daemon PID90353, shell PID90363. Scoped CLI confirmed workspace b4384c5b-86b4-4ae0-b0b9-3ccd582566cf, tab 9e2e767b-ad5c-4d01-bb61-28575a8057e7, live pane1. Exact PID/path-checked NSRunningApplication normal termination sent and observed true (29da80), GUI exited0 a3f842; daemon/shell survived unchanged (4d7be3). Reopened GUI PID91518/window39146 at same geometry, same workspace/tab/pane and shell PID (8e80e2/91b72a). Normal second quit passed84ecec, scoped CLI server stop passed5d561b and no test processes remained; mount ejected fae55d. Real installed GUI/daemon PID24715/24719 remained intact. Native ordinary-shell lifecycle only; no screenshot, grid, mouse, agent or SSH acceptance claimed. One IMK warning on second exit was observed, cause unconfirmed.
    daemon_entry_observation: Runtime and spawn_detached source agree that GUI startup relaunches tty7-app --daemon; CLI separately resolves tty7-server. Both run tty7-core daemon code. Bundle comment claiming GUI never uses this path was inaccurate; correct documentation rather than silently treating a comment as authorization for a new daemon architecture. No runtime migration is part of this observation.
    native_launch_plan: Verify the exact DMG hash, mount read-only and launch its GUI with a fresh /tmp config via --config-dir and inherited TTY7_CONFIG_DIR unset. Disable updates, CLI installation, tray and automatic agent restore; use zsh -f with isolated ZDOTDIR. Capture exact GUI/daemon/pane identities, send NSRunningApplication normal termination only to the verified test PID/path, observe process exit and daemon/pane survival, then reopen the same fixture. No real application replacement, screenshots/click acceptance or provider/SSH behavior is inferred.
    status: superseded
    owner: Codex
    contracts: [PRODUCT-BRAND-01, PACKAGE-CLEANUP-02, MACOS-METADATA-05, BUNDLED-HELPERS-06]
    artifact: dist/agentty-26.9.1-macos-arm64.dmg
    sha256: 6ababcb9557b8760abcfe9cc6237202e8246eecf665b8b8056647e85048e1997
    source_identity: 26.9.1+source.9acd4e260ff40248fb6f725e3edf57283e2dd47db6b28ef1de53563af8a765c7
    build: Offline locked native aarch64-apple-darwin release build of tty7/tty7-server/tty7-cli with pinned nightly-2026-07-31 completed in 3m09s (9f365c). GUI is Mach-O arm64; all three executables reference only system libraries/frameworks (31c0cf/b11d38). Unqualified otool hit Rosetta libxcrun mismatch (3cf852); arm64 invocation succeeded without tool installation or environment repair.
    cleanup: Initial build was intentionally stopped with SIGINT after free disk fell to 311 MiB (d8ad48/7254fb), terminal exit 130 aeee4b. Process check ca9df1 proved Cargo/rustc gone and real app/daemon remained under /Applications/tty7.app. Package-scoped cargo clean -p tty7 --profile dev removed 182 regenerable files/3.1 GiB (8c19e5), preserving release dependencies, source, helpers and real sessions. Same release command then completed; this was not an observation-timeout restart.
    packaging: Existing bundle-macos.sh aarch64-apple-darwin arm64 completed 80f93f with Apple credential environment variables explicitly unset. Canonical helper gate passed e16cde. Produced approximately 40 MiB DMG, ad-hoc signed only, no Developer ID, notarization, publication or installation. Canonical script removed its intermediate app/stage.
    verification: DMG checksum VALID 426bb3; mounted read-only/nobrowse at /tmp/agentty-dmg-verify-jjEpv6 (29cb59). Deep strict codesign verification and plist lint passed d6dadb. Package has Agentty display name, tty7-app executable, version 26.9.1, exact source identity and Applications symlink (844ab4). Both embedded helper SHA256 values match the verified v17 inputs. Mounted server reports control17/pane6 and CLI reports tty7 26.9.1 (ca5609). Signature explicitly adhoc, no TeamIdentifier; mount ejected successfully cb9650.
    next_action: Historical package only; do not launch this unguarded artifact for new acceptance. Its completed ordinary-shell lifecycle evidence is retained above. Use guarded_native_arm64_package for current delivery state.
  - area: development_launch_lineage
    status: verified
    owner: Codex
    contract: PRODUCT-BRAND-01
    finding: Packaging preflight c33e6c found cargo app selects nonexistent agentty-server and agentty-app although manifests retain tty7. Existing CI lineage checks omit the local launcher.
    plan: Extend canonical lineage validation to the existing app alias; use isolated fake-cargo invocation tests for order, argument forwarding and build-failure short circuit. Then fix only the stale selectors. No real GUI/config/daemon launch during tests.
    verification: Canonical source gate RED e64f70 and isolated shell execution RED 2fbfef preceded correcting both selectors. Five lineage tests/16 assertions passed b5763f; quick passed 5db49c. Covers exact argv including spaces, build-failure short circuit, wrong selectors, missing lock flags, unconditional launch and missing config. No Rust source change or real GUI launch; this does not prove installation.
    packaging_preflight: No macOS release target directory or dist artifacts exist. Current GUI development binary is absent, disk has 3.7 GiB free, and real app/daemon run from /Applications/tty7.app (b7a37b/d16359/c33e6c). Building or packaging remains separate work; do not claim tests are an installable product.
    packaging_next: Attempt one offline locked native arm64 release build of tty7, tty7-server and tty7-cli using the existing pinned toolchain and manifests. Keep source frozen; monitor disk and build handle. Do not install tools, change profiles, fabricate release inputs, use signing credentials or replace the real app. Packaging still requires canonical helper validation and local artifact verification.
  - area: whole_tab_sleep
    stale_reply_verification: Static missing-coverage RED 5e8667 preceded three paused-success tests. All three passed d34da8. Frozen GUI package suite and quick session 30749 exited 0 (35e3ac), with 1942 passed/0 failed/1 existing ignored. Actual GPUI clicks route through acquisition and SleepTab; exact same-instance connection, same-pane replacement entity and epoch invalidation independently reject the late success. Client ownership returning to fixture/cache/retained-use baseline proves handler completion before assertions; one tracked release follows with no retry or synthesized exit. Test/docs/static-only change, no production path or real target mutation. Native window closure and real SSH remain unverified.
    stale_reply_plan: Add three paused-success cases sharing the existing scripted peer and split fixture. Use client ownership return as a task-completion barrier, not an arbitrary settling sleep. Independently replace connection/entity/epoch; retain exact target identity and assert no old success or exit, no repeat request and one final workspace release. No production path change planned.
    cold_neighbor_verification: Missing-coverage static RED bca9a9 preceded canonical cold stopped hydration in the existing Sleep fixture. Focused cold case passed a5d2d6; all 11 sleep GUI tests passed d90018 and quick passed 482b13. Actual GPUI click submits both identities once, reply retains already-stopped pane 72, both GUI entity IDs survive reply and read-only refresh, and tracked release remains exact. Test/docs/static-only change; no new runtime path and no native/SSH acceptance claimed.
    native_permission_recheck: Read-only arm64 Swift AXIsProcessTrusted/CGPreflightScreenCaptureAccess preflight a720de still returned false/false. No permission request, real app input, configuration change or daemon restart was performed.
    gui_full_gate: Frozen-source quick/full session 37796 completed with exit 0 (9aea55), including all server suites and five stopped_resume tests. Supersedes the pending GUI quick/full note below, not native or real SSH acceptance.
    cold_neighbor_plan: Extend the existing mouse-to-control Sleep fixture using canonical session_to_pane stopped hydration. Assert full ordered request, already-stopped classification, exact entity retention and read-only refresh; no new production path. Paused successful replies remain a subsequent acceptance slice.
    gui_status: implemented
    gui_verification: Rendered missing-button RED 33edfa preceded shared coordinator/UI changes. Paused acquisition/runtime-change RED b098e0 exposed a stuck busy state; scope-versus-submission eligibility repair followed. All 10 sleep-focused GUI tests passed 41dcb2, including three actual GPUI click-to-scripted-control batch cases (accepted/partial/unknown), exactly one SleepTab, mutual exclusion, read-only refresh, tracked release, ordered reply validation and entity/epoch/binding guards. Private core fixture method access was corrected without widening production API. Quick/full and native acceptance pending.
    gui_build_cleanup: Disk reached 815 MiB free (f76961). Read-only process check cae607 found the real app/daemon under /Applications/tty7.app, not the repository build tree. Package-scoped cargo clean -p tty7 --profile dev removed 407 regenerable GUI build files/6.2 GiB (697da9), leaving 5.7 GiB (6ef3f8). Real app, daemon, user config, sessions and Linux helpers were untouched.
    gui_plan: Reuse the single Sleep coordinator, tracked acquisition and notification styles. Capture all pane targets/entities, serialize single versus whole-tab actions, validate partial replies and refresh without retry. Unsupported mixed tabs remain visibly disabled pending shell policy.
    status: implemented
    target_status: verified
    owner: Codex
    contract: SESSION-TAB-SLEEP-24
    decision: Target preflights exact complete tab membership and every recoverable runtime before stopping. Return partial execution facts without retry/rollback. Shared single-session validation remains canonical.
    pending_decision: Asked whether mixed ordinary shells should also terminate, with loss of variables/jobs/unsubmitted input disclosed. Do not silently kill or skip them while awaiting the answer.
    verification: Corrected external-tag wire fixture RED 9e9f5c preceded the new operation. Two core tests passed abbf93, including thirteen full-preflight/partial/cancellation scenarios and strict wire fields. Real isolated two-PTY Sleep/Resume passed 23a16d with exact split/binding retention, final output, whole-preflight refusal, repeated stopped readback and explicit per-slot Resume. Initial server test referenced an unavailable uuid dependency (70d494); replaced with a fixed foreign fixture ID without adding dependencies or relaxing assertions. Quick/full pending.
    artifact_impact: Control dialect advanced from 16 to 17. Both bundled Linux helpers were rebuilt offline/locked and matched the current native server source identity (90fb95); see bundled_helper_gate.real_artifacts. Debug GUI/native desktop packaging and actual Linux execution remain unverified. No real remote helper was deployed or user daemon restarted.
    full_gate: All four sleep-focused tests passed a52b5c; frozen-source quick/full session 63110 exited 0 (cc565c). GUI 1931/core 1354/CLI 138 and all server suites passed, including five stopped_resume tests. Existing ignored counts unchanged. Supersedes pending target gates, not whole-tab GUI/native/remote acceptance.
    next_action: Continue native/real-remote lifecycle acceptance, installed-product packaging and ordinary-shell policy. Paused successful reply guards, shared GUI full gate and cold stopped neighbor coverage are verified above; a synthetic epoch change is not native window closure. Matching v17 helper static evidence does not substitute for installed-product execution.
  - area: direct_machine_session_list
    status: verified
    owner: Codex
    contract: MACHINE-RAIL-01
    decision: Remove Section and group-name rendering algorithms; retain machine fold/search, font sizes, canonical numeric activation and filtered move semantics. Bind drag surfaces to complete TabId order and visible indices.
    verification: Static RED 173b67 and invalid-membership behavioral RED aabdc8 preceded replacement. Final-release GPUI RED e9b83d exposed stale pending index application; shared search projection plus pre-consumption identity validation repairs it. Sidebar 9 tests passed bb46ae, reorder 12 passed a70dff, formatting and harness 8dcef3 passed. Eleven obsolete section/group-only tests retired, four sidebar invariants and one reorder identity test added; original machine folding and generic drag tests retained. Unused Scratch group translation removed from all locales. Quick/full and native acceptance not yet claimed.
    full_gate: Frozen-source quick and full session 36752 exited 0. All workspace unit/integration suites passed, including target Sleep/Resume. Existing ignored-test policy unchanged. This supersedes the pending quick/full statement above; native interaction, real SSH and packaging acceptance remain open.
    next_action: Continue whole-tab multi-pane Sleep and native/remote lifecycle acceptance. Direct-list rendering debt is resolved; legacy wire/storage data compatibility remains intentionally separate.
  - area: obsolete_group_mutation
    status: verified
    owner: Codex
    contract: MACHINE-RAIL-01
    decision: Delete custom-group menu/create/rename/regroup and new-tab/fork/detach inference. Preserve existing legacy data and ordinary pane/tab identity behavior.
    verification: Static RED 7f3311 and real GPUI detach RED 51c934 preceded deletion. Detach passed 1d2709; final sidebar 16 and reorder 11 tests passed. Quick and frozen-source full session 64394 exited 0 (GUI 1937/core 1352/CLI 138 plus all integration suites). Fourteen retired feature-only tests were removed and one pane identity/sole-pane refusal regression added; existing ignored counts unchanged. Five group-action translation keys, orphaned rename/landing state, all three spawn inheritance sites and unused test cwd injector removed. No native interaction acceptance claimed for this revision.
    next_action: Render scaffolding retirement is now verified under direct_machine_session_list; preserve legacy data compatibility and continue remaining lifecycle/native acceptance.
    remaining_render_debt:
      owner: Codex
      status: resolved
      resolution: Section/name helpers and synthetic group keys removed. Sidebar 9/reorder 12 tests and quick/full session 36752 passed, including filtered permutations, real rendered bounds, numeric activation and stale final-release refusal.
      removal_condition: Replace sidebar_sections and group-shaped row surfaces after proving filtered row order, keyboard activation and hidden-row drop safety on the direct list.
      verification: Retain machine fold/search rendered-bound tests and replace old section-algorithm tests with direct-list permutation/visibility invariants; run focused tests and full gate.
  - area: obsolete_directory_settings
    status: verified
    owner: Codex
    contract: MACHINE-RAIL-01
    finding: Machine rail ignores directory headers but Window/Tabs settings still render and mutate the old repository/directory grouping preference.
    decision: Remove the obsolete selector, search entry, setter and translations; retain legacy data decoding. Custom-group menu/drag consumers remain a separate incomplete cleanup, not hidden completion.
    verification: Static RED e77691 and behavioral RED c25e28 preceded deletion. Existing positive search assertions were migrated from the retired setting to retained tab settings, with the new negative assertion intact. All 46 settings tests passed 0aa85c; quick passed 7a1b7d; frozen-source full session 8225 exited 0 with GUI 1950/core 1352/CLI 138 plus all integration suites. Existing ignored counts unchanged. No native interaction acceptance claimed for this new source revision.
    next_action: Remove the still-active custom-group tab menu and its write/drag/rename consumers, then retire test-only legacy computation. Do not treat this settings-only deletion as completion of the full machine model.
  - area: workspace_version_gate
    status: verified
    owner: Codex
    contract: QUALITY-VERSION-04
    decision: Restore the missing CI entry with pinned offline Cargo metadata and shared SemVer validation. Check workspace version equality without a fixed literal or version bump.
    verification: Missing executable RED 34befd preceded implementation. Canonical temporary-path fixture correction preserved foreign-root rejection; focused tests passed 65 assertions (9add05), quick passed f3e259, and real offline check with an empty temporary CARGO_HOME passed 0d978f. No manifest, lockfile, Rust source or user configuration changed. Full Rust suite was not rerun for this gate-only change; native and packaging acceptance remain open.
    next_action: Keep CI and local quick on this single executable; continue actual helper inspection, native UI and remote lifecycle acceptance separately.
  - area: bundled_helper_gate
    v17_gate: Helper fixtures passed 4 tests/47 assertions and quick passed with exit 0 (61c941). Rust source and dependency manifests were unchanged in this artifact-only refresh; the frozen-source full gate 63110 remains the source verification baseline. Native GUI, Linux runtime and real remote acceptance remain open.
    real_static_verification: After control v17, the canonical checker rejected the stale v16 x86 helper marker (06d0b2). Sequential offline locked release builds passed x86_64 25bea1 (62s) and aarch64 f2bbd5 (66s). Both new helpers passed the canonical checker in a fresh scratch stage (e1534b), then were copied to their exact bundled-server paths and passed final verification with matching SHA256 (90fb95). GNU readelf was exposed only through command-scoped PATH. Native protocol is control 17/pane 6 with the source identity below. No foreign Linux binary was executed, no remote deployment/user daemon restart occurred, and no tool or dependency was installed.
    historical_v16_static_verification: Both old v16 files passed canonical and independent static checks with the existing keg-only GNU readelf 2.47.20260726 (98a646). Superseded for current packaging by the v17 builds; their previous hashes are retained below.
    static_replay: 'PATH="/opt/homebrew/opt/binutils/bin:$PATH" bash script/check_bundled_remote_helpers target/debug/tty7-server bundled-server'
    real_cross_build_result: Both v17 helpers built with installed Zig 0.16.0 and nightly-2026-07-31 using --release --locked --offline -p tty7-server. file confirms static stripped ELF64 x86-64 and ARM aarch64 (6570c0), and canonical verification passes (90fb95). Temporary validated copies remain in /tmp/agentty-helpers-v17-K0RUT6. This is cross-build/static evidence only, not Linux execution or native packaging acceptance.
    real_artifacts:
      control: 17
      protocol: 6
      source_identity: 26.9.1+source.33b4405eaeb6b9dc46e4b68594734d55cbc1184761b5a952709caba59a036e37
      verification: Stale prior helper rejected23d235; rebuilt scratch helpers passed32c39a and canonical packaging0512ea. Both current hashes independently rechecked6e7947. Static/cross-build evidence only; no Linux runtime or remote deployment claim.
      x86_64:
        path: bundled-server/tty7-server-linux-x86_64-musl
        sha256: 5966ca8773e3bde345a7397df55c5830ba58cbbd92b66b0712011f3bcceaa4e8
      aarch64:
        path: bundled-server/tty7-server-linux-aarch64-musl
        sha256: 94304d5fa1debcbb0d8635a769088e2bac010d683b9c4e8a4d64ea284d5c96aa
    previous_unguarded_v17_artifacts:
      source_identity: 26.9.1+source.9acd4e260ff40248fb6f725e3edf57283e2dd47db6b28ef1de53563af8a765c7
      x86_64:
        path: bundled-server/tty7-server-linux-x86_64-musl
        sha256: 26d98ee605dd685e2080d23d58c86c672520e2e24454da8bb0e751d0f80eaa6f
      aarch64:
        path: bundled-server/tty7-server-linux-aarch64-musl
        sha256: b9a7d0829e6c79eed26f0fa9491e028fa626880b60fc18bfcfe784366c03097d
    previous_v16_artifacts:
      source_identity: 26.9.1+source.06db2f2e2bef82c88e8dad4bcc79a6c12bb04670bc1e51c76dec80b00cac7eff
      x86_64:
        path: bundled-server/tty7-server-linux-x86_64-musl
        sha256: c2b2e3f7f60b3e0a454dbdf9e8411afb392946231baaa06c629a06bcf0168a04
      aarch64:
        path: bundled-server/tty7-server-linux-aarch64-musl
        sha256: 01dc6a85f2744bb9c2d2c1a89268de1c9591c1fa35c6d3ca55cf0e33a85b35f9
    cross_build_preparation: Package-scoped cargo clean removed 474 regenerable tty7 dev files/6.5 GiB (b73eec), leaving 8.8 GiB (87d698); no running app/server/build processes or user data were touched. Offline build failed before compilation because locked curve25519-dalek-derive v0.1.1 was not cached (97bc8e). Continue the normal authorized build with --locked and network dependency fetching; this revises the offline-only attempt, not toolchain/configuration or dependency-version policy.
    cross_build_plan: Read-only preflight e2a018/978b20 found Zig 0.16.0, cargo-zigbuild and installed x86_64/aarch64 Linux musl targets, but no helper build directories or readelf and only about 3.2 GiB free. No tty7-app/server/Cargo/rustc processes found (414afa). Free only regenerable tty7 GUI dev package artifacts, then attempt one offline locked x86_64 helper release build using the installed toolchain. Do not install tools, fetch dependencies, sign, publish or use real remote sessions; successful cross-build alone is not Linux runtime acceptance.
    full_gate: Quick 497be7 and frozen-source full a2877f/d078a7 passed, including GUI 1949/core 1352 and all integration suites. Helper fixture 47 assertions and existing metadata 117 assertions remain green. This completes the missing checker and shared parsing path, not actual Linux ELF execution, signed artifacts or installed-product acceptance. No tools were installed, helpers downloaded or real remote state changed.
    status: in_progress
    owner: Codex
    contract: BUNDLED-HELPERS-06
    decision: Implement the missing canonical macOS bundle checker using shared native protocol parsing, existing static ELF gate, binary architecture header and embedded source-marker equality. Do not execute foreign binaries or claim runtime/cryptographic attestation from marker presence. Actual Linux artifacts, inspectors and runtime acceptance remain separate.
    verification: Missing checker RED 12f78e preceded implementation. Shared parsing exposed an incomplete metadata staging fixture (34f9fc), corrected without relaxing assertions; all 117 metadata assertions passed 97bbc5. Existing identity source guard rejected moved parsing (594cdd/23bb52); guard now covers both consumers and shared format validation, with three scratch-source bypass mutations. Four helper tests/47 assertions passed 249bd1 and quick passed 497be7. Real bundled-server directory and readelf are absent (21b54f); no actual Linux artifact/runtime acceptance claimed.
    next_action: Execute the statically verified helpers in isolated Linux environments of both supported architectures and validate protocol/session behavior. Complete native desktop packaging and UI acceptance separately. Static linkage and source markers do not prove Linux runtime behavior; no signing, publication or real remote deployment has been performed.
  - area: development_navigation
    status: verified
    owner: Codex
    contract: QUALITY-NAVIGATION-03
    decision: Rebuild missing root DEVELOPMENT.md from current specs and code, not archived 0.0.1/version and selected-environment-only vocabulary. Add an engineering docs index without duplicating milestone state. Extend the canonical harness and existing fixture suite to reject missing/empty/foreign entries and broken local links; no parallel roadmap or feature-memory skill.
    verification: Missing-entry and broken-link RED a55ee4 preceded harness implementation and current documentation. Full quality fixture suite passed 9a1945 with 9 tests/117 assertions, including missing/empty/foreign/directory entries and broken/foreign links. Actual repository harness passed 0744fd; quick passed b031be. No Rust runtime change, full Rust rerun or new native GUI acceptance is claimed.
    next_action: Maintain these navigation links through the existing harness; keep dynamic progress exclusively in tracker. Real artifacts and native UI remain separate delivery work. Runtime code, version values, user configuration and release policy were not changed.
  - area: agent_exited_live_shell_recovery
    status: awaiting_product_decision
    owner: Codex
    contract: SESSION-SLOT-17
    evidence: Inspected canonical stopped transaction, MachineStore.with_live_session, DaemonPane.sleep_session/write_input, PaneState prompt observations and foreground probes (45944d/c3dc0e/0c6b43). Stopped Resume explicitly refuses live predecessors; Sleep can stop a live carrier but is not proof of an idle shell. PaneState does not prove an empty editable input line; unknown foreground probes currently return false in the general display helper. No implementation or runtime verification claimed.
    decision: Clarify whether explicit Resume may replace the still-live idle shell and lose shell-local variables/input. Preserve the current no-injection and no-live-replacement boundaries until resolved. Reuse existing target proof/source validation/slot transaction and GUI recovery coordinator after defining the selected behavior; do not copy Ashide's relation framework.
    next_action: Obtain shell preservation versus replacement decision, then define the typed target operation and runtime/input concurrency tests before implementation. Other independently authorized product work can continue; this is not a whole-project blocker.
  - area: stopped_output_preview
    replacement_full_gate: Quick d12f84 and frozen-source full 83547c/c6c18e passed, GUI 1949/core 1352/CLI 138 plus source-identity and all server suites. Existing ignored counts unchanged. Six focused output tests cover capability refusal, missing link, actual same-slot replay, paused epoch retirement and independent same-instance connection/same-identity entity replacement. Supersedes pending automated replacement gates; native cold reopen and real-remote acceptance remain unfinished. No real user configuration, session or daemon was modified.
    replacement_result: Static missing-coverage RED 12a95b preceded two shared-fixture cases. After correcting a test-only String-to-PathBuf mismatch (d9d180), all six stopped_output tests passed 0a102b. Same-instance control replacement and same-target pending entity replacement independently preserve the current placeholder without changing epoch; old failure does not contaminate the new entity. No production behavior change. Quick/full pending; native and real-remote acceptance remain open.
    replacement_plan: Extend the same paused checked-output fixture with same-instance connection replacement and same-identity pending entity replacement. Keep epoch, target tab/pane/session unchanged to isolate exact client/entity guards. Old output must not land; a fresh placeholder keeps idle Resume without inheriting the old failure. Reuse existing shared fixture and canonical session_to_pane.
    gui_full_gate: Quick passed 12c776 and frozen-source full passed 356229/4a4471, GUI 1947/core 1352/CLI 138 plus source-identity and all server suites. Existing ignored counts unchanged. Shared scripted control capabilities, actual GPUI click-to-grid output, duplicate suppression and paused epoch retirement are verified. Supersedes pending gates for these paths; connection/entity replacement, native cold-reopen and real-remote acceptance remain separate. No real user configuration, provider session or daemon was modified.
    host_guard_result: Regression RED ad0337 additionally found an uninitialized awk attr falsely exempts a first-line non-test module. Require an observed cfg(test) line and recognize private/pub/pub(crate) test modules. All 21 assertions passed 1b71f6, including production-before-test and non-test module refusal; no allowlist expansion. Quick passed 12c776. Frozen-source full pending for GUI replay, paused epoch retirement and shared control fixture changes.
    host_guard_followup: Quick failed ed2c5a because the scanner recognizes only private trailing cfg(test) modules, not the existing pub(crate) tests module. Add deterministic private/public/pub(crate) fixture tests and production-before-test/non-test-public refusal cases before correcting the scanner. Do not add allowlist entries or hide fixture filesystem calls.
    gui_paused_result: Missing stale-test static RED bd37ac preceded paused-response coverage; all four stopped_output tests passed 72cf73. Both buttons remain inert while awaiting output; an epoch/use retirement before the response preserves the exact stopped placeholder, reports unavailable and keeps Resume. Successful replay checks saved-output in the actual terminal grid, same target/tab/pane and acquisition/final-release-only control. No production behavior change or native GUI acceptance; quick/full pending.
    gui_success_result: Static missing-test RED e570be and missing shared fixture capability API RED 3928e1 preceded test-support extension. Three stopped_output tests passed a5f3cd, including real GPUI click to checked socket to saved-output grid in the same tab/pane with retained Resume and exact acquisition/release-only control. Add a paused-response epoch-retirement regression next; native UI remains separate.
    gui_success_plan: Add an isolated-process GPUI mouse-to-checked-socket test, sharing the existing scripted control fixture with explicit opt-in capabilities. Assert saved grid text and retained Resume in the same tab/pane, exact proof and identity, and final tracked release with no Resume RPC. Preserve capability-empty behavior of existing control fixtures.
    current_gate_result: Quick passed 626c0b and frozen-source full passed 6a9c43/633b01, GUI 1945/core 1352/CLI 138 plus source-identity and all server suites including four stopped_resume tests. Existing ignored counts unchanged. Supersedes pending gates for implemented output transport, capability refusal, missing-link GUI recovery and snapshot failure handling only. Actual successful output-to-GUI landing, stale/cancel and native cold-reopen acceptance remain unfinished. Native read-only preflight f56fd3 still reports accessibility_trusted=false and screen_capture_allowed=false. No real user configuration, sessions or daemon were modified.
    snapshot_failure_result: Static missing-coverage RED cc9443 preceded extending the existing isolated daemon test, without production changes. All four stopped_resume integration tests passed 06599c; missing/corrupt/truncated/empty/over-cap requests return Error before geometry/output, leave fixture files and stopped facts unchanged, and subsequent valid reread/Resume succeed. Formatting passed 9f5790. GUI capability and missing-link tests previously passed 3bcd8e; complete successful GUI replay, stale/cancel, quick/full and native acceptance remain pending.
    failure_test_plan: Extend the existing isolated real-daemon Sleep/preview/Resume cycle with missing, corrupt, truncated, empty and oversized snapshots. Assert refusal before frames, no file repair or target mutation, then restore only the temporary fixture and prove valid reread/Resume. No production behavior change is planned for these already-specified failure semantics.
    gui_plan: Explicit View output uses a typed action in the existing stopped recovery coordinator. Keep pending output separate from Resume RPC, gate the pane peer on stopped-output-v1, then reuse canonical terminal construction and landing after exact epoch/client/entity/target revalidation. Failure restores stopped presentation and Resume. Focused feature-gate and mouse/failure-retention tests precede implementation.
    plan: Implement feature-gated stopped-output expectation on the existing checked terminal stream, target proof/stopped-identity validation and non-consuming snapshot replay. Then connect explicit View output through the existing stopped recovery coordinator and renderer. No new process or parallel file transport; native cold-reopen acceptance remains required.
    verification: Missing Stopped expectation RED 595ab5 preceded the target implementation. Real daemon checked-stream test passed 0ba1b1, verifying two identical non-consuming reads after carrier retirement and proof/session/cwd/tab refusal before output, unchanged target facts and later explicit Resume. Initial test fixture referenced unavailable uuid dependency (cfcc1f); use a nil foreign proof instead, without adding dependencies. GUI action, feature-gated client call, malformed/missing snapshot coverage, quick/full and native cold-reopen acceptance remain pending. No real user sessions or configuration were modified.
  - area: explicit_session_sleep
    gui_current_session_full_gate: Quick passed 64b6c2 and frozen-source full passed 168da2/91c28e, GUI 1943/core 1352/CLI 138 plus source-identity and all server suites including four stopped-Resume/Sleep tests. Existing ignored counts unchanged. Supersedes pending single-session GUI gates only; whole-tab Sleep, native/real-remote GUI acceptance and cold output replay remain incomplete. No real user configuration, sessions or daemon were modified.
    gui_current_session_plan: Add an explicitly single-session themed action through existing target SleepSession and tracked workspace use. Capture exact view/epoch/identity/connection, suppress duplicate clicks, preserve output until observed exit, and provide explicit target-only refresh for unknown outcomes. Whole-tab multi-pane Sleep and native automation remain required; this does not substitute for them.
    gui_current_session_verification: Missing-action RED f984de preceded implementation; initial fixture type error corrected without changing assertions. Three focused GPUI tests passed 3b5b24, including actual mouse-to-scripted-control, exact proof/identity, no duplicate stop, accepted-versus-unknown reply, explicit read-only refresh, view retention and tracked release. Connection helper RED ffd486 preceded weak exact-client scoping. A GPUI arena handle failure was fixed by checking the actually rendered button instead of constructing an unrendered element. Quick/full pending; whole-tab Sleep, real provider/native acceptance and cold output replay remain required.
    output_retirement_full_gate: Quick passed b6a696 and frozen-source full passed f2a304/43f1a6, GUI 1940/core 1352/CLI 138 plus source-identity/server suites, including four stopped-Resume/Sleep process tests. Existing ignored counts unchanged. Final bounded output snapshot, private permissions, sealed writer and explicit forget verified; GUI replay and Sleep action remain unimplemented. No real user sessions/configuration or daemon were modified.
    output_retirement_plan: Implemented after real missing-snapshot RED 58efbf. Registry retire_exited saves the final bounded frame before removing the carrier; normal/native spawn now share the existing reaper. Periodic/shutdown/retirement writers share per-pane capture/save coordination, with final sealing and locked explicit forget to prevent stale references recreating deleted output. Writer regression passed f74f7a; four real stopped-Resume/Sleep tests passed e34ba8, including snapshot decode, READY output, mode 0600, carrier removal and explicit deletion. Kill has no reply, so its deletion assertion now awaits the actual bounded filesystem result (7dbc60/d7525f), not just request encoding. Quick/full pending. This is final-output persistence, not GUI replay or the Sleep button.
    target_full_gate: Quick passed ea58b6 and frozen-source full passed eb8e9e/e8f2a4, GUI 1940/core 1351/CLI 138 plus source-identity and server suites, including four real stopped-Resume/Sleep tests. Existing ignored counts unchanged. Supersedes pending target gates only; no GUI Sleep action, whole-tab multi-pane behavior, retained output replay or native automation acceptance is claimed.
    target_implementation: SleepSession v16 routes through existing pane services/recovery module, exact proof and shared binding/slot validation, then existing backend termination. A small shared explicit-stop latch lets DeathReporter distinguish requested stop from ordinary teardown without retaining its reaper closure in DaemonPane. Missing-API RED d8064a; first real isolated Sleep/Resume cycle passed 68cdfa. Runtime guards/wire tests passed 4da8db, natural/explicit exit tests passed adc7ae and all four recovery tests passed 966e71. Initial unit fixture tried to revive a confirmed exit and was correctly rejected (701248); fixture now starts live instead, preserving terminal-exit immutability. Quick/full pending; GUI button, multi-pane scope, output replay and native acceptance remain required.
    status: in_progress
    owner: Codex
    contract: SESSION-SLEEP-22
    decision: Implement exact target SleepSession RPC and explicit DeathReporter intent before the GUI action. Existing kill marks daemon teardown and suppresses durable exit, so it cannot implement Sleep unmodified. Preserve target layout/binding and require actual exit readback.
    next_action: Wire the themed GUI Sleep action and operation lifetime/readback using the target primitive; resolve multi-pane scope (nonblocking question sent), preserve output and add native acceptance. Do not claim whole-tab Sleep from this single-pane primitive or use destructive close as its UI implementation.
  - area: tracked_workspace_uses
    legacy_removal_full_gate: Quick passed 633477 and frozen-source full passed cc5be5/02427a, GUI 1940/core 1349/CLI 138 plus six source-identity and all server suites. Existing ignored counts unchanged. Initial-claim cleanup API/state are deleted; four migrated regressions retain their behavior under tracked uses. No user configuration, provider sessions or real daemon were modified. Supersedes pending deletion gates, not native or mixed real-pane lifetime acceptance.
    legacy_removal_plan: Four core regressions migrated to tracked acquisition/retirement, including a bounded deadline through the canonical implementation. Static deletion RED 11f783 and missing deadline API RED 3c2150 preceded implementation. Migrated tests passed 48380c/ebee92 before deleting the old API, exclusivity flag and bookkeeping. Post-deletion 25 workspace tests passed b3b994; static checks passed 1b17c1 and source search found no obsolete core or GUI callers. Quick/full pending; this removes parallel cleanup, not native/real-pane lifetime acceptance.
    explicit_take_back_full_gate: Frozen-source quick passed 7deb08 and full passed f45003/49c928 with GUI 1940, core 1349, CLI 138 plus six source-identity and all server suites including 55 stdio and three stopped-Resume tests. Existing ignored counts unchanged. Supersedes pending gates for explicit takeover accounting only; native UI, real-pane close/reopen and legacy primitive removal remain required. No user configuration, provider session or real daemon was modified.
    explicit_take_back_plan: Implemented canonical takeover plus exact proof readback under one workspace gate, shared token registration and common window handoff/retirement. Static RED a4083f, missing API RED 9e5651, window-not-retaining behavioral RED b0b8cc. Core tests passed 9c53bc/6df62d; 45 remote-workspace tests passed 4a24a4, including twelve ordinary/explicit scope cases and malformed/foreign proof rejection. Test helper originally used one shared identity; added explicit hello injection to cover both same-token and foreign-token connections, preserving takeover-name expectations. Frozen-source quick/full pending; native and real-pane reopen remain unverified.
    automatic_use_full_gate: Frozen-source full retry passed (24409/7b7afe), GUI 1940/core 1347/CLI 138 plus six source-identity and all server suites, existing ignored counts unchanged. First full stopped during compilation on ENOSPC (68153/90f342), before tests. Read-only disk check found 157 MiB available; package-scoped cargo clean -p tty7 --profile dev removed 12.9 GiB regenerable artifacts (32c43f), leaving about 12 GiB, then unchanged sources rebuilt and passed. No user config, provider files or real daemon were touched. Supersedes pending full for ordinary automatic use, not explicit takeover or native/real-pane reopen acceptance.
    automatic_use_plan: Ordinary pump now acquires WorkspaceUse on HostOps blocking lane, retains the exact use in the validated window before relink/hydrate, or retires stale results through shared cleanup. Removed legacy request/reply adapter and replaced reply-shaped GUI bookkeeping with typed outcomes. Shared Rejoin control fixture moved to test_control.rs; no duplicate peer implementation. Missing API RED (df4d8c), acquisition and four initial scope cases passed (93b0b0/09294e). Explicit disconnect behavioral RED (6dd273) exposed a held-use leak; disconnect now retires window uses after pane detach and before link removal. Final 45 remote-workspace tests passed (d425e9), 17 Rejoin tests passed (fe3db0), fmt/static and quick passed (8dae49). Full pending; native/real pane reopen, mixed consumers, explicit takeover and legacy primitive removal remain open.
    automatic_scope_full_gate: Frozen-source full passed (89885/5b0c83), GUI 1938/core 1347/CLI 138 plus source-identity/server suites; existing ignored counts unchanged. Supersedes scope pending-full notes, not automatic use acquisition/handoff or native close/reopen. No real user config, sessions or daemon were modified.
    automatic_scope_plan: Implemented transient workspace_view_epoch and AttachmentScope (entity/workspace/epoch). Existing retirement invalidates the epoch even without a held use; workspace replacement also retires before changing views. Pump captures the scope, refreshes old sent/inflight eligibility, cancels old view takeover intent and rejects stale completion without ending a new request. Corrected test fixture imports/read context before clean missing-API RED (b7d5d2); final 43 remote-workspace tests passed (d7779a), fmt/static and quick passed (63370c/cd4ae9). An unescaped static regex was corrected to fixed-string matching. Full pending. This remains a receiving-view prerequisite, not automatic WorkspaceUse acquisition/handoff or native close/reopen acceptance.
    automatic_entry_full_gate: Frozen-source full passed (4002/789e8d), GUI 1936/core 1347/CLI 138 plus six source-identity and all server suites, existing ignored counts unchanged. Supersedes pending full notes for entry convergence only. Automatic tracked uses, entity/generation/reopen guards, legacy primitive deletion and native automation remain required.
    automatic_entry_plan: Reconnect now returns Connected only; deleted its captured-window acquisition loop and sent-list return. Pump is the single automatic acquisition entry. Successful completion performs deferred relink/hydration; restart/takeover rebuild intent survives until success. New connections clear old attachment bookkeeping. Completion rejects replaced control clients and closed/remapped workspace targets. Final 41 remote-workspace tests passed (bcd69a), quick passed (2510d0). Missing connection-only API RED (dbcd21) and missing guarded-completion RED (e12b7a) preceded implementation. Full pending; tracked automatic uses and real transport-to-view acceptance remain incomplete.
    rejoin_full_gate: Frozen-source full passed (79233/77cd96), GUI 1934/core 1347/CLI 138 plus six source-identity tests and all server suites, including three stopped Resume process tests. Existing ignored counts unchanged. Supersedes the pending full note below, not successful Rejoin close/reopen or native UI acceptance.
    rejoin_migration: Ticket now carries WorkspaceUse and successful checked UI commit retains it in the existing window holder. Failed preparation/commit drops prepared panes before retirement. Static RED (d24c25), missing release API RED (f3dd76), final seventeen Rejoin tests passed (498e62), core last-user test passed (f6b290), quick passed (7398e9). Shared cleanup test uses an ordered Ping barrier to prove no early release. Existing first/final-attachment cancellation now uses isolated control peers and preserves drop-before-release assertions. Full pending; committed Rejoin close/reopen and native acceptance remain separate.
    entity_release_acceptance: Raw window-removal behavioral RED (872a5a) proved a tracked use survived entity destruction. Tty7App now registers a final-release callback using the existing retirement helper. All six retained_resume_ tests passed (3e9cd4), fmt/quick/full passed (67112/469f80), GUI 1933/core 1347/CLI 138 plus source-identity/server suites, existing ignored counts unchanged. The raw-removal fixture proves use retirement and no late target effects, not caller bookmark cleanup. Normal CloseWindow still asserts saved closed state and exactly one release. Initial format-only check refusal was corrected with cargo fmt before gates.
    status: in_progress
    owner: Codex
    contract: WORKSPACE-USES-21
    decision: Extend the existing per-workspace claim state with tracked use IDs; migrate original-slot Resume and retain its use in the window. Only final owned tracked-use retirement sends conditional release on the blocking lane.
    temporary_debt: Unknown/raw or uncertain claims remain conservatively pinned; explicit Take Back and ordinary attachment use tracked accounting. Old initial-claim cleanup has been deleted after migrating its regressions. Mixed real-pane and close/reopen acceptance remains required.
    removal_condition: All acquisition paths retain and retire tracked uses across commit, cancel, close, reconnect and replacement, verified with real panes as well as isolated fixtures. Initial-claim cleanup removal has passed full gate; native and mixed real-pane acceptance remain open.
    verification: Missing API RED (7c0eb0), then last-owned-use, duplicate/foreign/stale-use and untracked-pinning regression passed (09af12). Window fixtures initially rejected the newly required release (a13a73); their target lease model now demands exactly one release and rejects proof queries afterward, preserving all no-delete/no-second-launch/no-attachment assertions. Two close tests passed (cc8cfe), eighteen resume_ and three old claim tests passed (51209f), fmt/quick/full passed (10581/f66fdc), GUI 1932/core 1347/CLI 138 plus source-identity and server suites. No native or real user session was touched.
    next_action: Implement explicit Sleep with retained target tab and recovery identity, keeping destructive close separate; continue live-shell exit recovery, mixed Resume/Rejoin/automatic real-pane close/reopen and native UI acceptance. Cleanup convergence is complete, not complete product lifecycle acceptance.
  - area: cancellation_claim_exclusivity
    failure_acceptance: Missing-test static RED (a7d4ad), then three workspace_claim-filtered tests passed (6d94f6), fmt check and quick passed (daf958). A paused first workspace does not block a second acquisition on the same client; both fresh claims release independently. Timeout waits for the exact Cancel frame before sending a late reply; a subsequent Ping/Pong establishes that the client processed it before cleanup is checked again. Unexpected and foreign-workspace replies also send no release. Peer completion is acknowledged with bounded channels. Test-only slice; no new product behavior or full gate claimed.
    status: in_progress
    owner: Codex
    contract: WORKSPACE-PROOF-11
    decision: Serialize ownership-changing calls in ControlClient per workspace and allow cancellation release only for a still-exclusive fresh proof. Route Resume and Rejoin cleanup through this one primitive; automatic attachment participates through the canonical call path.
    temporary_debt: Reused claims conservatively remain held until explicit detach or link closure; full view-use accounting is still required.
    removal_condition: Replace exclusivity-only cancellation eligibility with coordinated active and committed view uses, validated across reopen, all-consumers-cancel and link replacement; retain canonical target proof validation.
    verification: Missing API RED (0cfa06) preceded implementation; initial helper visibility and test ClientInner initialization errors were corrected without weakening assertions. Fresh/reused/replaced/foreign-client/explicit-takeover test and sixteen acquire-versus-cancel races passed (b20fab). Actual Rejoin prepare cancellation fixture passed (395a85). Frozen-source fmt, quick and full passed (57531/2a0a7d), GUI 1932/core 1344/CLI 138 plus source-identity and all server suites; existing ignored counts unchanged. No protocol shape change, real user session or native GUI interaction.
    next_action: Replace the conservative exclusivity stage with active/committed view-use accounting before enabling window-close reclamation, covering all-consumers-cancel, commit versus cancel, reopen and link replacement. Paused-RPC timeout, unexpected/foreign replies and independent workspace progress are now covered. Mid-attachment cancellation and real/native acceptance remain separate.
  - area: retained_terminal_resume
    reused_claim_reproduction: Wire-level regression passed (f16f1b), strengthened pane-record assertion passed (5b8a55); static missing-test RED (68a49b) preceded the test. Two same-connection claims share a proof; releasing the original invalidates the reused pane-attachment authority while preserving the tab and pane record. This characterizes existing protocol semantics, not a GUI fix. A fresh flag does not remain exclusive authority after a second consumer reuses the claim.
    lifetime_gate_result: Frozen-source fmt/focused/quick/full passed (29774/1bf053), GUI 1932/core 1342/CLI 138 plus six source-identity tests and all server suites; existing ignored counts unchanged. Also covers both newly added paused CloseWindow cases. No product logic changed this slice; shared ownership-use coordination, mid-attachment cancellation, reopen and native acceptance remain incomplete.
    running_close_acceptance: Live-successor paused CloseWindow test passed (178eba), eighteen resume_ tests passed (0ff9d4), fmt check and quick passed (dde75f). The isolated process pins its pane endpoint before transport use and keeps it listening until the coordinator releases PendingPane; no connection is queued. Existing stopped-close assertions and exact control sequence remain. Static missing-test RED (f83e91) preceded test addition; no production changes or new full gate. Real provider/native, mid-attachment cancellation and lease lifetime remain open.
    lease_inspection: windows.rs release_unused_hosts only removes unreferenced non-local machine links after delete/forget; app.detach_workspace does not release its exact workspace proof. Host server WorkspaceReleaseProof validates connection/workspace/nonce and detaches under the handover lock. Next design must distinguish a saved machine connection from active workspace ownership and serialize release versus same-connection reopen; do not close a shared HostLink as cleanup.
    close_acceptance: Paused-response CloseWindow test passed (27afa3); final seventeen resume_ filtered tests passed (a4360c) and quick passed (472e27). Real GPUI action removes its window/registry/baseline while another registered window remains; the saved view stays closed, late readback is discarded and exact requests contain no deletion or second Resume. Initial release-wait timeout (680c3c) was corrected by flushing GPUI deferred entity destruction at an App update boundary, keeping the assertion. Test-only changes; no product lifecycle fix or new full gate claimed. The scripted successor is stopped, not a real running provider. Lease release, running-successor cancellation, explicit TabClose and reopen remain open.
    control_gate_result: Layout regression plus all seventeen resume tests passed (be210c); quick passed (323c99). Unchanged-source full rerun passed (22362/7706ec), GUI 1930/core 1341/CLI 138 plus source-identity/server suites, existing ignored counts unchanged. Initial full invocation exited before Rust tests (323c99); the filtered capture did not retain its diagnostic, so cause remains unconfirmed. No checker or assertion was relaxed. This supersedes pending gate notes below, not native or cancellation acceptance.
    control_followup: Paused control fixture RED (dad100/622e48) exposed synchronous Resume waits in the GPUI test scheduler; after ten seconds the peer timed out and a liveness probe hit the UI-thread guard. Move recovery control acquisition/readback/release to the existing HostOps blocking lane, preserving semantics and keeping the paused-response assertion. Native UI freezing is not proven by this test. Focused rerun and gates pending.
    control_layout_red: After blocking-lane reuse the retained click reaches successor output, but the full chain fails maximized-state assertion (33d604). Canonical rebuild_tab_from_tree clears maximized unconditionally; preserve it using a shared slot-presentation predicate when the active tab still contains the old view or its retained wrapper.
    control_first_pass: Full retained-view mouse/control/routed attachment test passed (d2d437), including paused reply with retained output, exact guarded successor, terminal output, maximized state and focus. Add preserve-versus-remove regression for the shared layout predicate before final gates.
    cancellation_inspection: close_tab_inner is destructive tab close, not presentation cancellation; it removes the GUI tab and save_session can enqueue target TabClose. Test this separately from window/view detachment, preserving explicitly authorized close semantics. No close/cancellation acceptance is claimed by the retained success test.
    status: in_progress
    owner: Codex
    contract: SESSION-RETAINED-UI-20
    decision: Reuse the existing notice anchor and stopped Resume coordinator, retaining the original TerminalView inside PendingPane until successful landing. Preserve split and maximized presentation; no separate launcher or automatic recovery.
    evidence: Missing rendered action behavioral RED (0f3282), then basic click passed (3255c9). Expanded hidden-focus/maximized-target RED (46b4eb) led to selecting the visible maximized terminal. Two focused tests passed (a518f7), then all sixteen resume-related tests and quick passed (688688). Frozen-source full passed (38984/23c04d), GUI 1928/core 1341/CLI 138 plus source-identity and server suites; existing ignored counts unchanged. Covers stale binding, target-unconfirmed exit, original output, neighbor/ratio retention, unknown Refresh, maximized presentation and canonical landing focus. Initial import/element and test-helper compile errors were corrected without weakening assertions.
    next_action: Add separate destructive-tab-close and non-destructive window/view-detachment fixtures while Resume is in flight, plus reconnect and lease lifetime coverage. Retained-view control-to-attachment is now verified with isolated scripted peers. Live-shell Agent exit, Sleep, restart replay and native/real remote acceptance remain open.
  - area: stopped_resume_ui
    running_control_acceptance: Static presence RED (2af750), then running fixture failed (66800a); backtrace (4b5c3e) proves the GPUI test scheduler polls start_pane_spawn synchronous routed I/O on its UI thread. The existing HostOps blocking worker lane now handles pending terminal handshake, without disabling the guard or injecting synthetic landing. This isolates synchronous work from the GPUI future pool; it is not evidence of a native UI freeze. Final tests and gates passed below.
    running_control_verification: Final five stopped_resume tests passed (90bb28), quick passed (93748a), and frozen-source full passed (73862/4e4819), including GUI 1926/core 1341/CLI 138, six source-identity tests and server suites. Existing ignored counts unchanged. Covers routed Version/Owned AttachChecked, exact successor identity and terminal grid output in the original tab. Fixture corrections preserve canonical --pane and restore accepted Darwin sockets to blocking mode; the process-owned endpoint has scoped RAII cleanup. Real provider/SSH/native window acceptance remains separate.
    control_acceptance_followup: GPUI mouse-to-framed-control immediate-exit acceptance passed (2e85aa), all four stopped_resume tests passed (bd3896), quick passed (338087), and the full GUI package passed 1925 tests with one existing ignored (689780). Exact acquisition, Resume, MachineGet and proof request order is asserted; the original TabId and placeholder survive, both mirrors move to the successor, and one launch leaves Resume available. Initial fixture syntax failure (ad9b54) corrected; this was not a production behavioral failure. No production behavior changed and workspace full was not rerun. Running-successor attachment, lease lifecycle and native UI remain separate.
    running_control_fixture_plan: Exercise the production start_pane_spawn path with an isolated daemon transport endpoint and routed checked-attachment peer; connect_routed uses transport::connect before route negotiation, so a control-only fixture cannot prove terminal attachment. Do not point the fixture at the user's daemon or substitute synthetic Ready views for successful attachment.
    resize_gate_result: Missing comparator API RED (e07ca2) preceded shared same_slots/same_slot implementation; final eleven recovery-related tests passed (ff40b4), then quick and frozen-source full (75929) passed with exit 0. Ratio changes are accepted before submission and after replacement, latest verified target geometry wins, and six identity-boundary changes plus axis/order/neighbor/binding changes reject. Exact ResumeTarget PartialEq was removed to prevent accidental geometry-based identity checks. No native UI or successful GUI transport-to-attachment acceptance claimed.
    resize_followup: Source inspection found exact PaneNode equality in both preflight and reconciliation, so ordinary split resizing invalidates the operation. Add a shared slot comparator ignoring only ratios, with regressions for current target geometry and rejected topology/identity changes. Verification pending.
    gate_result: Final quick and frozen-source full (84110) passed with exit 0. GUI 1923 passed/one existing ignored, core 1341 passed/four existing ignored, CLI 138, source identity six and all server suites passed. This supersedes pending baseline/fresh-facts gate notes, not incomplete product acceptance.
    final_evidence: Baseline API RED (e58195) preceded canonical apply_to_mirror acknowledgment; ten resume-related tests passed (f84789). Behavioral RED (6d5c0a) proved snapshot rollback of a newer target exit/title; current same-successor facts now win, and ten focused tests passed (ccc551). Real daemon Owned attachment, occupied-controller refusal and three process fixtures passed (356039) and again in full. No user daemon/config/provider session was modified or native window driven.
    baseline_followup: First full passed (58282), GUI 1922/core 1341/CLI 138 and all server suites. Review found a stale diff-baseline race when RPC beats Layout; add canonical target snapshot acknowledgment before clearing the GUI hold, then rerun gates. Local acquired-lease lifetime after closing one window while the GUI remains alive also needs lifecycle acceptance; do not mark original-slot delivery complete.
    status: in_progress
    owner: Codex
    contract: SESSION-STOPPED-UI-19
    decision: Connect the stopped placeholder to the existing target Resume RPC; preserve the pending slot across early deltas and use non-preemptive checked Owned attachment for the successor.
    verification: Missing Owned API RED (f08852) and missing GUI API RED (f7f8c9) preceded implementation. Two projection/simulated-click tests passed (e2e502), and eight focused checked-attachment/core codec/ownership tests passed (bd1073). Expanded generation/double-click/hydration test, target integration, quick/full pending. The missing-trait/entity-borrow compile errors (366b40) were corrected; no assertions removed.
    remaining: Scripted control-to-running-terminal and immediate-exit GUI paths are verified, not complete product acceptance. Cancellation/reconnect and Local lease lifecycle need integrated fixtures. Changed split topology while Resume is unresolved remains refused. Ready-view recovery, live-shell handling, Sleep, restart output replay, real provider/SSH and native automation remain required.
    next_action: Extend the recovery entry to retained Ready views and handle immediate post-attach exit, while closing Local lease lifetime and cancel/reopen races. Follow with live-shell recovery, Sleep, restart replay and native acceptance. Structural split changes require explicit reconciliation design; ratio-only changes are supported. Refresh may reacquire a workspace non-preemptively but never starts a process.
  - area: stopped_resume_process_acceptance
    status: automated_verified
    owner: Codex
    gate_result: Final focused rerun (224178) passed all three process tests after output-deadline tightening; final quick (ed73fc) passed. Earlier full server-package run (42593) passed all 109 tests. No production Rust behavior changed this slice; workspace full was not rerun, and native GUI remains unverified.
    decision: Verify the real daemon control-to-PTY boundary before wiring original-slot UI; target layout notification can precede Resume reply and provider runtime identity.
    verification: Missing-test static RED (23dab0), then three real daemon/RPC/PTY tests passed (8af7ea). Quick passed (956ebd); all tty7-server suites passed (42593), 109 tests including 55 stdio conformance cases. Scratch source and executable fixture prove argv/cwd, retained slot, duplicate refusal, immediate exit, persistence rollback and both registry and OS-child cleanup. A test field-name compile error (3ec82b) was fixed before the behavioral run. No installed vendor CLI or native GUI acceptance is claimed. Final output-loop deadline tightening requires a focused rerun.
    next_action: Connect original-slot GUI action and guarded successor attachment. LocalLink does not acquire workspace leases automatically; an explicit Local Resume must non-preemptively acquire its exact workspace, never steal. Layout deltas may precede the RPC reply; provisional runtime identity cannot satisfy Checked Agent. Preserve the pending original slot until safe attachment or authoritative reconciliation, without automatic retry or Kill after unknown outcomes.
  - area: stopped_resume_transaction
    status: in_progress
    owner: Codex
    gate_result: Final frozen-source full (78706) passed with exit 0 after the source-access follow-up; GUI 1920 passed/one existing ignored, core 1340 passed/four existing ignored, CLI 138 passed, source identity six passed and all server integration suites passed. Quick (43971) also passed. This supersedes earlier pending gate notes below, not the outstanding UI and real-process acceptance.
    preflight_verification: First frozen-source full (69395) passed with exit 0, including GUI 1920/core 1339/CLI 138 and server suites. Follow-up source-boundary RED (43564) proved a foreign retained identity could reach the resolver before refusal. Preflight and final allocation now share stopped_session_record; seven focused tests passed (88554) and quick passed (43971). Native-SSH/unknown/foreign/forged/invalid-size carriers reject before source access. Final full pending after this follow-up.
    decision: Target-owned explicit Resume consumes the exact stopped predecessor under the existing layout transaction and attachment handover locks. Direct typed argv creates a fresh PTY; no text injection into a reused prompt and no second durable operation registry.
    verification: Missing API RED (29807) preceded target transaction and control v15 dispatch. Provisional identity RED (94115) reproduced erased recovery binding; target-only resume_pending now preserves same-provider provisional metadata until identity confirmation/replacement or terminal exit, without inventing runtime identity or a ShellSpec. Six focused tests pass (96204), including concurrent/duplicate predecessor consumption, rollback, acquired-proof cancellation/source/slot refusals, direct argv and wire input rejection. Expanded core passed 1322 tests with three existing ignored (68220). PTY resource-order static RED (992683) preceded acquiring reader/writer before child creation. Quick passed (15130); full pending. No real provider or daemon was modified.
    next_action: Connect original-slot GUI action and guarded attachment of the newly launched pane; pending runtime identity is not yet a Checked Agent attachment proof. Add actual target RPC/process cleanup and remote provider integration acceptance. Agent-exited live-shell handling, Sleep and native GUI remain required.
  - area: original_slot_replacement_integrity
    status: implemented
    owner: Codex
    gate_result: Frozen-source full (91438) passed with exit 0, including 1920 GUI tests, 1333 core tests, 138 CLI tests, six source-identity tests and server integration suites. Existing ignored tests unchanged. This supersedes the pre-gate pending note below; original-slot Resume coordinator and native GUI acceptance are not complete.
    decision: Reuse the canonical target owner resolver for replacement and checked attachment. Replacement currently picks the first matching tab and can leave duplicate old leaves without their record. Fix this production primitive before using it for original-slot recovery.
    verification: Behavioral RED (66384) reproduced first-match replacement of a duplicate split leaf. Shared occurrence-based resolver now serves checked attachment and canonical replacement. All 48 MachineStore tests pass (8540), including split/tab/workspace ambiguity, exact tab and nested split preservation, used successor and stale target refusal, no disk/delta effects and write-failure rollback. Static session guard (73160) and quick (14396) passed. A misplaced initial patch failed compilation (75629), was corrected before successful tests; no assertion weakened. Full pending.
    next_action: Prove atomic rejection for ambiguous layout and unchanged slot metadata on success. Then implement target-authorized original-slot recovery with source/binding/proof/generation checks; raw Input is not safe delivery to an existing prompt, and generic PaneReplace alone is not recovery authorization.
  - area: retained_session_resolution
    status: in_progress
    owner: Codex
    gate_result: Quick (37065) and frozen-source full (77303) passed with exit 0. Full includes 1331 core tests, four existing ignored, six source-identity tests, CLI and GUI suites and server integration suites. Existing ignored tests unchanged. Resolver RPC tests use an isolated peer, not a real remote provider installation; original-slot UI and target ownership acceptance remain separate.
    decision: Add target-owned exact identity lookup before original-slot Resume, sharing discovery traversal without recency truncation and preserving unique source/cwd validation.
    verification: Missing API RED (1967) preceded implementation. Three initial focused tests passed (15538); expanded core suite passed 1314 tests with three existing ignored (75628). Shared candidate walker replaces discovery's inline traversal; exact lookup ignores recency/title quotas, rejects ambiguous identity, changed cwd, incomplete/malformed sources, internal/symlink sources and client root injection. Six remote reply scenarios cover exact target success, refusal, wrong provider/UUID/cwd and wrong shape without Local fallback. Control v14 includes codec/deadline fixtures; no real daemon upgraded. Clean removed only 18.1GiB of tty7/tty7-core dev artifacts (61022), preserving user config and sessions. Quick/full pending.
    next_action: Connect original-slot recovery with target ownership, operation generation and idle-shell checks. Include actual resolve RPC coordinator coverage. The new RPC is read-only and does not make the Resume UI complete.
  - area: incremental_stopped_hydration
    status: in_progress
    owner: Codex
    gate_result: Quick (45864) and frozen-source full (80289) passed with exit 0. GPUI/core/server regression coverage is automated evidence, not native UI acceptance.
    resume_followup: Current resume_history_session creates a new tab and cannot implement original-slot recovery unchanged. Cover both confirmed stopped terminals and Agent-exited live shells. Resolve and validate the retained provider identity on the target before materialization; a recent-history quota cannot silently erase an older retained binding. An unknown foreground program is not an idle shell. Preserve original TabId/split slot and reject stale target/operation identity before command delivery.
    decision: Delete incremental direct terminal creation and reuse tree projection plus canonical session renderer, retaining existing views and target-scoped facts.
    verification: Behavioral RED (33730) proved delta lowering discarded exit. Canonical session renderer now handles new delta leaves using only mapped target facts; existing views remain unchanged and the direct terminal-creation branch is deleted. Focused regression passed (64032); expanded tree-sync suite passed all 59 tests (25305), including repeated insertion/restructuring, active tab preservation and missing remote facts versus Local same-ID records. Earlier disk-full compile (77236) resolved by cleaning only 2.3GiB of tty7 dev artifacts (18173); no user configuration or session modified. Quick/full pending.
    next_action: Implement validated original-slot Resume for stopped terminals and proven-idle live shells, explicit Sleep and restart output replay. Native GUI acceptance remains separate.
  - area: stopped_cold_hydration
    status: in_progress
    owner: Codex
    gate_result: Quick (87481) and frozen-source full (73702) passed with exit 0. Full includes 1919 GUI tests, 1327 core tests, 138 CLI tests, six source-identity tests and server integration suites. Existing ignored tests unchanged. Native UI acceptance remains separate.
    decision: Preserve target-confirmed stopped leaves through canonical tree projection and render a no-transport pending surface. Never infer termination from live=false or client data.
    verification: Two behavioral regressions failed (70961). Distinct target-only SessionPane::Stopped now lowers to a stopped pending surface, without Retry subscription or pane task. Fully stopped layouts skip alive-pane transport queries. Three focused tests passed (2083), covering core serialization refusal, tree lowering, tab identity/name/split retention, stopped state immunity to generic retry/failure/start, checked-Rejoin refusal and actual GPUI rendered status. Disk-full compilation (42709) was resolved by cleaning only tty7 dev artifacts, 3.3GiB (10150). No real configuration or session was modified. Quick/full verification pending.
    next_action: Whole-tree and incremental stopped hydration are implemented. Continue validated original-slot Resume, explicit Sleep and restart output replay; native restart acceptance remains required.
  - area: confirmed_terminal_exit
    status: in_progress
    owner: Codex
    decision: Persist target-confirmed exit independently from ephemeral live=false; reject stale runtime resurrection for the same stopped pane identity.
    verification: Persistence RED (1742) lost the exit object on reload; natural-death RED (db1f22) showed no durable record even for an unknown exit code. Added optional target PaneExit and canonical observe_terminal_exit, protected stopped records from delayed liveness/identity/binding mutation, kept seeds and legacy records unknown. Twelve focused exit-related tests passed (30963), including post-stop official title updates. Quick passed (18050); frozen-source full running (13638).
    full_result: Frozen-source full (13638) passed with exit 0, confirmed by terminal handle on 2026-09-10.
    next_action: Consume the confirmed fact in session_from_tree/session_to_pane cold hydration and explicit in-place recovery. Avoid starting a PTY merely to display a stopped row; explicit Sleep and native restart acceptance remain required.
  - area: exit_row_retention
    status: in_progress
    owner: Codex
    decision: Retain terminal rows after natural child exit; separate stopped runtime from target recovery identity before implementing explicit sleep and cold hydration.
    verification: Core RED (43046, 19403) proved stale natural-death identity and orphaned status. GPUI RED (98930) proved removed split leaf and stale automatic launch fields; disconnect control passed. Canonical provider clearing and non-destructive ChildExited observer implemented, unused close_leaf wrapper removed. All 115 pane tests passed (1333). Expanded run (95692) found an older remote test missing temporary config initialization; real config access was blocked, and fixture setup was corrected without changing assertions. Final focused run (91645) passed ten GUI and ten core exit-related tests, including final output, duplicate/removed-view events, snapshot isolation and preserved title/binding. Cargo clean removed only 4.0 GiB of regenerable tty7 dev artifacts (66894). Format (74030), quick (92650) and frozen-source full (99634) passed with exit 0; full includes 1917 GUI, 1324 core, 138 CLI, six source-identity tests and server integration suites. Existing ignored tests unchanged; no real user configuration, provider file or session modified.
    next_action: Define target-owned durable stopped/sleep state and cold hydration; cached live=false cannot distinguish a confirmed stop from daemon-restart uncertainty. Explicit sleep, reopen persistence, in-place Resume and native UI acceptance remain required separately.
  - area: live_codex_titles
    status: in_progress
    owner: Codex
    decision: Implement target-side index invalidation and typed PaneFacts titles, preserving user aliases and exact provider/session ownership. Reuse bounded cold parser and existing machine delta delivery; no whole-history polling or GUI file access.
    verification: Initial missing-model RED (92508), then target watcher and active-label tests passed (36885, 10719). CLI fixture omission caused full failure (7037); fixed and 138 CLI tests passed (45852). Late-name core RED (29141) and GPUI RED (35471) preceded shared binding eligibility and exited-label implementation. All five core title tests and both GPUI title tests pass (57091), including actual file updates after exit/terminal stop, identity replacement, alias priority and foreign-host refusal. Quick (78417) and frozen-source full (4627) passed with exit 0; full includes 1914 GUI, 138 CLI, 1322 core, six source-identity tests and server integration suites. Existing ignored tests remain unchanged. Earlier full retry (75632) correctly rejected a source hash changed by edits during execution; no assertion relaxed. No real user provider file, config or session was modified.
    next_action: Implement explicit dormant-session actions and verify complete target watcher to remote PaneFacts delivery to GUI. History-manager snapshot enrichment and native UI automation remain open; last native preflight lacked OS permissions.
  - area: macos_bundle_metadata
    status: in_progress
    owner: Codex
    decision: Use numeric system bundle versions and retain full Cargo version/source identity in separate diagnostic keys. Share one validated plist renderer with the production bundler; do not alter signing or release policy.
    verification: Actual bundler fixture RED (bd8582) proved a source hash in CFBundleVersion. Four tests/117 assertions passed (30481), parsing generated stable/prerelease/metadata plist values and linting with native plutil. Invalid versions, duplicate JSON keys, wrong field types and source mismatch fail without output. Real bundler staging exercised with dummy payloads, stub helper verification and mock codesign exit 93; no actual signing or DMG was performed. Quick (78417) and full (4627) passed with exit 0, including rerunning all 117 metadata assertions.
    next_action: Bundled helper verification and signed packaging remain explicitly unfinished; this fixture does not prove them. Product priority returns to dormant-session actions and complete recovery/UI acceptance.
  - area: canonical_source_build_identity
    status: in_progress
    owner: Codex
    decision: Separate release/download version from RemoteProtocol source identity. Use one core build-script SHA-256 closure shared by GUI/server, independent of Git and checkout location; preserve dialect compatibility and remote lifecycle.
    verification: Behavioral RED (39c246), then identity/release/dialect regression passed (50154). Six scratch-tree tests passed (25546), including an independently calculated SHA-256 vector, compiled-stamp comparison, mutation/rename/add/remove, missing inputs, symlink and special-file refusal. Actual tty7-server --protocol test passed (98676) from an empty temporary cwd; single JSON output equals compiled identity. Locked builds used existing sha2 dependency without rewriting Cargo.lock. Quick passed (21976); full passed with exit 0 (35577).
    next_action: Cross-platform helper identity validation remains required. This stamp covers the explicit core/server source closure, not compiler flags or artifact bytes. Pane/control hello version labels remain unchanged. macOS CFBundleVersion still needs separation from full source identity; real packaging/signing and native UI acceptance remain pending.
  - area: static_helper_inspection
    status: in_progress
    owner: Codex
    decision: Repair canonical assert-static.sh before reusing it for bundled helper verification; inspector errors must not become successful static-link proof.
    verification: RED (9d11bb) reproduced ignored inspector failure and unrecognized INTERP header. Four fixture tests/12 assertions passed (7c3386), shell syntax passed, quick passed (15470) and full passed with exit 0 (30724). Tests cover static/static-PIE success, each inspector failure with plausible output, interpreter, dependency, non-ELF, dynamic input and missing/extra arguments; no helper executable was run by these fixtures. Full gate separately runs isolated server integration tests.
    next_action: >-
      Real ELF verification remains required. Bundled identity remains unfinished: current client_version returns only CARGO_PKG_VERSION and there is no archive dialect marker, while macOS bundling requires version+revision.sourcehash. Define a canonical build identity before implementing check_bundled_remote_helpers; do not transplant archive grep vocabulary or relax the exact-identity requirement.
  - area: desktop_package_payload_cleanup
    status: in_progress
    owner: Codex
    decision: Align packaged core names, include Windows native server, validate target tuples and isolate stale-stage/final-package cleanup by architecture and format. Preserve product names and signing authority.
    verification: Package contract RED (78467e); three tests/24 assertions passed (8c1dd9, 6de895). Unix fixture runs execute actual macOS/Linux/AppImage scripts until missing-input failure, proving selected stale stages/finals removed and other architectures/formats preserved. Shell syntax checks passed; quick passed (27841); full passed with exit 0 (43495). Windows cleanup and installer payload are source-checked only. No real package, signature, install, or session mutation performed.
    next_action: Implement missing bundled-helper verification and validate real platform artifacts. Windows execution, macOS signing/launch, legacy shared-stage cleanup inventory and native UI acceptance remain pending; do not count source checks as installed-product proof.
  - area: ci_core_lineage
    status: in_progress
    owner: Codex
    decision: Correct nonexistent agentty-server package and helper names in CI/release/nightly while retaining agentty checkout/product names. Validate workflow selectors against the server manifest and runtime helper asset constants.
    verification: Source-contract RED (98ce66) identified incorrect package/output/helper/download names in all three workflows. Two tests with seven assertions passed (aa0282); Cargo metadata confirmed tty7-server and tty7-app targets (ec962b); quick passed (83672); full passed with exit 0 (88045). Workflow edits only replace technical core names; checkout/product names, triggers, permissions and secret references are unchanged. No workflow dispatched or release published.
    next_action: Repair platform bundle scripts and missing check_bundled_remote_helpers/check_package_cleanup/check_version_policy assets; DEVELOPMENT.md is also absent. Desktop packaging remains incomplete until actual artifacts are validated.
  - area: stale_remote_adoption
    status: in_progress
    owner: Codex
    decision: Share target-identity adoption eligibility between history carrier selection and actual remote open; reject conflicts before claim_remote and gate deferred tab activation on successful current-window entry.
    verification: Behavioral RED (15511) showed stale remote listing switched the current workspace despite another registered window. Expanded switcher suite passed 50 tests (68380), covering refused entry result, unchanged source tabs/mappings, closed/current/open mapping eligibility and duplicate mapping refusal. Quick passed (59083); full passed with exit 0 (97048). Shared guard is used before claim_remote and by history_carrier; no real SSH environment was touched.
    next_action: Finish native stale-row click acceptance; target ownership/checked attachment success and cross-machine UI remain separate required verification.
  - area: machine_session_window_conflict
    status: in_progress
    owner: Codex
    decision: Normal mapped-session clicks may not reveal another GUI window. Reject a registered foreign window with a localized existing-style notification; explicit new-window actions remain separate.
    verification: Two-window behavioral RED (93096) proved a normal click changed the foreign window active tab. Corrected guard passed (31738); expanded switcher suite passed 49 tests (45465), including explicit new-window behavior. Quick passed (80615); full passed with exit 0 (60828). Added en/zh/ja notification through existing WindowExt UI; no new visual style or font changes. Native permission preflight remains false for accessibility and screen capture (80093f); native focus/notification acceptance remains unverified.
    next_action: Finish native validation of mapped conflicts; successful cross-machine checked attachment and adoptable-row ownership coverage remain explicitly pending.
  - area: rejoin_prepare_control_cancellation
    status: verified
    owner: Codex
    decision: Exercise production prepare over an isolated real ControlClient with scripted wire responses. Verify cancellation before and after workspace acquisition and rejection of an unexpected acquisition reply.
    verification: Focused test passed (21143) across cancellation during MachineGet, cancellation after fresh/reused acquisition, and unexpected acquisition reply. Calls production prepare with a real Unix-socket ControlClient and scripted peer, asserts exact request sequence and EOF after close. Quick passed (10643). Production code unchanged; this does not exercise successful pane transport or UI commit, and no new full gate is claimed.
    next_action: Cover successful checked pane staging plus late target/proof replacement through the complete coordinator; retain native GUI acceptance as separate pending coverage.
  - area: rejoin_mid_attachment_cancellation
    status: verified
    owner: Codex
    decision: Add deterministic cross-thread cancellation coverage at the first and final attachment boundaries, including drop-before-release and reused ownership preservation. Production behavior is unchanged unless the regression exposes a defect.
    verification: Channel-controlled test passed (62483) for cancellation during first/final attachment crossed with fresh/reused ownership; quick passed (81794). It invokes production stage and release_with with fake attachment resources, not the complete prepare coordinator or real transports. Production code unchanged; no new full gate claimed. Initial build failed from disk exhaustion (50279); Cargo package-scoped dev cleanup removed 19.7 GiB of regenerable tty7 artifacts (57016), then build/test succeeded.
    next_action: Exercise complete prepare coordinator and GUI continuation using isolated control/pane transports; native UI acceptance remains separate and pending permissions.
  - area: staged_rejoin_origin_guard
    status: in_progress
    owner: Codex
    decision: Reject staged commit when origin tab/view layout or resolved remote route changes during attachment; preserve the current window and use existing conditional cleanup.
    verification: Same-workspace reorder regression failed with the previous workspace-only predicate (52990). Non-SSH route rejection regression failed (68818), then fourteen filtered tests passed (74247; thirteen Rejoin tests and one unrelated path-display test). Coverage includes reorder, rename, active selection, pane removal, split topology/ratio, WSL/LocalStdio without SSH and missing SSH profile rejection. Quick passed (98075); final full passed with exit 0 (35964). Remote route equality is wired at the actual commit gate but lacks a profile-change coordinator regression.
    next_action: Exercise pending-view replacement and route/profile edits against the full coordinator, then native UI race acceptance. Current snapshot compares final state rather than a monotonic source-edit generation; edit-and-revert behavior is not covered.
  - area: staged_history_rejoin
    status: in_progress
    owner: Codex
    decision: Stage the complete checked target layout before switching the current window. Discard staged connections before conditional lease release on cancellation/failure; commit already attached parts without a second attachment.
    verification: Neighbor liveness RED (15466); staged commit exposed missing client-store identity and fixture readiness (42423, 9621). Twelve filtered tests passed (85623), including six new planning/staging/cleanup/generation/GUI commit cases; one filtered path-display test is unrelated. Quick passed (67685); full passed with exit 0 (28288), including the strengthened incomplete-commit fixture. The actual history caller now acquires proof, constructs Checked, stages all panes, revalidates target facts and commits existing ShellParts. Old HistoryAction::Rejoin and activate_rejoin_workspace were removed. No real daemon upgrade or native GUI automation was performed.
    next_action: Verify the complete coordinator against isolated live control/pane fixtures and native UI, including timeout after acquisition, cancellation during attachment, late commit, source layout edits and profile-route changes. Native permissions remain unavailable at the last preflight; prepared commit and cleanup tests are not full live acceptance.
  - area: checked_restore_transport
    status: implemented
    owner: Codex
    decision: Carry immutable per-pane guards in the canonical RestorePolicy through split recursion and PendingSpawn; choose checked wire attachment with target capability preflight.
    verification: Two behavioral regressions failed (31050), exposing transport without a guard and unintended Resume delivery. Expanded protocol and pending tests passed (75717); split propagation passed (32570). Quick passed (55368); full passed with exit 0 (90068), GUI 1900 passed/1 existing ignored, core 1316 passed/4 existing ignored, CLI 138 passed and integration suites passed. No actual Rejoin operation yet constructs Checked, so these tests do not prove end-to-end recovery or native GUI acceptance.
    removal_condition: Complete history acquisition and cancellation integration so checked policy is constructed by the actual Rejoin operation, then remove remaining unguarded Rejoin hydration.
    next_action: Actual history staging now constructs Checked off the UI thread and commits complete prepared layouts without a second attach. Finish live coordinator and native acceptance; the old switch-before-hydrate caller has been removed.
  - area: workspace_acquisition_result
    status: implemented
    decision: Return workspace proof and fresh-versus-reused ownership atomically from the canonical IfFree attachment. GUI automatic attachment must reject unrelated success replies.
    verification: Acquisition result RED (32056), GUI foreign-workspace reply RED (95285), then all four IfFree tests passed (80025) and both automatic GUI attachment tests passed (86943). Quick passed (4009); full passed with exit 0 (35104), GUI 1898 passed/1 existing ignored, core 1316 passed/4 existing ignored, CLI 138 passed and all integration suites passed. Control dialect v13; no real target upgrade or native GUI acceptance.
    next_action: Connect acquisition lifetime to checked Rejoin hydration and cancellation. Carry per-pane checked guards through adopt_workspace, tabs_from_session and PendingSpawn including retry; currently the automatic connection caller validates the result but history hydration still uses ordinary pane Attach.
  - area: checked_shell_neighbors
    status: implemented
    decision: Extend the existing checked attachment guard with explicit Shell versus Agent expectations; Shell rejects foreground or residual agent facts under the same pane lock.
    verification: Shell-success regression failed before implementation (20424); seven checked-attach tests passed (18549), including Shell wire roundtrip and rejection of old v2 shape. Quick passed (76492); full passed with exit 0 (11673), GUI 1897 passed/1 existing ignored, core 1315 passed/4 existing ignored and CLI 138 passed. Native automation preflight still reports accessibility_trusted=false and screen_capture_allowed=false (857454); no real GUI automation performed.
    next_action: Carry checked-v3 guards through GUI split hydration and distinguish fresh acquisition from reused workspace ownership before cancellation cleanup. No ordinary Attach fallback is permitted. Client integration remains unfinished.
  - area: checked_attachment_authority
    status: implemented
    decision: Share one Services authority with the daemon pane registry, then hold handover/layout/pane locks in order while validating proof and exact ownership before canonical checked attachment.
    verification: Callback guard RED reproduced forged-proof execution (46067); all six checked-attach tests passed (86151), including layout/handover lock retention, duplicate leaves, separate-authority rejection and v1 payload rejection. Quick passed (19185); full passed with exit 0 (12446), including core 1314 passed/4 existing ignored and all workspace suites. No real daemon was upgraded and no native GUI acceptance was performed.
    next_action: Wire GUI acquisition and checked materialization. Entire split hydration must remain non-preemptive, including non-agent neighbors; the current required agent identity guard cannot yet cover shell-only leaves. Cancellation may release only a newly acquired attachment, never a reused one.
    source_sha256:
      crates/tty7-core/src/daemon/server.rs: 9aab6b6208d49e0726983912df9276773f3643c4d03a60c6bf6b2f2c96e9719a
      crates/tty7-core/src/daemon/protocol.rs: 03b7a2b0379d09e5afc6c29b5ee28f1bdde204d56a389b3f2381378d65e642e8
      crates/tty7-core/src/core/machine.rs: ba844c0723d058fcc21834982c191415cfea3e844adc48ad07c3573c5ab704ae
      crates/tty7-core/src/host/server.rs: 12001685059f79776446be0d9376ba43eeda3f7b76d501e8e22a18675b95f0fe
  - area: workspace_attachment_proof
    status: implemented
    decision: Add connection-bound target attachment proof and conditional release to the canonical handover path. Proof lifetime is one attachment, not the reusable client token.
    verification: Stale-release regression failed when attachment lifetime reused a nonce (34173); both proof tests pass with fresh per-attachment nonces (30398), including forged workspace/nonce and redacted debug/snapshot checks. Quick passed (91216); full passed with exit 0 (35049), including GUI 1897 passed/1 ignored, CLI 138 passed, core 1312 passed/4 ignored and server/integration suites. No ignored tests changed or real remote daemon upgraded.
    next_action: Integrate proof into checked pane materialization using the same target attachment registry. GUI cancellation must additionally know whether this operation acquired a fresh attachment or reused an existing one; simply obtaining a proof does not authorize automatic cleanup of a shared attachment.
    source_sha256:
      crates/tty7-core/src/daemon/control.rs: a9bdbbc3a9f7fad6ae45e1d9adb23e9e8a26b4424c88bf369c9e08af91767f44
      crates/tty7-core/src/host/server.rs: f4d3cb418ef4b348e0de791bb61be0bfb4c8bf8a8ff926c844ddd7955f56bd7f
  - area: checked_pane_attachment
    status: implemented
    decision: Add a typed checked-attach frame to the existing pane stream path. Check identity, liveness and controller occupancy while holding the pane lock, then use canonical replay and stream handling.
    verification: Two pane-state regressions failed before checks (82773); all four checked-attach tests passed (2688), including distinct wire roundtrip and missing-pane server refusal. Quick passed (90728); full passed with exit 0 (29394), including GUI 1897 passed/1 ignored, CLI 138 passed, core 1310 passed/4 ignored and server/integration suites. Existing ignored tests unchanged; no real daemon was upgraded.
    next_action: Carry an operation-scoped workspace ownership proof through the existing GUI restoration path and use AttachChecked there. The GUI still uses ordinary Attach on that path; this target-side capability alone does not make nonresident Rejoin safe or complete.
    source_sha256:
      crates/tty7-core/src/daemon/pane.rs: 58d6d5863f6ddb580ae6b97a9e80ba54143a38d9b2752d3ec476549f0c679f19
      crates/tty7-core/src/daemon/protocol.rs: 470099deb703c36cc63d242a70651c4ec89cb0671b66007243b2245da7e5dff0
      crates/tty7-core/src/daemon/server.rs: f5a79bf4bfe83aace3a7a8547455fa7541cfc4c8ba18afa303b000b1fc1d1f1a
    ownership_design_finding: The daemon constructs Services beside the pane Registry, but the Registry does not retain its attachment registry. Shared ownership validation must use the same Services instance, including handoff startup; creating another control_services instance would create a separate live ownership table. Stdio fallback can serve control in a different process and must not impersonate the pane daemon's lease authority.
  - area: atomic_workspace_attachment
    status: implemented
    decision: Add IfFree to the canonical target handover primitive; automatic GUI pump/reconnect use it, explicit Take Back keeps takeover. Advance control dialect to v11 with no downgrade fallback.
    verification: Occupied and invalid-ID regressions failed before the guard (46659), GUI automatic intent failed before selection fix (73698), UUID alias and same-token Detach failures were reproduced (16500, 17023). All 46 target-server tests pass (78330), GUI intent passes (77172), quick passes (73120). Full passed with exit 0 (36631), including GUI 1897 passed/1 ignored, CLI 138 passed, core 1306 passed/4 ignored and server/integration suites. No ignored tests changed and no real remote daemon upgraded.
    next_action: Integrate ownership-guarded pane materialization and operation-scoped cancellation. Automatic IfFree requests alone do not prevent hydration racing a refusal or later takeover.
    source_sha256:
      crates/tty7-core/src/daemon/control.rs: 27f40ac1f659aa295fb5a7c2568eb83c0a0b388ab65f09b7a111712c830f66f1
      crates/tty7-core/src/host/server.rs: fc54bbaf361bb5ef0a7625964670560323fa1f6dfafcb75acdd3806105066363
      src/ui/remote_workspace.rs: 24214f3a812da7d3e256446288a5b7df3155971b904b3fb5bf31b934e19adbae
  - area: native_ui_automation_preflight
    status: pending
    native_scratch_smoke:
      result: Native macOS startup and SIGTERM/reopen smoke passed for an isolated ordinary shell; not full UI automation, normal Quit, provider Resume/Rejoin or terminal-grid acceptance.
      build: Locked pinned dev GUI build passed d81236 in 34.29 seconds. Existing warnings retained; no Rust source changes. GUI SHA256 bad2802b4833c0c7dc6bfe6a87ad3646d4c70ddc0a62e67ac766fe2b55d078f5 (9173bc).
      scratch: /tmp/agentty-native-smoke-uuDzimiu
      isolation: Fresh --config-dir, updates/CLI installation/provider auto-restore/tray disabled, /bin/zsh -f and scratch ZDOTDIR. Unset inherited TTY7_CONFIG_DIR rather than equating scratch with the machine-owned directory; otherwise legacy-tree adoption could become eligible. Debug build skips startup hook refresh. No real provider or remote environment was launched.
      launch: '/usr/bin/arch -arm64 /usr/bin/env -u TTY7_CONFIG_DIR ZDOTDIR=/tmp/agentty-native-smoke-uuDzimiu /Users/admin/agentty/target/debug/tty7-app --config-dir /tmp/agentty-native-smoke-uuDzimiu'
      before: GUI PID 50846, native window 38573 at 1440x900; debug GUI-owned daemon PID 50881, shell PID 50882 (a0f9d0/11c1d4). This dev path used tty7-app --daemon, not the bundled standalone server.
      after: Exact-command-checked SIGTERM ended only GUI (0ed073). Daemon and shell retained their PIDs and original start time (828dae); new GUI PID 51670 created window 38593 at 1440x900. Assertions preserved workspace 37a36cd9-4ae1-4ab9-ba83-077cc5bd2c5c, tab 42d38446-7863-46e6-9bd5-60f2ffaf3aed, live pane 1 and active view (9fde6a). No duplicate pane was persisted. These facts do not assert a visible terminal grid or provider reattachment.
      cleanup: Stopped only the second owned GUI and the scratch daemon via --config-dir and --stop-daemon. All four recorded PIDs are absent (c815f9); scratch config/state/scrollback retained for review. No broad process kill or directory deletion.
      limitations: Fresh preflight 463dbd still denies Accessibility and screen capture; asynchronous user permission request sent. No clicks or screenshots performed. Both launches emitted an xcrun/Rosetta architecture diagnostic and an IMK mach-port warning, despite creating windows; causes remain unconfirmed and no visual-success claim is made.
    finding: Native Swift preflight returned accessibility_trusted=false and screen_capture_allowed=false (696071). cliclick and appium exist; selected developer directory is CommandLineTools. No real GUI was driven or captured and no privacy permissions were changed.
    verification: Both GPUI jump-bottom tests passed (2820), including rendered hit testing, button hiding, bottom offset and absence of PTY input. This is simulated GUI evidence, not native window acceptance.
    next_action: Turn the recorded isolated native startup into reproducible interaction acceptance once the actual runner has Accessibility and Screen Recording permission. Cover rail/menu/history/bottom scrolling and real provider lifecycle separately; investigate the observed startup diagnostics. Do not equate SIGTERM shell survival with normal Quit or agent Resume acceptance.
  - area: nonresident_rejoin_orchestration
    status: in_progress
    verification: Current rejoin filter passed all 5 selected tests (27528). Duplicate split-leaf regression then failed (1341), and both nonresident selection tests passed after occurrence-based resolution (40604). All 19 history-manager tests passed (50671); presubmit quick passed (16875). This does not verify native UI or atomic target ownership.
    finding: WorkspaceAttach forcibly transfers ownership; MachineGet preflight alone has a takeover race. Duplicate references inside one split tree must also fail selection.
    next_action: Implement target-side atomic reservation and identity-guarded materialization before considering nonresident Rejoin safe to release. The existing orchestration is still work in progress; no native or release validation is claimed.
  - area: full_tree_attach_policy
    status: implemented
    finding: adopt_workspace rebuilds complete MachineTree sessions using ordinary restoration; this bypasses the attach-only policy already used by individual deltas.
    verification: Split regression failed with ReplaceMissing instead of AttachOnly (77519), then passed (7995). Quick/full passed (29146, 54233); full GUI 1894 passed/1 ignored, CLI 138 passed, core 1303 passed/4 ignored, server/integration suites passed. No real target or provider files changed.
    source_sha256: 'src/ui/app.rs:3dc72bbc7f5654e0d2d3b7c7beffd31e63091379ab848331e9516dbde1a7e77c'
    next_action: Implement non-resident history Rejoin lookup and exact target carrier selection using this attach-only adoption path. Real GUI machine switching and provider identity race acceptance remain unverified.
  - area: attach_only_materialization
    status: implemented
    finding: build_pane_from_tree currently invokes ordinary replacement-capable restoration for target layout deltas. Non-resident Rejoin cannot safely reuse it until attach-only intent survives retries and suppresses all launch fallbacks.
    verification: Confirmed-missing replacement RED (55153), pending Resume-command RED (6517), then all three attach-only tests passed (67970). Quick passed (96764); full passed (8096), including GUI 1893 passed/1 ignored, CLI 138 passed, core 1303 passed/4 ignored and server/integration suites. No ignored tests changed.
    next_action: Wire non-resident history lookup to fresh target facts, exact provider/session/pane identity and an unowned carrier in the current window. Workspace hydration must retain AttachOnly intent; switching through ordinary replacement-capable hydration is not acceptable.
    source_sha256:
      src/ui/app.rs: 521f3a094a062443114237ca371ff4a42a63b55d16432ddd3b40104019e229f7
      src/ui/pending_pane.rs: 16e8dd3ef52360e4a5510ad1e07174f2e1da07754e08d4b6f9be890ca5ada0cb
      src/ui/tree_sync.rs: cb87093084902a62f923349502177e392d97f163202ec0f059e933d0c0ed3da7
      src/terminal/view.rs: 554152f9f0affb467d3edbd099bd8cfb0790b37532d02e2919baae1adb3223a1
      src/terminal/remote.rs: 6a6d435218ec50036ee68b9c4150d1c985fdb7bff030871f9f435f5efb07506b
  - area: desktop_io_boundary_extraction
    status: implemented
    verification: Boundary test failed on original GUI filesystem implementation (8adb8f), now passes all 8 assertions. All 14 file-copy tests pass (62396). presubmit quick passes (65605), including clean host boundary and Rust format. No allowlist entry added.
    full_verification: Editor tests passed 5/5 (22914). Full presubmit passed with exit 0 (42861); GUI 1888 passed/1 ignored, CLI 138 passed, core 1303 passed/4 ignored, server and integration suites passed. Existing ignored tests were not changed. This is automated workspace validation, not native product acceptance.
    next_action: Return to attach-only non-resident Rejoin and retained local failure placeholders; native remote drag/upload remains separate acceptance.
    source_sha256:
      src/core/desktop_files.rs: 89085b104213ba42ace83ab4be776d5b142c822badd0bf7c729804ab45fd4617
      src/ui/file_copy.rs: 1847eaa535d311aef2b033bc38477468f2c737d5cc9769c1c89b86cc44c7eec8
      src/ui/code_editor.rs: 171ec70df48db789a89b4297d247a3bffa0de7368010908bb6f016cee239255a
  - area: local_restore_failure_retention
    status: implemented
    finding: new_terminal uses synchronous Result for Local; session_to_pane returns None on error, whereas remote failure retains PendingPane. After rejecting uncertain Attach failures, Local also needs a retained retryable slot so a failed restore cannot silently remove the logical tab.
    verification: Local failure retention RED reproduced missing slot (80749); cancellation cleanup RED reproduced killing an attached existing pane (31772). Both regressions pass (44851). Quick (69343) and full (33477) pass; full GUI 1890 passed/1 ignored, CLI 138 passed, core 1303 passed/4 ignored, server/integration suites passed.
    source_sha256: 'src/ui/app.rs:6104511c51a4881c5499dbb0d1e25da401676790a1ecadda2a4b703f0ceb64d6'
    next_action: Integrate non-resident attach-only Rejoin with target identity and window ownership checks; native Local failure/retry and GUI close/reopen remain unverified. This fix retains an error slot but does not implement dormant-session actions.
  - area: attach_failure_is_not_missing
    finding: spawn_shell_terminal_in falls back to Spawn for every Attach failure; attach_reply_prefix additionally fabricates no-such-pane when an Error frame cannot be read completely.
    status: implemented
    verification: Truncated-frame test reproduced fabricated no-such-pane verdict (62090); classifier RED reproduced foreign-pane replacement permission (2839). All 58 terminal remote tests pass (42668); full GUI passes 1888 tests with 1 existing ignored (55665, 51.69s). Quick and full both stop at five existing editor/file-copy boundary violations (35805, 15049). No real daemon, SSH configuration or provider session was modified.
    next_action: Non-resident Rejoin requires attach-only intent through the canonical materialization path, including a confirmed-missing no-Spawn test and cancellation/ownership checks. Ordinary restore now preserves unknown failures but is not itself Rejoin.
    source_sha256:
      src/terminal/remote.rs: 6bf97e0914a0230f986fb341b66b5a6d815c58da438d668c139560d34dc26db2
      src/terminal/view.rs: 3dcdf36b16119dadfb779d015d02bd8ca6749f5ab9f42a48b3ffad115defb8de
      src/terminal/mod.rs: 9bfd7ba96d59e51e1410dd5d098472f853145bc15ac82a0884eb08884f2e595c
  - area: resident_selection_identity
    finding: switcher_open_tab uses a captured index for resident tabs; reorder can focus an unrelated session. Cross-window activation still opens the owning window and remains a separate unfinished requirement.
    verification: Runnable regression failed with mismatched TabIds (90416), then all 48 switcher tests passed (55584). Initial private-field compilation failure was test setup, not behavioral RED. Quick reference and runner checks pass; five existing editor/file-copy I/O violations still fail the gate (23098).
    next_action: Replace foreign-window reveal with an explicit, identity-checked current-window ownership transaction; do not bypass WindowRegistry or duplicate target panes.
    status: implemented
  - area: missing_behavior_references_closed
    verification: Machine identity focused test passed (35460); scan failure focused test passed (33108); full GUI passed 1881 tests with 1 existing ignored (44550, 16.46s), including all three new regressions. Reference validation succeeds. Full release gate and remote lifecycle acceptance remain incomplete.
    finding: Added actual production-path tests for machine identity across /root, nested repository, another repository and /tmp while session cwd changes; real offline refresh retaining committed history and live tab identities; resident Rejoin rejecting a daemon Exited report without a spawn fallback. These extend coverage without deleting old references. Non-resident attach-only Rejoin remains incomplete.
    gate_result: Quick now passes reference validation and all 6 quality-runner tests, then fails host-boundary checks on code_editor metadata, file_copy local source I/O and settings key-file reads (67409). No allowlist changed. Read-only triage identifies local desktop drop sources and client key files; their local ownership needs explicit canonical boundaries, not an assertion that remote paths were tested.
  - area: resume_consumption_audit
    finding: Existing one-shot test populated no automatic agent fields, but the real Resume path populates both explicit invocation and agent/session fields. Add a regression for duplicate completion with automatic restore enabled and a projection test proving exited recovery metadata never becomes auto-launch identity.
    resolution: Reproduced duplicate codex resume command in test 63637; consuming explicit intent now clears duplicated automatic identity fields, including restored-pane suppression. Seven explicit-action tests passed (91923), including the new persisted exited-binding projection test. Quick now reports three remaining missing references, not four; no unresolved reference was deleted.
  - area: recovery_projection_20260909
    gui_regression: Full tty7-app suite passed 1872 tests with 1 existing ignored (64325, 16.46s). This covers the compiled badge integration and existing regressions, not actual provider exit/restart, live remote recovery or visual badge acceptance.
    finding: Re-observation of prior test handle 31454 returned Unknown process id, so its outcome was not assumed. Fresh core projection run passed both tests (38000); GUI target-authority isolation test passed (66314). Format and agent-session source checks pass. Both presubmit modes still fail with missing script (exit 127). The referenced ASHIDE_FALLBACK_ASSET_MATRIX.yaml is also absent in this worktree; lifecycle reference migration needs this evidence restored, not assumed.
  - area: core_full_suite_20260909
    finding: Full core suite after validation RPC had 1274 passed, 1 failed, 3 ignored. WSL settle timing assertion observed 1.66s under parallel suite; isolated rerun passed in 0.57s. Timing-test isolation investigation remains open; no assertion was weakened. presubmit quick remains unavailable (missing script, exit 127).
  - area: gui_full_suite_20260909
    finding: Full tty7-app suite after branding had 1850 passed, 6 failed and 1 ignored. Three old product-label expectations were updated. Three repo-group folding tests still fail because the machine projection removed that visible grouping; machine-fold parity and old-path deletion remain required, not waived.
    revalidation: Second full GUI run passed 1853 with 3 repo-group fold failures and 1 ignored (session 56140); all branding, title, i18n and history ScanState tests passed. Linux SNI display title subsequently changed without altering its internal id; Linux/Windows runtime remains unverified.
    machine_fold_resolution: Replaced the three obsolete repo-fold UI tests with rendered machine-fold parity tests retaining hidden-row, active-row, unfold and search assertions; additionally verify no tab deletion and no active-content change. Machine test filter passed 62 tests. Removed obsolete directory header renderer and DragGroup; full GUI passed 1856 with 1 existing ignored (session 50170). Subsequent removal of unused header-level Git aggregation awaits focused sidebar verification. Other legacy grouping settings/model/drag helpers still require cleanup.
    cleanup_verification: After removal of header-level Git aggregation, all 28 sidebar tests pass (session 25403), machine wiring guard and cargo fmt check pass. Presubmit quick again reports missing script (exit 127).
  - area: core_test_fixture_isolation
    finding: Full core run found saving_twice_replaces_rather_than_appends failing while isolated rerun passed; scrollback sweep test shares the process-wide temp snapshot directory and deletes other tests' snapshots.
    change: Serialize only the four filesystem snapshot tests with a shared mutex held for each test; production behavior and assertions unchanged.
feedback_policy:
  purpose: Persist actionable user product, interaction, architecture, and quality feedback across attention and session switches.
  allowed_statuses:
    - captured
    - triaged
    - promoted
    - implemented
    - verified
    - declined
    - superseded
milestones:
  - id: CLIENT-IO-BOUNDARIES
    status: in_progress
    items:
      - id: isolated-startup-hook-refresh
        spec: CLIENT-HOOK-03
        owner: Codex
        status: implemented
        verification: 29 hook tests passed789259; frozen-source quick/full18276 passed with GUI1942/core1355/CLI138 and unchanged ignored counts. Source-matched artifacts rebuilt; real isolated release startup, normal quit and reopen preserved stale provider fixture bytes (36a794/a60b78/e63e2b). Quick28912 passed1cb1f9. Native interaction and real provider acceptance remain separate.
        decision: Secondary config startup cannot automatically rewrite machine-owned provider hooks; preserve normal default installation behavior and explicit management.
        next_action: Isolated startup guard acceptance is complete; continue remaining provider/SSH lifecycle and native UI acceptance without expanding this fixture's evidence scope.
      - id: local-key-account
        spec: CLIENT-KEY-01
        status: implemented
        regression: Full tty7-app suite passed 1882 tests with 1 existing ignored (60712, 16.48s). Format and client-credential source check pass. No real keys, user config or OS keychain changed.
        decision: Centralize local key fingerprint I/O in client keychain core; keep credential semantics and remove GUI raw-key reads and obsolete allowlist entries.
        evidence: Scratch regression failed first because canonical local-file account API was absent (55556). Four keychain tests now pass (84111). Connection, prompt save/delete and settings profile cleanup all reuse the core helper; removed obsolete ssh_prompt/ssh_connect read allowlist entries. Quality runner tests pass (6 tests, 37 assertions); quick reference validation passes but host boundary still rejects editor metadata and file-copy I/O (72230).
        next_action: Automated full gate passed (42861); native credential UI acceptance remains separate. No further I/O extraction is pending for this item.
      - id: file-drop-source-ownership
        spec: CLIENT-DROP-02
        status: in_progress
        regression: Full tty7-app passed 1885 tests with 1 existing ignored (99860, 17.21s), including existing local copy mode-bit, replacement rollback and terminal regressions. Format/path-drag guards pass. Quick still fails on known editor/file-copy I/O boundaries (29945); no new allowlist exemptions.
        implementation: File-tree producer now emits TreePathDrag with HostId. Tree, terminal and SFTP consumers preserve local copy/upload and same-host quoted path insertion through existing primitives; remote copy sources and foreign-terminal insertion explicitly reject before path reads. Self-copy comparison now checks destination Host identity. Pure regressions failed first (79105); source identity and simulated GUI same-name rejection passed (62398). Full GUI regression pending; actual cross-window drag acceptance and core I/O relocation remain open.
        owner: agentty
        finding: file_tree emits ExternalPaths even for remote rows while file_copy assumes all sources are local. A same-named local file could therefore be mistaken for a remote drag source; lexical same-directory checks also compare paths across machines without host identity.
        removal_condition: Internal tree drag carries source Host identity; remote-source drops never read client same-name files, and cross-machine equal paths do not suppress an explicitly local desktop upload.
        verification: Add fake-host same-path/collision and rejection regressions before replacing untyped source handling. Preserve copy staging, mode bits, errors and replacement rollback tests.
  - id: TERMINAL-JUMP-BOTTOM
    status: in_progress
    items:
      - id: pane-jump-bottom
        spec: TERMINAL-BOTTOM-01
        status: in_progress
        decision: Reuse jump_to_prompt and native themed Button; no PTY input and no alternate-screen interception.
        evidence: Added native themed Button with ChevronDown and three locales, reusing jump_to_prompt. Two focused tests pass (56760), including rendered mouse click, show/hide, viewport offset, stale animation cancellation, alternate-screen suppression and decoded socket verification of no PTY Input frames. Normal protocol focus/resize frames remain allowed. No actual sessions or user configuration changed.
        regression: Full tty7-app suite passed 1878 tests with 1 existing ignored (67126, 16.47s). Format, terminal-bottom and agent-session source checks pass. Quick remains fail closed on three unrelated missing test references; full release acceptance is not claimed.
        next_action: Manual visual review for narrow panes and editor/notice overlap; continue the original machine/session lifecycle scope.
  - id: CANONICAL-QUALITY-GATE
    status: in_progress
    items:
      - id: workspace-version-check
        spec: QUALITY-VERSION-04
        owner: Codex
        status: verified
        evidence: Executable restored; two isolated tests/65 assertions, real offline metadata, empty Cargo cache probe and quick passed (9add05/f3e259/0d978f). No fixed version or version bump introduced.
        next_action: Actual CI runner and package/runtime acceptance remain separate from local checker verification.
      - id: development-navigation
        spec: QUALITY-NAVIGATION-03
        owner: Codex
        status: verified
        evidence: Current DEVELOPMENT.md and engineering docs index replace missing navigation, without importing obsolete archive model/version rules. Missing-entry/link RED a55ee4; quality fixture suite 9 tests/117 assertions passed 9a1945 and quick b031be. Canonical harness checks required entries and relative links; no runtime or native acceptance claim.
        next_action: Keep only stable policy/navigation here and completion evidence in this tracker; broader user-facing documentation and release/native verification remain separate.
      - id: presubmit-and-reference-integrity
        spec: QUALITY-GATE-01
        status: in_progress
        decision: Restore quick/full entry without dropping existing source checks; fail on stale matrix references and preserve separate runtime/packaging acceptance.
        evidence: Scratch suite initially had 3 failures proving missing runner/checker. After implementation all 6 tests and 37 assertions pass (869dbf); fixtures exercise reference failures, archive exclusion, invalid modes, cwd independence, error propagation, fmt failure short-circuit and locked full workspace invocation. Standalone Rust format check passes. No real Cargo command is executed by fixture shims.
        historical_gate_result: Initial real quick/full failed closed on four unresolved test references; subsequently resolved without waiving checks.
        actual_gate_result: Real quick and full now pass (65605, 42861), including workspace Cargo tests after removing GUI desktop I/O violations. Native product acceptance and missing project/packaging assets remain separate incomplete work.
        unresolved_tests: [machine_identity_never_uses_repo_or_cwd, rejoin_requires_a_live_pane, explicit_exit_is_not_auto_resumable, scan_failure_preserves_rows]
        next_action: Keep the passing automated gate intact while implementing non-resident Rejoin and local failure retention. Audit missing CI check_version_policy and root DEVELOPMENT.md separately; Ashide fallback matrix has been restored. Native acceptance and remaining original product requirements remain open.
  - id: EXPLICIT-REJOIN
    status: in_progress
    items:
      - id: resident-rejoin
        spec: SESSION-RECOVERY-03
        status: in_progress
        regression: Full tty7-app suite passed 1874 tests with 1 existing ignored (52598, 16.75s). Resident UI path only; non-resident/remote attach-only acceptance remains incomplete.
        decision: Resolve current-window live provider/session identity at click time and focus the exact existing pane without command or workspace effects. Remote/non-resident attach-only Rejoin remains required separately.
        evidence: New resident action test failed before the methods existed (20355). Both resident regressions passed (27057); simulated daemon reports prove existing pane ID/tab IDs retained, exact pane focus, rejection of foreign provider/session/Host, disconnected stream and ambiguous duplicate identity. Shared native button and three locale strings added. Format/source guard pass; presubmit quick is still missing (exit 127).
        next_action: Implement non-resident attach-only Rejoin with target-side identity/liveness validation and no spawn fallback; add actual split-pane, stale-click and socket-input assertions for resident action. Current button deliberately says Rejoin open pane and does not claim non-resident support.
  - id: RECOVERABLE-AGENT-IDENTITY
    status: in_progress
    items:
      - id: agent-exited-live-shell-recovery
        spec: SESSION-SLOT-17
        owner: Codex
        status: awaiting_product_decision
        evidence: Existing target stopped Resume cannot serve a live shell, and prompt/agent absence cannot prove input-safe reuse. Design alternatives recorded under SESSION-SLOT-17.live_shell_decision; no destructive replacement or injection implemented.
        next_action: Confirm whether the shell process and pending input must survive explicit Resume, then promote the chosen behavior to a separate contract, matrix and failing tests.
      - id: explicit-session-sleep
        whole_tab_contract: SESSION-TAB-SLEEP-24
        whole_tab_target_evidence: Exact complete-membership preflight, structured partial/cancel results and retained split/output implemented. Real two-PTY cycle 23a16d and frozen-source quick/full 63110 passed. Matching v17 Linux helpers now pass canonical static verification 90fb95. Ordinary-shell policy, whole-tab UI and native/remote acceptance remain open.
        gui_current_session_full_gate: Quick passed 64b6c2 and frozen-source full passed 168da2/91c28e. Three new rendered/control tests pass; no new ignored tests. Whole-tab, real remote/native and cold-reopen acceptance remain open.
        gui_current_session_evidence: Explicit single-session action implemented with existing themed pill/button and unchanged font size. Three GPUI tests passed 3b5b24 after missing-action RED f984de and connection guard RED ffd486. Scripted control verifies exact checked stop, unknown outcome, target-only refresh and final tracked-use retirement; this is not whole-tab or native acceptance. Quick/full pending.
        output_retirement_full_gate: Quick passed b6a696 and frozen-source full passed f2a304/43f1a6, including final output and explicit deletion acceptance. Supersedes pending output persistence gates only, not GUI/native acceptance.
        output_retirement_evidence: Missing-snapshot RED 58efbf; shared sealed writer test PASS f74f7a and four real daemon tests PASS e34ba8. Final output is saved before exited carrier removal, with private permissions and explicit forget. Quick/full pending; no new UI is claimed.
        output_retirement_plan: Preserve final bounded output before exited carrier reaping and prove snapshot remains after carrier removal; reuse existing snapshot format and per-pane writer coordination.
        target_full_gate: Quick passed ea58b6 and frozen-source full passed eb8e9e/e8f2a4, including the real daemon Sleep-to-Resume cycle. GUI action, multi-pane scope, output replay and native acceptance remain required.
        spec: SESSION-SLEEP-22
        owner: Codex
        status: in_progress
        evidence: Missing-API RED d8064a followed by real isolated daemon/PTY Sleep/Resume cycle PASS 68cdfa, wire/guard PASS 4da8db, exit semantics PASS adc7ae, four recovery regressions PASS 966e71. Target primitive implemented; quick/full and GUI/native acceptance pending. No user sessions were touched.
        next_action: Complete whole-tab multi-pane scope, real daemon/remote GUI Sleep-to-Resume acceptance, cold stopped-output presentation and native UI automation. Preserve the verified single-session control path.
      - id: stopped-output-preview
        spec: SESSION-OUTPUT-23
        owner: Codex
        status: in_progress
        evidence: Target non-consuming checked reads, foreign identity and missing/corrupt/truncated/empty/oversized snapshot refusal are verified. Explicit GUI action reaches actual saved grid output in the original tab/pane with retained Resume; paused epoch retirement preserves stopped presentation and duplicate actions stay disabled (72cf73). Quick 12c776 and frozen-source full 356229/4a4471 passed. Native cold reopen and real-remote acceptance remain unverified.
        next_action: Connection/entity replacement passed 0a102b and quick/full d12f84/83547c/c6c18e. Exercise cold reopen and stopped output through the native/real-remote environment. Preserve canonical checked transport and original-slot recovery without new process fallback; Agent-exited live-shell recovery is the next independent product gap.
      - id: tracked-workspace-uses
        legacy_removal_full_gate: Obsolete API/state deleted after regression migration. Quick passed 633477 and frozen-source full passed cc5be5/02427a, GUI 1940/core 1349/CLI 138 plus integration suites. Supersedes pending deletion plan; real-pane/native acceptance remains required.
        legacy_removal_plan: Replace proof-only cleanup regressions with tracked tokens before deleting the old API and exclusivity flag; verify timeout, reacquire, foreign token and independent-workspace cases.
        explicit_take_back_full_gate: Quick passed 7deb08, frozen-source full passed f45003/49c928; GUI 1940/core 1349/CLI 138 and integration suites. Supersedes pending gates for takeover, not legacy deletion or real-pane/native acceptance.
        explicit_take_back_plan: Canonical tracked takeover and exact-proof readback implemented; same holder handles commit/stale/disconnect. Static and missing-API RED a4083f/9e5651, behavioral window-use RED b0b8cc, core focused PASS 9c53bc/6df62d and 45 GUI focused PASS 4a24a4. Quick/full pending. Old primitive removal and real-pane/native acceptance remain required.
        automatic_use_full_gate: Full passed after scoped build-cache cleanup (24409/7b7afe), GUI 1940/core 1347/CLI 138 plus integration suites. Ordinary Resume/Rejoin/automatic use paths now share accounting; explicit takeover, obsolete initial-claim primitive deletion, mixed-consumer/native acceptance remain required.
        automatic_use_test: automatic_attachment_acquires_tracked_use_without_takeover
        automatic_use_evidence: Ordinary auto-use acquisition/current-scope handoff/stale retirement and explicit disconnect cleanup implemented. Final 45 remote-workspace tests and quick passed (d425e9/8dae49), 17 Rejoin tests passed (fe3db0); full pending. Five scope cases include current commit, explicit disconnect, changed epoch, removed registry and removed connection. No native or successful pane attachment claimed.
        automatic_scope_full_gate: Full passed (89885/5b0c83), including existing Resume/Rejoin and window-close regressions. Scope identity and eligibility implemented; tracked automatic acquisition remains the next required migration.
        automatic_scope_test: automatic_attachment_scope_expires_on_view_retirement
        automatic_scope_evidence: Entity replacement with a fixed epoch, retirement with unchanged workspace ID, scope refresh, stale versus current failure completion and cancellation of old takeover intent passed within 43 remote-workspace tests (d7779a); quick passed (cd4ae9). Next wire tracked acquisition and window handoff through this scope.
        automatic_entry_full_gate: Full passed (4002/789e8d), GUI 1936/core 1347/CLI 138 and integration suites. Single-entry prerequisite verified, not complete automatic ownership migration.
        automatic_entry_test: reconnect_leaves_current_workspace_acquisition_to_the_pump
        automatic_entry_evidence: Single pump acquisition path and late connection reply rejection implemented; 41 focused tests and quick passed (bcd69a/2510d0). Next migrate this entry to retained uses with window entity and generation guards, reopen eligibility, and explicit takeover semantics; do not treat convergence as completed lifetime tracking.
        rejoin_full_gate: Full passed (79233/77cd96), GUI 1934/core 1347/CLI 138 and source-identity/server suites. Automatic attachment, legacy primitive removal, committed Rejoin reopen and native automation remain open.
        rejoin_test: rejoin_cleanup_preserves_other_tracked_users
        rejoin_evidence: Tracked Rejoin preparation, cancellation and window handoff implemented; seventeen focused tests and quick passed (498e62/7398e9). Actual successful Rejoin followed by close/reopen and native acceptance remain open.
        entity_release_test: retained_resume_dropped_view_retires_use
        entity_release_evidence: Raw-removal RED (872a5a), six retained Resume tests plus quick/full passed (67112/469f80); final entity callback reuses the idempotent retirement helper without target layout writes.
        spec: WORKSPACE-USES-21
        owner: Codex
        status: in_progress
        evidence: Shared use accounting and Resume window retention implemented. Core regression, both paused CloseWindow fixtures, eighteen resume tests, quick and full passed (10581/f66fdc); GUI 1932/core 1347/CLI 138 plus integration suites.
        next_action: Migrate Rejoin and automatic attachment and validate close/reopen before removing untracked pinning; final entity retirement is now wired and verified separately from bookmark cleanup.
      - id: retained-terminal-resume
        lease_lifetime_next: Establish the same-connection reuse boundary, then design one ownership-use lifetime shared by Resume, Rejoin and automatic attachment; raw delayed proof release cannot safely close a view.
        running_close_test: retained_resume_close_window_never_attaches_successor
        running_close_evidence: Live-successor no-connection regression passed (178eba), eighteen resume_ tests (0ff9d4), fmt and quick (dde75f) passed; no production changes. Reopen/lease lifecycle and native acceptance remain separate.
        close_test: retained_resume_close_window_discards_late_reply
        close_evidence: Paused CloseWindow fixture plus seventeen resume_ tests (a4360c) and quick (472e27) passed; stopped-successor readback cannot recreate a closed view or trigger destructive requests. No native or lease-lifetime acceptance claimed.
        spec: SESSION-RETAINED-UI-20
        status: in_progress
        owner: Codex
        decision: Preserve the old TerminalView as presentation while the canonical target Resume coordinator runs; use the shared blocking I/O lane and slot-presentation predicate.
        evidence: Retained-click missing-action RED (0f3282), wrong maximized-target RED (46b4eb), paused-control failure (dad100) and full-chain maximized loss RED (33d604) preceded fixes. Seventeen recovery tests plus layout regression (be210c), quick and full (22362) passed. Scripted control/routed attachment preserves output before the reply and lands the successor with maximized focus.
        next_action: Verify close/detach/reconnect and real provider/native acceptance; do not count scripted peers as installed-product proof.
      - id: stopped-resume-transaction
        spec: SESSION-STOPPED-RESUME-18
        ui_spec: SESSION-STOPPED-UI-19
        ui_status: in_progress
        ui_gate_result: Placeholder and retained-view Resume/Refresh, scoped preflight, guarded Owned attachment, early-delta hold, baseline acknowledgment, newer-facts preference and blocking I/O are implemented. Latest full (22362) passed, including scripted retained-view control-to-attachment. Native/real provider, lease lifetime, close/reconnect and live-shell recovery remain open.
        status: in_progress
        owner: Codex
        gate_result: Final quick/full passed (43971, 78706). Control v15 target implementation is present; original-slot GUI, guarded new-pane attachment, successful real Resume RPC/provider execution, cleanup acceptance and live-shell recovery remain required.
        preflight_verification: First full passed (69395); source-boundary regression RED (43564), shared preflight/final guard, seven focused tests and quick passed (88554, 43971). Final full pending; original-slot UI and actual provider execution acceptance remain separate.
        decision: Explicit target operation validates and consumes a stopped predecessor; metadata comes from the target, not client seeds. Duplicate requests cannot create another successor.
        evidence: API RED (29807), provisional-binding RED (94115), six focused tests pass (96204), expanded core 1322 passed/three existing ignored (68220), quick passed (15130). Full pending. Target request is implemented; GUI integration, actual RPC/process acceptance and native UI remain unfinished.
        next_action: Deliver target operation and then UI, preserving uncertain-outcome refresh without retry or Kill. Cover live-shell recovery separately, not by treating missing foreground observations as idle.
      - id: original-slot-replacement-integrity
        spec: SESSION-SLOT-17
        status: implemented
        owner: Codex
        gate_result: Quick (14396) and full (91438) passed. Layout integrity only; explicit Resume delivery and UI remain required.
        decision: Harden the existing replacement primitive using the same occurrence-based target ownership validation as checked attachment; do not create an alternative layout mutation path.
        evidence: Duplicate-split RED (66384), all 48 MachineStore tests passed (8540), static guard and quick passed (73160, 14396). Full pending; actual Resume coordinator and native GUI remain unfinished.
        next_action: Test duplicate old leaves, stale workspace, used successor, persistence rollback and exact tab/split preservation. Follow with original-slot Resume coordinator and UI; neither layout validation nor source lookup alone completes Resume.
      - id: retained-session-resolution
        spec: SESSION-RESOLVE-16
        status: in_progress
        gate_result: Quick (37065) and frozen-source full (77303) passed with exit 0; original-slot UI, complete RPC coordinator and real remote acceptance remain separate.
        decision: Resolve retained provider identity independently of recent history quotas on the target; no process effects.
        evidence: Missing API RED (1967), focused pass (15538), all core tests pass (75628); source lookup and Local/Remote Host/control integration implemented. Quick/full pending.
        next_action: Original-slot recovery integration and complete RPC coordinator verification; do not equate a validated source locator with liveness or permission to inject into a shell.
      - id: stopped-cold-hydration
        incremental_status: in_progress
        incremental_gate_result: Quick (45864) and frozen-source full (80289) passed with exit 0. Native incremental UI acceptance remains separate.
        incremental_evidence: Behavioral RED (33730), focused pass (64032), all 59 tree-sync tests pass (25305). Delta insertion now shares session lowering/rendering and retains existing views; quick/full pending.
        spec: SESSION-COLD-HYDRATION-15
        status: in_progress
        gate_result: Quick (87481) and frozen-source full (73702) passed with exit 0; native UI acceptance remains separate.
        decision: Carry a target-only stopped leaf through the canonical renderer without scheduling transport. Preserve identity and split topology.
        evidence: Two behavioral RED tests (70961); three focused core/GUI tests pass (2083), including rendered stopped status and retained original tab/split identity. Quick/full pending; native acceptance not claimed.
        next_action: Implement validated original-slot Resume, output replay and explicit Sleep; whole-tree and incremental hydration now share the canonical renderer.
      - id: confirmed-terminal-exit
        full_result: Full (13638) passed with exit 0.
        spec: SESSION-STOP-FACT-14
        status: in_progress
        decision: Target-only optional exit record, durable across reload and terminal for its pane ID. No GUI seed promotion or liveness inference.
        evidence: Persistence and real death-path RED (1742, db1f22); twelve focused core tests passed (30963), quick passed (18050). PaneExit survives target reload, seed injection is ignored, delayed facts cannot revive the old pane, and late titles remain allowed. Full running (13638).
        next_action: Complete full verification, then implement no-PTY cold hydration, explicit in-place recovery and Sleep. Persisting the fact alone does not make startup cold-safe.
      - id: exited-row-retention
        spec: SESSION-EXIT-RETENTION-13
        status: in_progress
        decision: Natural process exit preserves the existing row and terminal output; explicit close remains destructive. Runtime identity clears without losing target recovery binding.
        evidence: Core RED (43046, 19403), GUI RED (98930); 115 pane tests passed (1333). Natural death clears runtime before Exited while keeping recovery binding/title; GUI keeps existing tab, split views, output and selection. Expanded regression (91645) passed ten GUI and ten core tests after correcting missing temp config initialization in an existing remote test (95692). Quick (92650) and frozen-source full (99634) passed with exit 0; native and cold-reopen acceptance remain open.
        next_action: Implement explicit sleep and durable cold restore, then validate native UI. Current live=false is reset on daemon startup, so it cannot alone authorize dormant hydration or replacement.
      - id: target-recovery-binding
        spec: AGENT-BINDING-08
        status: in_progress
        projection_plan: Project typed recovery identity and bound-pane liveness from the target tree into TabView, then share a localized informational badge between sidebar and switcher. Running agents outrank recovery badges; no badge triggers side effects.
        compatibility_verification: All 138 CLI binary tests passed (session 23230), and full tty7-app passed 1871 tests with 1 existing ignored (session 48394, 16.61s). Format/static source check pass. presubmit quick/full remain unavailable (missing script, exit 127). The persisted binding is implemented; dormant UI, resume consumption, migration/downgrade acceptance and real lifecycle validation remain unfinished.
        evidence: PaneRecord now carries optional AgentRecoveryBinding with provider/session/cwd/argv, populated only by daemon observed facts via observe_agent_identity. Exit preserves it independently of cleared runtime agent facts; unknown new identity clears it. PaneSeed never supplies it; missing old field defaults to None. Both new tests failed first (session 79105), then passed (session 82557). Full core serial suite passed 1284 tests with 3 existing ignored (session 55890, 40.78s). CLI and GUI compatibility checks pending. No real provider files or configuration modified; dormant UI and explicit recovery consumption remain unfinished.
        projection_verification: Shared informational badge is wired into sidebar, machine rows and switcher. Core tests prove bound-pane liveness independent of other split panes and live-agent precedence; GUI test proves unknown and unavailable remote mappings never expose Local recovery metadata. Real rendered badge geometry, stale runtime facts and explicit recovery consumption remain to be accepted.
        next_action: Define and implement dormant and stopped-tab explicit recovery actions through target validation; verify real exit/reopen and cross-window lifecycle. Source audit confirms close_tab_inner kills all pane processes, removes the row and pushes an undo snapshot, while reopen_closed_tab uses ReplaceMissing and a new TabId. These cannot implement sleep-with-row-retention unchanged. Do not treat the informational badge or persisted identity as a working Resume or Rejoin action.
      - id: foreground-provider-replacement
        spec: AGENT-IDENTITY-07
        status: in_progress
        core_verification: Full core serial suite passed 1282 tests with 3 existing ignored (session 47648, 39.06s). Serial mode retains prior workaround for unrelated parallel WSL timing interference; no assertions changed. Formatting and scanner source check pass; presubmit quick/full both unavailable (missing script, exit 127).
        gui_verification: Full tty7-app suite passed 1871 tests with 1 existing ignored (session 73226, 16.50s). Runtime identity isolation is implemented and regression-tested; dormant binding persistence and real lifecycle acceptance remain unfinished.
        evidence: apply_agent clears session only when new provider is None, allowing direct provider replacement to retain the previous session ID and argv. Separate dormant binding storage remains unimplemented.
        next_action: Add failing provider-transition test, clear old runtime identity before publishing replacement, and verify core regression without altering exit semantics or real sessions.
        implementation: apply_agent now clears old session/status and argv on every provider change before publishing the replacement identity; same-provider observations still preserve rich state. Typed foreign-provider hook events are rejected, and unknown provider establishment reuses apply_agent. Two regressions were reproduced first (sessions 66358 and 46878), then all 110 pane tests passed (session 49949). Full core/GUI verification pending; dormant binding persistence remains separate unfinished work.
  - id: NATIVE-SESSION-TITLES
    status: in_progress
    items:
      - id: history-title-subscription
        spec: SESSION-HISTORY-TITLE-26
        status: in_progress
        owner: Codex
        decision: One target-owned bounded metadata subscription for previously validated history rows, independent of pane existence; reuse IndexSource and canonical source validation. No new writable registry or parallel discovery parser.
        next_action: Verify native rendered title updates and real provider/SSH delivery. Target, Host wire and HistoryManager lifetime/projection are implemented; full96752 and final52 history tests cover close/refresh/late scans and identity guards. Fresh v18 package is recorded under history_v18_package. Do not recreate the consumer or treat ordinary-shell reopen as title acceptance.
      - id: pane-independent-title-source
        spec: SESSION-LIVE-TITLE-12
        status: in_progress
        owner: Codex
        decision: Extract the existing source engine without duplicating watcher/parser code or adding an unused public API; preserve current pane binding publication and no-read-without-pane-interest behavior at its consumer.
        verification: Missing-source API REDd16fb0 and static RED8d766b preceded implementation. Five focused live-title tests passed160fa3 including no-pane filesystem title changes, partial cache preservation and replacement. Frozen-source quick/full60830 passed19a7d5 (GUI1932/core1356/CLI138 plus integrations). The full run exposed a preexisting route-fixture config leak; corrected fixture then passed its three focused testscd9d93. History subscription/UI remains unimplemented.
        next_action: Complete target history interest/subscription and Host transport, then open-manager identity/search/selection projection and cancellation tests. A source test with no pane is not the final user-visible live-title feature.
      - id: codex-live-title-projection
        spec: SESSION-LIVE-TITLE-12
        status: in_progress
        evidence: Target watcher reuses bounded index parser; separate provider title is session-bound and published by MachineStore PaneFacts. Core/real filesystem tests passed (36885); active-label GPUI passed (10719). CLI fixture failure in full (7037) repaired. Expanded five core/two GUI title tests passed (57091), quick (78417) and frozen-source full (4627) passed. Earlier source-stamp failure (75632) came from edits during validation and was rerun without relaxing assertions.
        next_action: Complete remote daemon-to-GUI acceptance, explicit dormant lifecycle and history-manager projection; do not equate simulated UI with native automation.
      - id: codex-late-title-after-exit
        spec: SESSION-LIVE-TITLE-12
        status: in_progress
        decision: Keep target-bound title enrichment eligible after provider exit or terminal stop. Share the target identity predicate between watcher selection and commit; GUI accepts exited titles only after target and runtime identities agree. No launch or lifecycle effect.
        evidence: Core RED (29141) proved missing post-exit title publication and index update; GPUI RED (35471) showed agentty instead of the late official title. Implemented one PaneRecord binding predicate for source selection/commit/display and agreed-exit GUI projection. Five core/two GUI title tests passed (57091); quick (78417) and frozen-source full (4627) passed with exit 0. No source liveness or binding is manufactured and no user alias is changed.
        next_action: Native exited-title acceptance remains required. History-manager live projection, complete remote transport and explicit sleep/resume remain separate required work.
      - id: codex-native-title-reader
        spec: SESSION-TITLE-06
        status: in_progress
        evidence: Native-title merge implemented in shared target scanner. Five new title tests and all 15 discovery/adapter tests passed (session 5439). Index read bounded to 32MiB/10000 records with regular-file checks; malformed/incomplete source fails whole generation. Header-only session identity validation no longer depends on having a prompt excerpt. No real provider files modified. Full core serial run pending; realtime watcher and GUI aliases not yet integrated.
        verification: Full core serial run passed 1280 tests with 3 pre-existing ignored (session 16163, 37.46s). Serial mode avoids known parallel WSL wall-clock-test interference, without changing any assertions. Formatting and scanner source gate pass; presubmit remains absent. Does not verify live title-update latency.
        next_action: Read bounded target-owned Codex index, merge last-appended title by UUID, verify header-only recovery and error preservation. Follow with target-side metadata invalidation, alias priority and shared manager shell.
  - id: PRODUCT-BRANDING
    status: in_progress
    items:
      - id: bundled-helper-static-gate
        spec: BUNDLED-HELPERS-06
        owner: Codex
        status: verified
        evidence: Missing script implemented using shared native protocol parsing and existing static ELF gate, with architecture/source marker checks. Four fixture tests/47 assertions and metadata 117 assertions passed; quick 497be7 and full a2877f/d078a7 passed. No actual foreign helper execution or artifact attestation claimed.
        real_artifact_verification: Both real helpers passed the canonical checker and independent static ELF linkage checks with GNU readelf (98a646); hashes and source identity unchanged. This closes the static-gate item only, not the parent PRODUCT-BRANDING milestone.
        next_action: Linux runtime, native desktop package and installed-product verification remain required before delivery.
      - id: gui-name-core-lineage
        spec: PRODUCT-BRAND-01
        status: in_progress
        next_action: Unify GUI display strings and preserve tty7 technical identity; repair inherited packaging scripts which currently expect absent agentty binary targets.
        evidence: GUI menus, About title, three locale files, tray, notifications, fallback titles and Windows product resource now display agentty. Upstream link, crate names, binary names and storage paths unchanged. Product-branding test and source guard pass; complete packaging and rendered verification remain pending.
  - id: MACHINE-WORKBENCH
    entrypoint_retirement:
      spec: MACHINE-ENTRYPOINT-25
      status: source_cleanup_verified_native_acceptance_pending
      owner: Codex
      next_action: Source retirement and final helper cleanup passed quick/full18274. Refresh final native package, finish remaining guide audit and native menu/keyboard plus real vendor/SSH acceptance. Existing DMG e51f8d15 has isolated ordinary-shell lifecycle evidence only and predates final helper deletion; do not relabel it as current-source acceptance. Canonical core/CLI layout operations and persisted data remain intact.
    status: in_progress
    items:
      - id: direct-machine-session-list
        spec: MACHINE-RAIL-01
        owner: Codex
        status: verified
        evidence: Section/name algorithms and synthetic group keys removed. Sidebar 9 and reorder 12 focused tests pass, including real GPUI release with changed tabs/search plus unchanged successful reorder.
        verification: Frozen-source quick/full session 36752 exited 0. Old rendering path removed, legacy data preserved; no native or real-SSH acceptance claim.
        next_action: Continue multi-pane Sleep and native/remote lifecycle acceptance.
      - id: obsolete-group-mutations
        spec: MACHINE-RAIL-01
        owner: Codex
        status: verified
        evidence: Menu/create/rename/regroup and cwd/custom inheritance removed after static and detach REDs. Detach identity and sole-pane refusal pass; sidebar 16/reorder 11, quick and full session 64394 pass. Native interactions and the remaining group-shaped render scaffolding are separate unfinished work.
        next_action: Simplify direct machine-session rendering and preserve legacy decoding without reintroducing GUI group mutation.
      - id: obsolete-directory-settings
        spec: MACHINE-RAIL-01
        owner: Codex
        status: verified
        evidence: Static RED e77691 and search behavior RED c25e28 preceded removal of the obsolete settings control, mutation method and six translation keys.
        verification: 46 settings tests passed 0aa85c, quick passed 7a1b7d and full session 8225 exited 0 (GUI 1950/core 1352/CLI 138 plus integration suites). Settings control, setter and translations removed; no user data migration or native interaction performed.
        next_action: Custom-group tab-menu/drag/rename cleanup remains open and must delete its old production consumers, not only hide them.
      - id: workspace-attachment-proof
        cancellation_exclusivity_status: implemented
        cancellation_failure_tests: [workspace_claims_progress_independently, workspace_claim_uncertainty_prevents_tracked_cleanup]
        cancellation_exclusivity_evidence: Shared ControlClient ownership-call coordination and canonical Resume/Rejoin cancellation release passed two target regressions, actual Rejoin cancellation, quick and full (57531/2a0a7d). This protects later acquisitions; complete view-lifetime reclamation remains in progress.
        lifetime_boundary: Same-connection reuse is not a new view claim; the old proof can release it. Wire regression added under WORKSPACE-PROOF-11; client lifetime coordination remains required before delayed close cleanup.
        lifetime_evidence: Wire regression and target tab/pane retention passed (5b8a55), then quick/full passed (29774/1bf053). No production cleanup fix is claimed; Resume, Rejoin and automatic attachment must coordinate active uses before release.
        spec: WORKSPACE-PROOF-11
        status: implemented
        evidence: Connection-bound proof issuance and conditional release share canonical handover/detach. Lifetime regression, forged/foreign proof checks, redacted debug and snapshot checks pass (30398); quick/full pass (91216, 35049). Control dialect v12, no deployment.
        next_action: Consume the new atomic acquisition proof and fresh-versus-reused marker in checked GUI hydration and cancellation; target receipt alone does not complete Rejoin.
      - id: checked-pane-attachment
        spec: PANE-CHECKED-ATTACH-10
        status: implemented
        evidence: Target state predicate, checked protocol frame and server dispatch implemented through canonical replay/stream handling. Four focused tests and quick/full passed (2688, 90728, 29394).
        authority_verification: Checked v2 now guards proof and unique layout while sharing control Services with the pane Registry on normal/handoff startup. Six focused tests and quick/full passed (86151, 19185, 12446).
        next_action: GUI proof acquisition, newly-acquired versus reused cleanup ownership, and checked split hydration including shell neighbors remain required; ordinary Attach is unchanged.
      - id: atomic-workspace-attachment
        spec: WORKSPACE-OWNERSHIP-09
        status: implemented
        evidence: Canonical target primitive distinguishes IfFree from explicit takeover. GUI pump and reconnect now select non-preemptive attachment unless Take Back is explicit. Connection identity and canonical UUID guard registry/tree transitions, including Detach.
        verification: Target 46 tests, GUI request selection, quick and full passed (78330, 77172, 73120, 36631).
        next_action: Complete pane-level guarded hydration before claiming nonresident Rejoin safe.
      - id: nonresident-rejoin-orchestration
        spec: SESSION-RECOVERY-03
        status: in_progress
        next_action: Staged checked attachment, acquisition cleanup and prepared UI commit are integrated. Finish isolated live control/pane coordinator tests, race/cancellation acceptance and native GUI automation before closing this item.
      - id: full-tree-attach-policy
        spec: SESSION-RECOVERY-03
        status: implemented
        evidence: adopt_workspace passes AttachOnly through tabs_from_session and recursive session_to_pane; startup and explicit reopen pass ReplaceMissing. No eligible target pane in AttachOnly can launch a native SSH replacement or issue Resume. Full gate passed (54233).
        next_action: Non-resident Rejoin orchestration and native acceptance remain required.
      - id: attach-only-materialization
        spec: SESSION-RECOVERY-03
        status: implemented
        evidence: RestorePolicy is carried through canonical synchronous/pending creation and retry. Target layout delta attachment uses AttachOnly; missing ID rejects before allocation, all Attach failures stay errors and Resume commands are suppressed. Three focused tests and full gate passed (67970, 8096).
        next_action: Non-resident history Rejoin integration and native acceptance remain required; current-window remote hydration is not yet attach-only.
      - id: local-restore-failure-retention
        spec: SESSION-RECOVERY-03
        status: implemented
        evidence: Failed Local restoration retains PendingPane with original pane/cwd/owner/session; initial error does not schedule another attempt. Existing retry subscription and completion are reused. Late restored completion only detaches; cleanup kills only newly created panes. Full gate passed (33477).
        next_action: Native failure/retry/close-reopen acceptance and non-resident Rejoin remain open.
      - id: restore-attach-failure-boundary
        spec: SESSION-RECOVERY-03
        status: implemented
        evidence: Canonical spawn_shell_terminal_in propagates all unknown Attach failures through restoration_attachment; only complete exact-pane missing refusal permits ordinary replacement. Full GUI 1888 passed, 1 existing ignored (55665).
        next_action: Native disconnect/reconnect acceptance and non-resident attach-only Rejoin remain required.
      - id: stable-resident-selection
        spec: MACHINE-SESSION-02
        status: implemented
        evidence: Removed captured-index parameter from shared switcher_open_tab and all callers. Exact resident TabId lookup handles reorder and rejects removed targets. All 48 switcher tests pass (55584).
        next_action: Native interaction acceptance and current-window cross-machine activation remain required; this does not complete machine-session navigation.
      - id: shared-manager-presentation
        spec: MACHINE-SCAN-04
        status: in_progress
        current_slice: Share native search-row rendering; add target-snapshot filtering and identity-based selection with keyboard navigation. Query changes cancel pending Resume without rescanning.
        search_verification: All 24 history-filtered GUI tests passed (session 28475, 1.50s). New tests cover metadata search, visible identity reconciliation, actual typing/Up-Down/Enter/Escape and scrolling to the last of 30 rows. Initial regressions caught whitespace matching, propagated Enter inserting a newline, and off-card keyboard selection; fixed without weakening assertions. Shared native Input search and cursor-step primitives now serve switcher and history. Source gate and formatting pass; full GUI rerun pending. Real IME, focus traversal and manual visual acceptance remain unverified.
        search_full_verification: Full tty7-app suite passed 1867 tests with 1 existing ignored (session 61799, 16.56s), including old switcher interactions and locale parity. presubmit quick remains unavailable (missing script, exit 127). Cross-machine recovery and live provider updates remain separate unfinished requirements.
        verification: Full tty7-app suite passed 1863 tests with 1 existing ignored (session 43721, 16.62s). presubmit quick was attempted and failed with exit 127 because the script is absent. Rendered geometry tests do not replace manual visual, accessibility or live-target acceptance.
        next_action: Extract shared native card, scrim and viewport geometry; test narrow/short viewport bounds. Then converge search and keyboard primitives without importing workspace business logic.
        evidence: Shared manager_shell now supplies switcher list/form and history card chrome, scrim and responsive geometry. Regression first failed on old 320px minimum/120px top layout (session 34896); all 3 focused layout/render tests then passed (session 92439), including actual card bounds at 1440x900, 768x560 and 375x300. Formatting and shared-consumer source check pass. Full GUI rerun pending. Search and keyboard reuse remain unfinished; this is not a complete FEEDBACK-016 delivery.
      - id: launcher-terminal-submenu
        spec: MACHINE-LAUNCHER-05
        status: in_progress
        verification: sh script/check_terminal_menu and focused tty7 tests; UI verification pending
        evidence: Default action and alternate-shell submenu are wired in NewTabMenu. Launcher uses switcher_groups machine projection. Post-integration compilation and both the_new_tab_menu tests passed with installed nightly-2026-07-31 (session 71014, exit 0). Rust 1.93 fails in pinned gpui cold_path dependency. Source guards and cargo fmt --check pass; rendered UI and machine lifecycle tests remain pending. script/presubmit is absent in this checkout.
      - id: shared-machine-rail
        spec: MACHINE-RAIL-01
        status: in_progress
        next_action: Replace obsolete repo-fold UI regressions with equally strong rendered machine-fold tests; retain legacy data compatibility while deleting unused directory grouping UI and drag paths.
        decision: Reuse switcher_groups and existing workspace activation/RemoteLinks; retain target-owned legacy WorkspaceIds beneath machine projection. Remove visible repository header layer, preserve active tab controls.
        current_slice: Persist machine pin/fold preferences through client Config only; pinned empty machines remain in switcher_groups without supervision. Compact current rows without altering font sizes, and nest saved SSH configurations.
        evidence: render_machine_rail and launcher share switcher_groups and machine_link_label. Pin/fold use client Config.machine_rail; pinned empty targets are projected without transport, Local remains first. Active rows use 18px avatar/6px gap/2px vertical padding with unchanged fonts. Saved configurations are nested and use managed-host connection, not a native SSH pane. Removed unused workspace_head and disconnected machine_rail.rs scaffolding. Four preference tests passed (session 86389), plus 38 machine-related GUI tests including live projection/dedup/no-connect-on-pin (session 92610). Presubmit quick was attempted and failed with exit 127 because script/presubmit is absent. Rendered UI, cross-window ownership, history scan, recovery and full gate remain unfinished.
      - id: session-projection
        spec: MACHINE-SESSION-02
        status: planned
      - id: session-recovery
        spec: SESSION-RECOVERY-03
        status: in_progress
        integration_plan: Validate selected target source first, choose an unowned carrier in the current window through switcher_groups and WorkspaceStore, then bounded asynchronous hydration wait guarded by operation/Host/carrier identity before canonical new-pane Resume.
        ownership_slice: Audit carrier eligibility against explicit WorkspaceStore identity and WindowRegistry ownership, including existing client mappings behind adoptable remote listing rows. Missing workspace is not implicitly Local.
        ownership_regression: Full tty7-app suite passed 1871 tests with 1 existing ignored (session 88687, 16.45s). presubmit quick remains unavailable (missing script, exit 127). This verifies eligibility and existing GUI regressions, not live multi-window or remote recovery acceptance.
        ownership_evidence: Regression first failed because an unresolved candidate was accepted as Local (session 49381). Group.history_carrier now verifies store/target-mirror identity, Host match, open/preempted flags and WindowRegistry ownership; stale adoptable remote rows check all existing mappings by target workspace identity. Three carrier tests pass (session 4504), including foreign mapping, stale open=false and stale adoption behind an occupied mapping. Format and source gate pass. Full GUI rerun pending; live multi-window activation and successful remote materialization remain unverified.
        cross_machine_regression: Full tty7-app suite passed 1870 tests with 1 existing ignored (session 39013, 16.59s). Format and history source guard pass. presubmit quick was attempted and remains absent (exit 127). Full GUI regression does not prove real target recovery or all cross-window ownership cases.
        cross_machine_progress: Removed the pre-validation foreign-machine rejection. Successful target validation now calls activate_history_machine, reuses a non-open/non-preempted carrier or creates one through WorkspaceStore, switches within this window, and polls authoritative readiness before canonical pane creation. ResumeContinuation checks operation UUID, Host Arc/connectivity, carrier identity and preemption on each tick; cancellation/timeout stops without creating a pane. Polling is bounded to 200 nonblocking 100ms intervals. All 27 history tests passed (session 62756), including unknown-machine no effects, current-machine identity preservation and real poll-path cancellation/stale generation/timeout/Host replacement/no-pane checks. Full GUI rerun pending. Cross-window ownership-conflict fixtures, successful target materialization, real remote acceptance and rollback coverage remain unfinished; no real daemon was changed.
        next_action: Deliver explicit typed Resume only to the returned new PaneSlot; pending delivery is one-shot, independent of automatic-restore preference, and suppressed for an already-restored live pane. Connect manager validation and cross-machine hydration afterwards.
        current_slice: Typed Codex/Claude invocation, source-bound validation, manager generation guard and explicit pending-pane delivery; retain source row on failures and scope every effect to the selected machine.
        evidence: Target validation RPC and manager generation/host-instance guards now connect explicit Resume to canonical new_tab_with_cwd and its returned PaneSlot. Ready slots receive the typed command directly; pending slots retain one-shot explicit intent for land_pane. Foreign-host action currently fails closed until cross-machine activation/hydration is implemented. Full pre-shell GUI suite passed 1860 tests with 1 existing ignored (session 3881). Real target Resume, Rejoin and dormant lifecycle remain incomplete.
      - id: target-history-scan
        spec: MACHINE-SCAN-04
        status: in_progress
        current_slice: Wire machine-header on-demand scan to a native list/detail manager with host-instance and operation-generation guards; future action menu entries disabled without handlers. Resume materialization is the next dependent step, not claimed complete by the read-only panel.
        integration_progress: Machine header now opens target-scoped history manager. It runs HostOps scans, verifies HostRegistry instance plus fresh UUID before committing, and keeps previous rows on error. List/detail and disabled Delete/Fork/Convert menu are rendered; all three locales added. Resume remains visibly disabled as implementation-in-progress, to be replaced by source validation and explicit new-pane delivery before release. Four initial tests pass; actual rendered-card coverage and full GUI rerun pending.
        integration_verification: Full GUI run passes 1858 tests with 1 existing ignored (session 96898), including offline manager rendering/closing, locale parity and reserved handler-free actions. Formatting and history-manager static wiring checks pass. Real target discovery/visual inspection and Resume remain pending. presubmit quick still unavailable (missing script).
        decision: One Host.agent_sessions boundary with target-only bounded JSONL scanning; typed provider/session/source records. Port archive failure semantics, not its multi-provider framework. Remote roots are resolved server-side, not supplied by GUI.
        verification: Target-root fixtures, malformed source, internal session filtering, missing stores, source limits, head truncation and remote RPC parity; history manager UI/generation reducer still pending.
        evidence: Target scanner and Host local/remote/server adapters implemented; ControlRequest/Reply AgentSessions uses dialect v10 and a 20s RPC deadline. Nine focused discovery/remote isolation tests passed. Full core suite passed 1272 tests with 3 pre-existing ignored after snapshot-test isolation fix (session 81725). Format and scanner source checks pass. GUI panel, stale-generation reducer, Resume validation/materialization and rendered verification remain pending; no real remote daemon was upgraded.
feedback:
  - id: FEEDBACK-20260909-018
    summary: 交付前记得运行 UI 自动化，不能仅以编译和组件测试代替真实窗口验收。
    status: captured
    areas: [machine_rail, history_manager, terminal_bottom, gui_acceptance]
    verification: Native macOS permission preflight repeated on 2026-09-10 (c6bc0b) returned accessibility_trusted=false and screen_capture_allowed=false. Run through arch -arm64 because the unqualified Swift invocation failed under Rosetta (72d5ee). Three new Sleep GPUI tests passed 3b5b24 and latest frozen-source full passed 168da2/91c28e; real desktop UI automation remains unverified pending permissions and an isolated runner. Preflight used AXIsProcessTrusted and CGPreflightScreenCaptureAccess without requesting or changing permissions.
    decision: 使用隔离配置和测试会话，覆盖多机器展开与切换、历史面板与显式 Resume/Rejoin、终端上滑后回到底部。分别报告组件测试和真实 GUI 自动化结果，不操作现有真实会话。
    next_action: 建立原生 GUI 自动化入口和可复现验收记录；保存运行命令、源码或构建标识及截图。未完成或受权限限制的路径明确标注未验证。
  - id: FEEDBACK-20260909-017
    summary: 终端上滑后显示快速回到底部按钮，复用已有 UI 样式。
    status: implemented
    area: terminal_scrollback
    decision: 在非备用屏幕且离开底部时显示原生主题按钮；点击仅滚动当前 pane 到底部并取消残留滚动动画，不发送终端输入；到底部自动隐藏。
    next_action: 按 TERMINAL-BOTTOM-01 实现并验证可见性、点击、动画取消及无终端输入。
  - id: FEEDBACK-20260909-016
    summary: 历史发现和管理外壳复用此前 workspace 管理/切换器的样式。
    status: promoted
    area: history_manager_visual_reuse
    decision: 复用浮层、卡片、左右栏比例、搜索、选中态和键盘交互的视觉与基础组件；不复用 workspace 创建、目录分组或窗口归属的业务逻辑。当前独立 history_manager 卡片需收敛到共享样式，避免长期两套外壳。
    next_action: 提升到 MACHINE-SCAN-04 的共享外壳契约，再提取 switcher 与 manager 共用原生布局组件并验证窄窗口与键盘操作。
  - id: FEEDBACK-20260909-015
    summary: 参考 /Users/admin/ashide 最新会话命名逻辑；读取 Codex 官方名称，并在命名完成后及时更新。
    status: promoted
    area: native_session_title_freshness
    decision: 已只读核对 ashide 的 cli_agent_store parse/source_watcher 及 discovery_tests。Codex session_index.jsonl 的 thread_name 是正式标题来源；同 ID 最后追加的有效名称优先于时间戳，正式标题优先于 rollout 首句。官方 App Server 有 thread/name/updated，但当前终端承载不能假设能订阅它。拟由目标 Host 观察命名源变化，按已知会话身份更新标题而非每轮重扫成员；保留用户手动命名、上一有效标题和连接/代次边界。
    next_action: SESSION-HISTORY-TITLE-26 已补齐无 pane 历史会话的 target-to-manager 实时标题链路及失败/取消测试，见 open_history_live_title_gap 的最新验证字段。继续做真实 provider、原生 UI 和远端标题更新验收；不把旧的“没有订阅链路”调查结论作为当前状态。
  - id: FEEDBACK-20260909-014
    summary: 内核继续叫 tty7 保留上游血缘，面向用户的 UI 产品名改为 agentty。
    status: promoted
    area: product_branding
    decision: GUI 显示名与内核身份分离；不改 crate、CLI/server、协议、配置与会话路径，不删除上游归属。打包使用真实 tty7 构建产物，外层产品展示 agentty。
    next_action: 按 PRODUCT-BRAND-01 完成界面、多语言、打包显示与兼容标识测试。
  - id: FEEDBACK-20260909-013
    summary: 历史会话入口定位升级为历史会话管理，未来考虑 Resume、删除会话文件、Fork 新会话、转换为其他 Agent 会话；先预留，首期仍只实现发现与 Resume。
    status: triaged
    area: historical_session_management
    decision: 用户进一步要求预留 UI，避免后续重构。历史管理采用会话列表加详情/操作区；Resume 为主操作，更多菜单预留删除、Fork、转换项，首期均禁用并明确标注“规划中”，无事件处理器和副作用。已明确的 live Rejoin 保持独立语义。记录 provider/session/source 身份供后续扩展，不实现或调用删除、Fork、跨 provider 转换；不承诺不同 provider 间无损转换。
    next_action: 历史管理首期实现可扩展的行选择、详情区与 action menu，并测试预留项禁用且没有可执行路径；后续分别设计删除范围与恢复策略、provider fork 能力和跨 provider 转换语义后，再提升为独立实施项。
    related:
      - FEEDBACK-20260909-010
      - FEEDBACK-20260909-002
  - id: FEEDBACK-20260909-012
    summary: 左上角 + 按钮需适配 Machine Rail：统一管理当前已连接环境/机器的状态与连接操作；新终端入口过多，一级仅展示默认终端，其他 shell 或项目入口收进折叠二级菜单。
    status: triaged
    area: machine_launcher_and_new_terminal_menu
    decision: + 与 Machine Rail 共用机器身份、选中状态和连接状态，不另建第二套 workspace 概念。一级只保留当前机器的默认“新建终端”和“其他终端”二级菜单；其他 shell 不再平铺，也不重复列出默认 shell。菜单注明当前目标机器，shell 清单来自该机器；离线时不得偷偷在本机创建终端。环境区标识当前机器及真实连接状态，已连接机器点击仅激活，离线机器提供显式连接；Pin/Disconnect 等管理动作放进对应机器的操作菜单，已保存 SSH 配置不得冒充已连接环境。连接和断开复用左栏同一入口，Disconnect 不停止目标进程。未要求新增项目或工作目录入口。
    next_action: 已确认 src/ui/tab_strip.rs 的 NewTabMenu::build 平铺全部 shell，new_tab_menu_rows 使用保存的 SSH 配置而非连接态。实施前补齐 SPEC/matrix 与测试，覆盖默认 shell 未检测到、远端清单未返回、无额外 shell、离线目标、菜单打开后断线及重复激活；保留现有新建终端快捷键与分屏修饰键，不缩小字号。
    related:
      - FEEDBACK-20260909-011
      - FEEDBACK-20260909-009
  - id: FEEDBACK-20260909-011
    summary: 左栏会话标签需要更紧凑，减少空白与重复占用；不得通过缩小字体实现。
    status: captured
    area: session_row_information_density
    decision: 保持当前正文/标题字号可读性，优先压缩头像、行高、垂直 padding、分隔间隙和重复 provider/branch 信息。会话行采用单行标题为主，状态以紧凑 icon/pill 表达；目录保留为可辨认的次级信息（可内联截断、hover/detail 或展开态），不得仅因压缩而丢失会话归属。Machine header 与会话行使用不同密度层级。
    next_action: 在 Machine Rail SPEC 中定义行高、最小触控/点击目标、文字预算、目录可见性和窄侧栏行为，并以视觉基线验证中英文长标题与 waiting 状态。
    related:
      - FEEDBACK-20260909-010
      - FEEDBACK-20260909-009
  - id: FEEDBACK-20260909-010
    summary: 每个 Machine workspace 展开多个会话；workspace 右侧提供扫描图标，按需发现该环境下的 agent 历史会话，并对候选会话提供显式 Resume 或 Rejoin。
    status: captured
    area: machine_workspace_session_actions
    decision: Machine 节点下投影 live、dormant 与历史候选会话。扫描仅在目标 Host 上读 provider store；live pane/session 由 daemon/Host 事实提供。Rejoin 仅附着仍由目标 daemon 持有的 live pane/session，保持原进程且不执行命令；Resume 针对已结束/未附着但 provider 可恢复的历史会话，新建 pane 并走 provider 的 canonical resume invocation。若 live identity 不存在则 Rejoin fail closed；若 provider 会话不可恢复则 Resume 明确失败。两者均在当前选中 Machine 的内容区 materialize。
    next_action: 定义会话行状态、扫描结果合并、Rejoin/Resume 资格判定、命令文案与 target-side tests；首批实现 Codex 和 Claude。
    related:
      - FEEDBACK-20260909-009
      - FEEDBACK-20260909-007
      - FEEDBACK-20260909-002
  - id: FEEDBACK-20260909-009
    summary: 澄清多 workspace 左栏意图：workspace 仅指示一台 Machine/Host，不承载项目或目录；多台机器并列常驻左栏，可 pin，并可显式连接/断开。
    status: captured
    area: machine_rail_workspace_semantics
    decision: 以 Machine/Host 为每个左栏 workspace 的唯一身份，一台机器至多一个可见 workspace 节点；Local 固定存在，远端节点由 pin/最近使用/显式添加管理。pin 只控制节点常驻，不隐含连接或自动重连；Disconnect 仅断开本客户端到目标机的 transport，不停止目标机 daemon、pane 或 agent，节点保留离线状态并提供显式 Reconnect。内容区始终只绑定当前选中 Machine；目录、repo、agent session 均为该机器下的次级事实。
    next_action: 以此替代 FEEDBACK-20260909-003 的“多 workspace”模糊定义；为 Machine rail、pin/connect/disconnect、单一活动 Host、远端事实失联及跨机器 agent 恢复补充 SPEC 和测试矩阵。
    related:
      - FEEDBACK-20260909-008
      - FEEDBACK-20260909-006
      - FEEDBACK-20260909-003
  - id: FEEDBACK-20260909-008
    summary: Workspace 展示、休眠标签、左栏组织等客户端配置应存于本地，不要求目标机器保留这类用户界面配置。
    status: captured
    area: client_workspace_state_ownership
    decision: 客户端本地存储 workspace/Host 的可见性、顺序、折叠、休眠标签与显式用户命名等 UI 意图；目标机/provider 保持 agent transcript、provider session、live pane、实际 cwd/进程和 hooks 等事实。本地 dormant binding 仅是带身份的恢复意图/缓存，重开远端会话必须经目标 Host 验证并执行，不能以本地记录伪造远端成功。跨客户端同步不是首期承诺，若未来需要必须另行设计同步事实源。
    next_action: 定义本地 durable UI state 与 target facts 的数据模型、断线/删除/过期处理及恢复验证。
    related:
      - FEEDBACK-20260909-007
      - FEEDBACK-20260909-006
  - id: FEEDBACK-20260909-007
    summary: 探索“休眠标签”：将已停止或主动休眠的 agent 标签保留为可再次打开的会话入口；需区分 agent 的显式 /exit 与产品主动休眠。
    status: promoted
    area: dormant_agent_tab_lifecycle
    decision: /exit 不自动恢复；绑定保留与退出行保留已分别提升到 AGENT-BINDING-08、SESSION-EXIT-RETENTION-13。显式休眠仍须在停止前持久化 typed intent，且不得从已清空 runtime 猜测身份。恢复按 SESSION-RECOVERY-03 明确区分 Rejoin 与 Resume；早期“attach 失败即 resume”设想已被失败不降级的规范替代。
    next_action: 退出保留已通过 full（99634）；继续实现显式休眠、目标持久化停止状态、重开后的冷标签投影及原标签显式恢复。当前保留行和恢复绑定不算完整休眠功能，原生 UI 尚未验收。
    related:
      - FEEDBACK-20260909-006
      - FEEDBACK-20260909-002
  - id: FEEDBACK-20260909-006
    summary: 后续取消 workspace 的目录概念：一个 workspace 即一台机器；目录属于 pane/agent 会话，并由 agent/tab UI 展示，而非作为 workspace 根或身份。
    status: captured
    area: workspace_machine_identity_model
    decision: 作为 FEEDBACK-20260909-005 的替代方向。Workspace 绑定 Host/Machine，不持有 project root；首个 pane 使用目标机正常 shell 起始目录。Agent/session 行显示其有效 cwd，历史会话按 Host 发现和恢复。文件树、Git、目录操作必须显式由活动 pane/session cwd 或用户选择的路径锚定，不提升为 workspace 事实。
    next_action: 在实施前将该决策提升为 Workspace/Machine SPEC，并审计当前 WorkspaceId、远端 workspace、左栏 Repo Group、文件树与持久化中隐含的“workspace root”假设。
    related:
      - FEEDBACK-20260909-005
      - FEEDBACK-20260909-003
      - FEEDBACK-20260909-002
  - id: FEEDBACK-20260909-005
    summary: 新建远端 workspace 仅选择 Host 与名称；root 用户连接时默认落入 /root，未提供项目目录选择。Repo 分组将 /root 显示为 ROOT，用户 cd 到其子目录仍留在 ROOT，强化了 workspace 根目录的误解。
    status: superseded
    superseded_by: FEEDBACK-20260909-006
    supersession_reason: 用户随后明确取消 workspace 目录概念，改为机器身份。保留此处最初问题和候选方案作为历史，不再实施 workspace 根目录选择器；会话 cwd 仍按当前 MACHINE-RAIL-01 契约处理。
    area: remote_workspace_initial_directory
    decision: 当前 create_remote_workspace 接收远端 home 但未将其建模为持久化、可编辑的 workspace root；新 pane 以无 cwd 请求创建并退回 daemon cwd/HOME。应将 workspace root 与 tab 的 cwd/Repo Group 分离；创建/打开远端 workspace 时提供目标目录选择或最近目录，并明确其只决定首个 pane/文件树默认锚点，不替代实时 pane cwd。
    next_action: 不再提升本条目录选择候选；按 FEEDBACK-20260909-006 和 MACHINE-RAIL-01 验收机器归属与会话 cwd。
    related:
      - FEEDBACK-20260909-004
      - FEEDBACK-20260909-003
  - id: FEEDBACK-20260909-004
    summary: 远端左栏将 tab 的 Git/目录分组显示为“ROOT 1”，用户误解为 remote workspace 根目录；在 pane 中 cd 后仍留在该组时，分组依据与实时 cwd 的关系不透明。
    status: superseded
    superseded_by: FEEDBACK-20260909-009
    supersession_reason: 后续明确使用机器侧栏并删除目录分组，而非改名或修补 ROOT 分组。原始现象保留，旧分组重构方案不再执行。
    area: sidebar_workspace_repo_grouping_clarity
    decision: 当前 Repo Group 与 Workspace/Environment 身份混用视觉层级。后续多-workspace 左栏设计须将 workspace 身份、Host 身份和 repo/directory 分组明确区分；需核实 remote cwd 更新及 Git probe 后，跨非同一 repo 目录是否会重新分组。
    next_action: 按 MACHINE-RAIL-01 验收不存在目录一级分组；cwd 变化不得改变机器身份。
    related:
      - FEEDBACK-20260909-003
  - id: FEEDBACK-20260909-003
    summary: 将 workspace 由切换器改为左栏常驻多 workspace 列表，减少显式切换成本；各 workspace 可展示身份与必要摘要，点击后在同一窗口就地激活。
    status: superseded
    superseded_by: FEEDBACK-20260909-009
    supersession_reason: 多 workspace 的模糊描述已被用户进一步明确为多机器并列。按后续机器模型实现，不恢复旧 workspace 管理行为。
    area: multi_workspace_left_rail
    decision: 候选方案应保持单一内容区活动 Workspace authority；左栏是多 workspace 的导航与缓存摘要，不让非活动 workspace 的本地缓存冒充其 Host 或 pane 实时事实。需与 Environment 维度明确分层，避免把 workspace、远端 Host 和历史会话混为同一树节点。
    next_action: 按 FEEDBACK-20260909-009 及 MACHINE-RAIL-01 完成原生多机器交互验收。
    related:
      - FEEDBACK-20260909-001
      - FEEDBACK-20260909-002
  - id: FEEDBACK-20260909-002
    summary: 每个 Environment 的右侧提供按需“历史会话”按钮；用户点击后发现该 Environment/Host 的 provider 历史会话，选择一条即在当前窗口恢复为新 pane。
    status: captured
    area: lightweight_environment_session_discovery
    decision: 作为 FEEDBACK-20260909-001 的优先候选。按需发现面板不替换当前 pane 生命周期或全局 Session Navigator；发现结果是目标 Host 的短生命周期投影，恢复统一走现有 pane materialization 与 provider resume command。
    next_action: 定义 Environment/Host 作用域、刷新/缓存/失败展示、Codex+Claude 首批 provider、历史行到当前窗口新 pane 的身份与 cwd 传递契约。
    related:
      - FEEDBACK-20260909-001
  - id: FEEDBACK-20260909-001
    summary: 评估在当前 tty7 版本上二次开发，选择性融合历史归档 Agent Session Navigator 的 provider 会话发现、环境级状态和恢复能力。
    status: promoted
    area: agent_session_architecture
    decision: 后续“落地以上提及的所有开发项目，先计划后实施”已授权当前明确范围。沿 MACHINE-SCAN-04、SESSION-RECOVERY-03 等现行契约实现，保留终端优先及目标事实归属，不整体搬回历史 UI、Environment 或 RPC 模型。
    next_action: 当前实现与验证沿对应 milestone 和最新 verification_notes 继续收尾；原生权限、真实 provider/SSH 验收及明确标记的产品选择仍需单独补齐，不能从一般实施授权推导破坏性替换或远端部署范围。
